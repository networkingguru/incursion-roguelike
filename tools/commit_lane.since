# The commit the lane rule starts after. Everything at or before it is exempt.
# See AGENTS.md, "Classifying a change".
#
# 0662d6e grandfathers the pre-rule gamepad/help/build commits that predate
# consistent lane: tagging; the lane rule is enforced only for commits after it.
0662d6e
