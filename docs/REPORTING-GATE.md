<!-- citations: this-port -->

# Reporting gate

Rules for claiming a bug is real and that a fix is the real fix. They apply to
anything public: an issue on `networkingguru/incursion-roguelike`, a comment on
one, and above all anything sent to `rmtew` or `HexDecimal`.

The point is narrow. Upstream has no reason to trust us. A single confident
report that turns out to be wrong, or a patch that moves a bug instead of
removing it, costs more credibility than five good reports earn.

## The four questions

Answer all four in writing before the claim goes public. A missing answer is a
blocking result, not a caveat to add later.

### 1. Reachability chain

State the call path from a user action to the defect, with file and line at
every hop. "This cast is wrong" is not a chain. This is:

```
MakeLev.cpp:1108   builds a Door
  -> ft->PlaceAt(...)
  -> Display.cpp:56   Thing::PlaceAt
  -> Display.cpp:162  m->Things.Add(myHandle)   (only T_MAP is excluded)
  => every dungeon with a door puts a Door* in the list Augury walks
```

You MUST build the chain before filing, not after somebody asks for it.

### 2. Provenance

Ask: would this misbehave on Win32, with the original typedefs, on the upstream
compiler?

- **No** -> the defect is ours. It MUST NOT go upstream. The save-corruption
  bug fails here: `*((long*)&hm)` was harmless on Windows, because `long` is 4
  bytes there. Our typedef narrowing created the overrun.
- **Yes** -> it is upstream's. Say what makes you certain. Best evidence is the
  codebase arguing against itself -- a comment or guard that shows a previous
  maintainer met the same trap elsewhere.

  The example that used to sit here was `Map::besideWall()`'s comment about an
  out-of-bounds check, offered as evidence that `Monster::ChooseAction()` walks
  into the same trap. **That was withdrawn on 2026-08-18** (inc-5xn): measurement
  showed the ambush scan never made an out-of-bounds read in 79 sessions, and
  `Creature::Walk` rejects the ring by coordinate. A comment proving somebody
  once hit a trap does not prove the site you are blaming reaches it. Use such a
  comment to establish the defect is upstream's, never to establish which caller
  triggers it.

### 3. Blast radius

Two checks, both after the fix compiles:

- **Siblings.** Grep every caller of the function you touched. Fixing the one
  call site named in the report, while a sibling has the same defect, is a
  band-aid. Prefer the funnel where one exists.

  The worked example here used to be `Map::GetAt()`, described as "the funnel
  every accessor uses". **Both halves were wrong** (inc-5xn, 2026-08-18):
  `Map::GetAt` walks a square's contents list, and the lookup that actually goes
  out of bounds is `Map::At` beneath it, so guarding `GetAt` covered 8 reads out
  of 47,962. Counting callers is not enough -- confirm the function you guard is
  the one on the failing path.
- **What is newly reachable.** A fix changes which branches run. Re-read the
  surrounding code and ask what now executes that did not before.

  This is not hypothetical. Adding the missing `isItem()` guard to `Magic::Augury`
  made a *second* defect more reachable: before the guard, any door set `o` to
  non-NULL, so the `o == NULL` branch rarely ran; after it, an item-free map
  reaches a backwards `goto` and hangs. Shipping only the cast fix -- the one the
  compiler found -- would have converted upstream's crash into a hang.

### 4. Confidence tier

Every report MUST carry one of these, stated in the report, in these words:

| Tier | Meaning |
|---|---|
| **Observed** | An oracle changed state. A log went quiet, a counter dropped, a crash stopped. Numbers before and after. |
| **Traced** | Reachability proved by reading code. Never executed. |
| **Reasoned** | Neither. A hypothesis, however well argued. |

**Only Observed goes to `rmtew` or `HexDecimal`.** Traced and Reasoned stay on
our fork, where being wrong costs nothing.

The tier is about evidence, not about how sure you feel. `Magic::Augury` holds
three defects that are certainly real, and it is still Traced, because that code
has never been seen to run.

## A gameplay fix needs a before/after observation

A fix that changes what the player sees or does in play MUST ship a gameplay
oracle before it is done: the game run on the code WITHOUT the fix and again
WITH it, the changed value read from a player-facing screen on each side, and
both values recorded (a committed `tools/keys/<name>.keys` is the usual form).
This earns the Observed tier.

A structural check -- a grep or an assert on source or the compiled module,
red-green on a rebuild -- proves the source changed, not that the game behaves
differently. It earns Traced, never Observed, and it does NOT satisfy this rule.

The only exception is a specific written exception from Brian, naming the fix
and why the observation is impractical (for example, a value that no
player-facing screen ever prints). Record the exception at the fix site and in
the tracking bead. The Sunblade's light radius was cited here as the standing
case; it no longer is, because the activated field renders as a countable disc
of light on the map (bd inc-cyma, `tools/keys/sunblade-light.keys`).

Absent the observation or the exception, a gameplay fix is not finished, and a
claim about it MUST NOT be graded Observed.

## Titles

The title MUST NOT assert more than the body proves. This is the easiest rule
to break, because a title is written first and never revisited. A body that
carefully says "consistent with a present-timing artefact, though a single run
is weak evidence" under a title that says "(present-timing, not drawing)" is a
false claim, whatever the body says. A maintainer reads the title first.

## Separate the three things

Keep them apart, in the report and in your head:

- **An observation** -- something happened, here is the log. Always fileable.
- **A diagnosis** -- this is why. Needs the chain.
- **A patch** -- this fixes it. Needs the blast radius check.

Filing an observation as an observation is honest and useful. Filing an
observation dressed as a diagnosis is how you lose a maintainer's attention.

## Reconstructing a removed probe

Several defects in this port were found with a **probe**: a small block of
diagnostic code, switched on by an environment variable, that records what the
engine actually did. Some of them exist because the state cannot be reached any
other way -- no scripted session can drive the wizard menu's glyph pickers, for
instance -- so for those bugs the probe is not a convenience. It **is** the
reproduction, and the evidence in the table below rests on it.

That creates an obligation. A probe deleted from the source takes the
reproduction with it, and a reader who asks "how do you know?" a year from now
has no way to run what we ran.

**So a probe is never simply deleted.** When one has served its purpose:

1. Save it as a patch under `docs/evidence/<bead-id>/`, against the commit it
   last applied to. `docs/evidence/inc-ijs/destroy-probe.patch` is the worked
   example -- apply it with `git apply`, rebuild, and the probe is back.
2. Name that patch in the bead, and in this document's row for the defect, so
   the path is reachable from the public claim rather than only from our
   tracker.
3. Only then remove it from `src/`.

`tools/check_probe_hooks.sh` reports the state of every one of them. A hook
whose bead is finished but whose patch does not yet exist is reported as
**HELD**, which means exactly this: it may not be removed yet. One whose patch
exists is reported as **RETIRED**, and the source copy can go.

The same script requires that every hook name the bead it serves, in a comment
above its `getenv`. Hooks that predate the rule are listed in
`tools/probe_hooks.baseline`; new ones must declare themselves.

## Marking a base-code bug that we have fixed

The rules above govern what we SEND. This section governs what we KEEP. They are
different problems: a fix can sit in our tree for years without ever being
reported, and if nothing marks it, the knowledge that it was upstream's bug and
not our port's dies with the session that found it.

**Every fix to a base-code defect MUST carry an `upstream:` comment at the fix
site.** Lowercase tag, same shape as `ponytail:`. Greppable:

```
grep -rn "upstream:" src/ inc/
```

The comment MUST state four things, because a maintainer who wakes up years from
now has none of our context:

1. **That the defect is upstream's, and why.** Answer question 2 of the gate:
   would this misbehave on Win32, with the original typedefs, on the upstream
   compiler? If the answer is no, it is a port artefact and MUST NOT carry this
   marker -- mislabelling ours as theirs is exactly the credibility cost the top
   of this document is about.
2. **The evidence tier**, in the words of the table above: Observed, Traced or
   Reasoned.
3. **The tracking id**, so the full reasoning is recoverable.
4. **Whether it has been sent**, so nobody re-sends it and nobody assumes it
   went out when it did not.

Marking is NOT reporting, and marking creates no obligation to report. A marked
fix goes out only through the gate above, and only when Brian has read the
literal text that will be published.

### Base-code bugs fixed locally

| Fix | Tier | Sent? | Tracked |
|---|---|---|---|
| The `?` key-bindings screen sorted its entries with `SortKeys`, a comparator that returned `a->Val > b->Val`, a 0/1 result where `qsort` needs negative, zero or positive; an invalid comparator gives an arbitrary order, and the screen showed `Rest and Recover` first and the `Direction Keys` header at the bottom of the right column. The loop builds the entries in enum order, which is the order the headers assume, so the sort is removed (`src/Help.cpp`, `DescribeKeys`) | **Observed** -- the scrambled order on the headless dump and on the Ally, in enum order with the sort gone; `tools/check_pad_help.sh` asserts the header then North | no | inc-pk2p |
| The Sword of Warning's description reads `50% chance`, but on one of its two render paths (`src/Help.cpp:3522`) the text is a `Format()` format string, so vsprintf reads `% c` as a conversion, consumes `ItemLevel()` and prints its low byte as a garbage character (a smiley). vsprintf on Win32 and the original compiler reads the literal `%` the same way. Rewrote the prose to `50 percent`, which needs no escaping and is correct on both the `Format` path and the plain-`Lookup` path (`src/Help.cpp:3537`) (`src/Tables.cpp`) | **Reasoned** -- the clean source string, the `Format`/vsprintf render path and the space-flag `% c` conversion together explain the observed smiley; the rewrite removes the `%` that triggers it | no | inc-1y0v |
| The Sunblade page promised double damage against creatures tied to the Negative Material Plane, but its shared handler instead added `5d6` and gated that rider on wraiths alone; Win32 reads the same effect data and therefore has both disagreements. Restored the multiplier and targeted every `M_UNDEAD` creature plus spectral mold and mote of negative energy (`lib/m_items.irh`) | **Reasoned** -- the complete handler shape is guarded structurally by `tools/check_sunblade_negative_plane.sh`: undead plus both named non-undead denizens trigger the live multiplier, while the wraith-only gate and `5d6` rider are absent; a headless behavioral check remains the goal | no | inc-tek.8.8 |
| The Sunblade's page always promised mild cold resistance, but its shared effect omitted the cold-resistance grant entirely; Win32 reads the same effect data and therefore has the same omission. Added a passive `RESIST / AD_COLD / PLUS_1PER1` grant before the existing light field and moved the activation flag from slot 2 to slot 3 so only that field remains the three-per-day activation (`lib/m_items.irh`) | **Reasoned** -- seed 1, a known +2 Sunblade raised Cold resistance 0 -> 2 and Life Drain resistance 0 -> 4 when wielded, while activation still produced the light field; guarded by `tools/check_sunblade_cold.sh`, confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Sunblade page promises a 60-foot light burst, but its shared light-field data used radius 5, only 50 feet under the file's own 10-feet-per-square convention; Win32 reads the same radius. Changed only the light field's radius to 6 (`lib/m_items.irh`) | **Observed** -- on seed 1 the activated field paints a light disc on the map, six squares (60 ft) to each side of the wielder after the fix and five before it, both counted off the `@` row of the after-activation dump; `tools/keys/sunblade-light.keys` drives it, `tools/check_sunblade_light_range.sh` reads the current build, and `tools/oracle_ab.sh` reproduces the 5-vs-6 across commit e47f209 (before build `e47f209^`) | no | inc-tek.8.8 |
| The unidentified-magic autopickup sweep (`Player::AutoPickupFloor`) hoards any item carrying an effect id, so a dead priest's grantless holy symbol -- and the shield the priest hook stamps with the same god emblem -- fill the pack one at a time and never converge, because a symbol has no flavour to learn as a kind. This defeats the 0.6.4 changelog's own claim that holy symbols are exempt from autopickup, whose `!isType(T_SYMBOL)` guard covers only the stacking branch above. Added `Item::isFlavorGodMark()` and excluded a god's holy-symbol emblem from the sweep -- any holy-symbol item whatever its god grants, so Hesani's `EA_GRANT` symbol stays exempt like the other 16, plus a grantless god-marked shield; only a real shield enchantment, such as Hesani's buckler, still hoards (`inc/Item.h`, `src/Item.cpp`, `src/Player.cpp`) | **Observed** -- seed 1, an orc barbarian auto-stows a `? holy symbol` and its `EA_GENERIC` god-marked shield; seed 38 drops Hesani's `EA_GRANT` symbol, which the pre-fix grantless-only rule also stowed; guarded across both seeds by `tools/check_symbol_autopickup.sh`, confirmed red on the pre-fix binary and green on the rebuilt one | no | inc-upw.52 |
| The same unidentified-magic autopickup sweep (`Player::AutoPickupFloor`, `src/Player.cpp`) that inc-upw.52 patched for holy symbols also hoards every `EF_MUNDANE` item -- holy water, tanglefoot bags and the rest of the alchemy line (`lib/alchemy.irh`) -- because a mundane effect carries an effect id but has no flavour to learn, so `isKnown()` never converges and the sweep stows one at a time forever. inc-upw.52's `isFlavorGodMark()` whitelist was too narrow: the codebase itself calls these items "not magic" (`lib/alchemy.irh:1363`), and an unidentified-magic sweep must not hoard a non-magic item. Excluded any `EF_MUNDANE` effect from the sweep, the same shape as the god-mark exemption, which clears the whole class at once (`src/Player.cpp`) | **Observed** -- a `curate` template drops 3d4 holy water and a `rogue-archer` template 1d2 tanglefoot bags; before the fix each pile auto-stowed one vial/bag at a time and left the view, after it both remain in view while the same drop's unidentified potion still stows (proving the branch still runs and still hoards real magic); guarded by `tools/check_mundane_autopickup.sh`, confirmed red on the pre-fix binary and green on the rebuilt one | no | inc-mc37 |
| The Bracers of Defense page stated one equal-to-plus rate for both Defense Class and Coverage, although the shared effect grants Defense Class at `PLUS_1PER1` and Coverage at `PLUS_2PER1`; the unequal rates apply identically on Win32 with the original typedefs and compiler. Corrected only the page to state both rates (`lib/m_items.irh`) | **Observed** -- the disagreement is directly present in the original effect definition, and the corrected wording is guarded structurally by `tools/check_bracers_defense_page.sh` | no | inc-tek.8.8 |
| The Shadowstone page called the item a cloak and promised a Hide bonus equal to its magical plus, although the shared data defines a stone and grants `PLUS_2PER1`; the copied prose is wrong identically on Win32. Corrected only the page to name the stone and its twice-per-plus bonus (`lib/m_items.irh`) | **Observed** -- the disagreement is present directly in the original effect definition and the corrected wording is guarded structurally by `tools/check_shadowstone_page.sh` | no | inc-tek.8.8 |
| The Cloak of Shadow Shifting promised travel in darkened areas but its shared effect allowed travel from and to lit squares, and its page called a seven-use daily activation "at will"; the TODO and unchanged travel implementation misbehave identically on Win32. Added a shared effect flag, refused activation on a lit source before charging the item, rejected lit destinations in random and controlled travel, and corrected the page (`inc/Defines.h`, `lib/m_items.irh`, `src/Magic.cpp`, `src/Effects.cpp`) | **Reasoned** -- the source gate, both destination gates, flag assignment and corrected frequency are guarded structurally by `tools/check_shadow_shifting.sh`; a headless behavioral check remains the goal | no | inc-tek.8.8 |
| The Cloak of Resistance promised a resistance bonus but granted `ADJUST`, the plain magic-bonus stati, so it stacked with actual resistance bonuses; the same resource grant and bonus routing run unchanged on Win32. Changed only the grant to `ADJUST_RES` (`lib/m_items.irh`) | **Observed** -- seed 1, +3 cloak and auspicious +2 armour, all three saving throws read from the character sheet in one session. Cloak alone remained Fortitude/Reflex/Will `6/4/6` before and after, while cloak plus armour fell from `8/6/8` to `6/4/6`; the readout changed from `+3 magic, +2 resistance` to one `+3 resistance`. Guarded by `tools/check_cloak_resistance.sh`, confirmed red on the pre-fix module and green after recompiling | no | inc-tek.8.8 |
| The Wand of Acid's initial blast was acid, but both branches of its residual-burn handler passed `AD_FIRE` to `ThrowDmg`; the same data and constants run unchanged on Win32 (`lib/m_items.irh`) | **Observed** -- same seed and +4 wand against a fire-immune hell hound: residual damage `0, 0` before and `6, 2` after. Guarded by `tools/check_wand_acid_type.sh`; an acid-immune second side is unreachable because immunity to the initial acid blast prevents `POST(EV_MAGIC_HIT)` attaching the burn | no | inc-tek.8.8 |
| `Character::HasFeat` never read a race's Monster template, so players silently lost every racial feat that was not also an explicit `Grants:` entry -- 8 feats across 6 races (`src/Create.cpp`) | **Observed** -- same seed and key script; Dragonkin sheet had no Mantis Leap before, has it after | no | inc-2a0 |
| Two empty hands produced one strike per swing forever, while two weapons produced two. `AttackMode()` returns `S_BRAWL` before the `S_DUAL` test is reached, so no feat could ever apply to fists (`src/Creature.cpp`, `src/Fight.cpp`) | **Observed** -- same seed; `Hit:2 / -3` and one Punch before, `Hit:2 / 2` and two Punches after | no | inc-dzz |
| `Map::At()` answers an out-of-bounds query with the (0,0) square instead of failing. Guarded at `Map::GetAt` (`src/Display.cpp`). **The original claim -- that monster AI read off the map 343 times in 13 seconds -- is withdrawn**: measured 2026-08-18, the reads come from level generation and the guard covers 8 of 47,962 | **Observed**, but far smaller than first reported: 8 reads stopped, no player-visible change, byte-identical screen dumps | **yes**, rmtew#40 + #41, open; correction posted 2026-08-19 | inc-f13, inc-5xn |
| `TargetSystem::giveOrder` built its `Target` uninitialised, so an escort read stack garbage as the handle of the creature to follow. Two of the eight sites also left `damageDoneToMe` uninitialised, turning escorts hostile to their own leaders (`src/Target.cpp`) | **Observed** -- 250 seeds against a build differing in nothing else; asserts 89,545 -> 0, error lines 139,948 -> 48,245 | **yes**, rmtew#42, **MERGED** | inc-zmk |
| `TargetSort` ordered targets tied on priority and type by subtracting the two objects' addresses, so the same seed played a different game on every launch. The only comparator in `src/` doing pointer arithmetic (`src/Target.cpp`) | **Observed** -- randomisation off: 30 of 30 runs identical; on: 4 of 15 diverged; p = 0.9% | **yes**, rmtew#44, open | inc-qik |
| The `Falling` global was a raw `Creature*` carried across a level change. A faller that died on the way left it dangling, and the next creature the allocator placed on that address paid 3d6 per level it never fell (`src/Move.cpp`) | **Observed** -- seed 3, two layouts, same 204,218 draws: one rolled 6d6, the other did not. 40 of 40 seeds pass after; seed 3 failed a dozen attempts before | no | inc-yml |
| `tmpstr` wrote one pointer past the end of the 64000-entry `StrBufDelQueue` before testing the bound, and the `ASSERT` meant to stop it only logs and returns (`src/Base.cpp`) | **Observed** -- A/B on a probe build with `STRING_QUEUE_SIZE=400`, same seed, reproducing then not reproducing the live crash's twelve assert lines | no | inc-upw.18 |
| `CurrentRoutine` was a 64-byte global filled by `strcpy` and two `strcat`s with no length check, on the hottest path in the engine (`src/VMachine.cpp`) | **Traced** for the overrun -- read out of the code, never seen to fire. The *speed* half of the same commit is NOT listed: that one is ours, because the breadcrumb is inside `#ifdef DEBUG` and reached players only because our build must pass `-DDEBUG` (inc-9df.3) | no | inc-upw.20 |
| `Map::RunOver` asked only for `DF_OPEN`, so a door carrying `DF_BROKEN` without it was a wall to every route search, while `Door::SetImage` marks the same square `Solid=false` and the player walks through it. Fixed by giving the engine one answer, `Door::isPassable`, and asking it here (`inc/Feature.h`, `src/Djikstra.cpp`) | **Observed** -- a real save; three doors of flags `0x40` sealed a temple, the search reached 142 of 16384 squares and both staircases ranked unreachable; after, the nearer ranks 210 on the safe tier. Guarded by (`tools/check_broken_door.sh`) | no | inc-8zu |
| The same reader defect at the monster movement site: `Monster::MoveTowards` asked for `DF_OPEN`, so a monster meeting a broken doorway called `OpenDoor` on a hole. `Door::Event`'s `DF_BROKEN` arm answers and returns `ABORT` without spending time, `Monster::OpenDoor` reads that zero `Timeout` as "the attempt did not happen", and falls through to `TryToDestroyThing` -- the monster attacks an empty doorway, up to five times, one turn each (`inc/Feature.h`, `src/Monster.cpp`) | **Traced** -- read the three call sites together; the doors that reach it are the ones measured for inc-8zu. Guarded by (`tools/check_broken_door.sh`) | no | inc-wj8 |
| `Door::SetImage` brands a door `DF_BROKEN` when it can read no doorframe beside it, and the brand was permanent. `SetImage` runs during level generation, before the walls next to a door exist, so ordinary doors were branded and kept it for the rest of the game -- the square went `Solid=false`, `EV_CLOSE` refused the door forever, `isDead()` called it destroyed, and the route search would not path through it. Cleared on any redraw that can read the frame; safe because only the damage path also sets `DF_OPEN`, so `DF_BROKEN` without `DF_OPEN` can only be this branch's own mark (`src/Feature.cpp`) | **Observed** -- seed 7, `tools/keys/dive12.keys`, 3029 redraws over 446 doors on a build differing in nothing else: stale brands surviving a readable-frame redraw 42 -> 0, doors ending closed and branded broken 6 -> 0; 14 of the 63 brand events hit a LOCKED door and so made a locked door walkable. Guarded by (`tools/check_broken_door.sh`) | no | inc-95d |
| `Magic::Augury` held three defects in six lines: a handle cast to a pointer, a missing `isItem()` guard, and a `goto` to a label above the scan that made an item-free map loop forever (`src/Effects.cpp`) | **Traced** -- never executed. Augury has therefore never worked, which is also why there is no regression risk | no | inc-upw.1 |
| `Player::MoveDepth` declared its follower array `static` in a function that re-enters itself (`src/Feature.cpp`) | **Reasoned** -- kept as hardening only. The crash claim made for it in b3b5351 was retracted in 668043c after a controlled A/B showed the same seed segfaulting identically with and without it | **yes**, rmtew#43, sent as hardening, open | inc-upw.15 |
| Loading a save/module group trusted `compSize`/`groupSize` out of the file with no sanity check, and handed the decompressor (`LZ_Uncompress`/`RLE_Uncompress`) only the compressed input's length -- never the output buffer's real capacity -- with nothing afterwards comparing what came out against the size the file claimed. A corrupt or hostile stream could write past the heap block sized for that claim (`src/lz.c`, `src/rle.c`, `src/Term.cpp`, `src/Registry.cpp`) | **Traced** -- read `CFile::LoadCompressed`, `LZ_Uncompress` and `RLE_Uncompress` together; confirmed with a canary-guarded-buffer harness (`tools/check_lz_uncompress.sh`) and 10 hand-corrupted save files exercising bad/negative/huge `compSize`/`groupSize`, truncation, an actual overflow-attempt stream, and a stream that decodes shorter than it claims (`tools/check_load_corrupt.sh`) -- all refused cleanly under UBSan, none touching memory past its declared capacity | no | inc-l0t |
| `CFile::Seek` discarded `realloc()`'s return value when growing its buffer, so on a move `data` kept pointing at memory `realloc` may have already freed -- every access after a growing `Seek()` was a use-after-free (`src/Term.cpp`) | **Traced** -- read the code; the return value was never assigned or checked | no | inc-l0t |
| `CFile::FRead` zero-filled and reported nothing when a read ran past the end of the decompressed in-memory buffer, so a short or corrupt group read back as fabricated zero fields instead of failing (`src/Term.cpp`) | **Traced** -- read every call site (7, all in `Registry::LoadGroup`) to confirm none relies on a legitimate short read before changing the behaviour; confirmed via `tools/check_load_corrupt.sh`'s truncated-file cases | no | inc-l0t |
| `Item::Item(rID,int16)` read `eID` and `Plus` inside `MaxHP()` before it assigned them, and never assigned `Parent`, `homeID`, `Flavor`, `DmgType` or `swingCount` at all -- it relied on the zero-fill in `Object::operator new`. Reading a member the constructor leaves indeterminate is undefined, so an optimising compiler that does not preserve that fill reads stale heap; Win32 with the original compiler misbehaves the same way. Assigned every such member before use, which equals the zero-fill clang already produced, so clang runs are byte-unchanged (`src/Item.cpp`) | **Observed** -- Debian 11 GCC `-O2`, seed 7: the unfixed constructor fires the `src/Item.cpp` HP `ASSERT` and dereferences a wild `Parent` handle (`invalid object handle (1344285811)`), segfaulting character creation before any map, while clang and GCC `-O0` are clean. Per-TU then per-function bisection isolated it to this constructor; a red/green A/B on the fix (reverting the `Parent`-group line alone reproduces the exact handle) confirmed it. Guarded by `tools/check_gcc_o2_char_create.sh`, which builds GCC `-O2` and fails on the handle signature or a mapless run | no | inc-nw0v |
| `Creature::Multiply` refused to breed a child above `GENERATION` 1, but only stamped that stati on the child AFTER placing it, so anything the placement threw (a field re-triggering the blast that started it) saw an unstamped, generation-0 child and passed the cap. An instrumented probe build showed the recursion never actually reaches a child at all: `Creature::FieldOn`'s `FI_MODIFIER` case sets the re-thrown event's `EActor` to the field's *creator* (the original parent, forever generation 0) rather than the creature standing in the field, and a monster script (e.g. brown mold) calls `EActor->Multiply(...)`, so the same parent recurses on its own field, unbounded, no matter how any child is stamped. Fixed both: `GENERATION` now stamps before placement (real but insufficient on its own), and `Multiply` now refuses to nest past a small depth -- the bead's own second proposed fix, "Multiply must not recurse within one event chain" (`src/Creature.cpp`) | **Traced** -- read `Creature::Multiply`, `Creature::FieldOn`, `Magic::Blast`, `Magic::MagicHit` together; the generation-order hypothesis was disproven and the actual mechanism found with an instrumented probe build logging every `Multiply` entry, child creation, and `Blast` `EActor`/`EVictim` pair across the reproduction seed; reproduced clean before and after (`tools/headless.sh tools/keys/dive.keys 3104`: `Fatal("Event Stack Overflow!")` -> completes with `errors: none`) | no | inc-upw.5 |
| `Game::Get` computed a module slot as the resource id's top byte minus one with no range check, so an id with a zero top byte wrapped (unsigned arithmetic) into a huge slot and an id above `MAX_MODULES` ran off the array; the `ASSERT` guarding the dereference (`inc/Defines.h:93`) only logs and falls through, so an out-of-range slot holding non-zero garbage crashed with nothing in `errors.log`. Range-checked the slot and return `NULL` -- a logged miss -- instead (`src/Res.cpp`) | **Traced** -- read `Game::Get` and `ASSERT`'s fall-through definition; reproduced clean before and after (`tools/headless.sh tools/keys/dive.keys 3387`: SIGBUS with no log -> `Game::Get: resource id 1 names module slot -1, out of range` logged). Fixing this exposed a second, distinct, PRE-EXISTING defect one call deeper -- `Creature::isMType` dereferences `TMON(tmID)` with no NULL check, so the same seed now SIGSEGVs instead of SIGBUS-ing, at `src/Values.cpp:2163`. That defect is real, was masked by `Game::Get`'s crash-or-garbage behaviour before now, and is flagged but deliberately NOT fixed here -- tracked separately as inc-upw.24 | no | inc-upw.16 |
| `Creature::isMType` dereferenced `TMON(tmID)` (`#define TMON(l) ((TMonster*) theGame->Get(l))`, `inc/Res.h:11`) with no NULL check, at `src/Values.cpp:2164` and again at `:2248` (`MA_PERSON` case) further down the same function. Once inc-upw.16 made `Game::Get` return `NULL` for a wild resource id instead of a garbage-but-nonNULL pointer, this became a live SIGSEGV instead of a silent garbage-field read. Fetched `TMON(tmID)` once into a local `TMonster*`, return `false` (an unresolvable template matches no type) if it is `NULL`, and reused the same pointer at both dereference sites (`src/Values.cpp`) | **Traced** -- read the whole function body for every use of `TMON(tmID)`; reproduced clean before and after (`tools/headless.sh tools/keys/dive.keys 3387`: SIGSEGV twice in a row on the pre-fix `incursion-headless` binary, both times logging `Game::Get: resource id 262145 names module slot -1, out of range` immediately before the crash -> clean exit after, across 6 seeds and `tools/check_headless.sh`'s full regression suite) | no | inc-upw.24 |
| `TextTerm::ShowMapOverview` starts its staircase search from `case KY_CMD_UP:` and `case KY_CMD_DOWN:`, and neither label can ever match. The overview loop reads keys with `GetCharCmd(KY_CMD_ARROW_MODE)`, which returns no command outside the eight compass arrows (`src/Wposix.cpp:1738-1739`, and the identical lines at `src/Wcurses.cpp:1382` and `src/Wlibtcod.cpp:1805`), so the keyset hit is dropped and the raw character comes back instead. With no label for the raw character, `'<'` and `'>'` fell to `default:`, which closes the map -- the two keys a player reaches for did nothing but leave. `','` and `'.'` worked all along, because `KY_COMMA` and `KY_PERIOD` ARE the raw characters. Added `case '<':` and `case '>':` beside them (`src/Term.cpp`) | **Observed** -- headless session on seed 1: the map opens, `'.'` runs the search and `'>'` closes the map (screen mode 1 -> 2). `tools/check_stair_cycle.sh` is red on a build differing only in those two case labels and green with them | no | inc-pw1.5 |
| `Player::Create` zeroed `SkillRanks[0..SK_LAST]` inclusive (`i != SK_LAST+1`), one write past the 49-element `SkillRanks[SK_LASTSKILL]`. Every other loop over `SkillRanks` in the codebase stops at `i != SK_LAST` with no `+1` (`src/Debug.cpp:1679`, `src/Create.cpp:2909`/`2941`, `src/Managers.cpp`, `src/Help.cpp:82`); this was the only outlier. Dropped the `+1` (`src/Create.cpp`) | **Traced** -- UBSan chargen (seed 8, `tools/keys/dive.keys`): `index 49 out of bounds for int8[49]` at `Create.cpp:114` before, gone after; compared against all 8 sibling loops over `SkillRanks` for consistency | no | inc-bd2 |
| `Creature::CalcValues` looped `i=0..4` over the five `A_HIT_*`/`A_SPD_*` slots (ARCHERY, BRAWL, MELEE, THROWN, OFFHAND) and fed `i` straight into `Character::GetBAB(mode)`, but `TClass::AttkVal` holds only the 4 real combat modes -- there is no OFFHAND entry, and OFFHAND's hit bonus already borrows MELEE's value everywhere else in the same function (`A_HIT_OFFHAND`/`A_SPD_OFFHAND` above it). Made the `GetBAB` call do the same for `i==4` instead of reading `AttkVal[4]` (`src/Values.cpp`) | **Traced** -- UBSan chargen: `index 4 out of bounds for uint8[4]` at `Values.cpp:2589` and `:2590` (two reads of the same bad index, one function) before, gone after | no | inc-bd2 |
| `Character::FeatPrereq`'s `FP_ATTR` case forwards `fc->arg` straight into `Character::IAttr(int8 a)`, which is sized only for the 7-entry base-ability-score domain (`A_STR..A_LUC`, see `BAttr[7]`, `TRace::AttrAdj[8]`, `FT_IMPROVED_STRENGTH+a`). Two Iron Skin prerequisites in `FeatTab.cpp` pass `A_SAV_FORT` (28) -- an index from the unrelated 41-entry `A_HIT_*`/`A_SAV_*`/... scheme `Creature::Attr`/`GetAttr` is sized for -- 42 bytes past `BAttr`'s end, with whatever those bytes held silently deciding whether the feat's Fortitude-save prerequisite passed. `AdvanceLevel` already calls `CalcValues()` immediately before feat prereqs are checked, commented `// For Attribute-based feat prereqs`, so the fully-computed `Attr[]` table is available; made `IAttr` defer to `GetAttr` for any index outside the ability-score range instead of reading past `BAttr` (`src/Create.cpp`) | **Traced** -- UBSan chargen: `index 28 out of bounds for int16[7]` and `int8[8]` at `Create.cpp:3691` (two reads at one call site) before, gone after; checked all 8 `IAttr` callers and the ~60 other `FP_ATTR` entries in `FeatTab.cpp` -- all pass 0-6, so behaviour for every existing caller is unchanged. **May change observable feat-grant behaviour**: Iron Skin's two `A_SAV_FORT` conjuncts now check the real, computed Fortitude-save bonus instead of reading whatever garbage followed `BAttr` in memory | no | inc-bd2 |
| inc-zmk (`inc-upw.4`'s own fix) only stops a NEW `Target` from carrying garbage; `Registry::SaveGroup`/`LoadGroup` copy whole objects as raw bytes, so a `Target` already written to a save or module before c9201dd came back with its stale `data` union untouched, and `Target::GetThingOrNULL`'s `default:` branch treated ANY `TargetType` with no case of its own -- including `OrderWalkToPoint`, whose `data.Area.x/y` sit in the same union bytes -- as a creature handle, then cast whatever `Exists()` found without checking it was actually a Creature (`GetCreature`'s own `ASSERT` for that only logs and keeps going, per `Defines.h`). Added `TargetSystem::SanitizeLoadedTargets()`, run once for every `Creature` object `Registry::LoadGroup` loads (covers save files and modules alike, since both use this same loop) to zero `data` for every `Target` type that does not legitimately carry a creature handle, and separately tightened `GetThingOrNULL` to enumerate the legitimate types explicitly, verify the resolved object's actual type before returning it, and give `OrderWalkToPoint` its own no-handle case instead of falling into the old catch-all (`src/Registry.cpp`, `src/Target.cpp`, `inc/Target.h`) | **Observed** -- a 20-turn sandboxed walk on a read-only copy of Brian's own `save/Jaoin.sav`, loaded under a binary carrying c9201dd but not this fix, logged 74 `ASSERT failed: '...Get(h)->isCreature()'` lines (`inc/Base.h:607`, from `Target::GetThingOrNULL` via `Monster::Movement`); 0 with this fix in place, same save, same walk, same seed. A save written entirely by the fixed binary (fresh character, save, reload) produced byte-identical `-dump` output whether loaded by a binary with or without this fix, confirming the sanitiser is a no-op on already-clean data | no | inc-upw.13 |
| `Food::Eat` (`CA_BURNING_HUNGER` branch) ran `Eaten += 5` before checking eligibility, and its refusal branch did `return ABORT` instead of `goto StopEating`, so it skipped the give-back that the sibling vomit/full branches in the same function already use. The eat menu (`Player.cpp`) splits one unit off the stack with `TakeOne()` before `EV_EAT` fires; a refused Dragonkin meal (blood-only diet) degraded and destroyed that split-off unit anyway, one ration per refused attempt, with the player told no and charged for it every time. Moved `Eaten += 5` to after the eligibility check and changed the refusal to `goto StopEating`, the same label the vomit/full branches already jump to (`src/Item.cpp`) | **Observed** -- `tools/keys/dragonkin-ration.keys`, seed 1: pre-fix, "9 food rations" decremented by one on every refused eat, down to quantity 1 ("food ration") after 8 refusals; post-fix, the same 8 refusals leave "9 food rations" unchanged throughout. Regression-checked with a non-`CA_BURNING_HUNGER` character (plain Lizardfolk, same seed): eating a food ration still prints "You finish eating the food ration.", raises Hunger State, and decrements the stack (8 -> 7) exactly as before | no | inc-i9q.1 |
| `Character::Sacrifice` walked a god's `SACRIFICE_LIST` two slots at a time and stopped at the first zero in the LEFT slot of a (category, value) pair. `MA_ALL` is `0` (`inc/Defines.h:1513`), so a `MA_ALL` row terminated the list instead of matching every creature, and every row after it was unreachable. Khasrach, the orc god, is seven rows of which the last five are `MA_ALL` (`lib/religion.irh:2828`), so his altar refused every corpse that was not an orc or a goblinoid and his only two reachable rows were both punishments. Terminate on a whole zero ROW instead, which is the contract `Resource::GetList` documents when it pads every list with three zeros *"because certain loops use values three at a time"* (`src/Annot.cpp:351`); `Creature::isMType` likewise opens with `if (!mt) return true;` (`src/Values.cpp:2166`), a line that exists only to make `MA_ALL` a wildcard and was unreachable from here (`src/Prayer.cpp`) | **Observed** -- seed 1, same key script, one line of source between the two builds. Dwarf corpse at a Khasrach altar: *"Khasrach seems uninterested in your offering"* before, *"Khasrach is impressed"* with `sacVal 88` and favour 0 -> 88 after. Control on the same seed, kobold corpse (matches row 1, `MA_GOBLINOID SAC_ANGRY`, which sits before the first `MA_ALL` and was always read): *"Anger: Khasrach +1 (offensive sacrifice)"* on **both** builds, byte-identical. Both runs are `tools/check_sacrifice.sh`, which was confirmed red on the unfixed build and green on the fixed one. Sibling sweep: `SACRIFICE_LIST` is the only list in the game whose left column has a legal zero value -- `FU_*`, `AID_*` and `MSG_*` all start at 1 and `REL_DEFAULT` is -20, so the other four pair/triple-stepping loops over `GetList` results (`MakeLev.cpp:3243`, `Prayer.cpp:235`/`556`/`1824`) cannot hit this. Newly reachable: Khasrach's five `MA_ALL 10` rows and Aiswin's two `MA_ALL 2` rows, all taking the plain weight branch; Aiswin's trailing `MA_CHOICE1`/`MA_CHOICE2` rows stay unmatched because `isMType` returns false for 125/126 | no | inc-upw.27 |
| `TextTerm::BarterManager`'s shop list never scrolled in either direction. Every arrow key jumps to its `PartialRedraw` label, which opens with `ClearScroll(true)`; `TextTerm::ClearScroll(bool full)` zeroes `TextTerm::offset` when `full` is set (`src/TextTerm.cpp:720-728`), and the single `UpdateScrollArea(offset, WIN_CUSTOM)` at the end of that redraw is the only draw -- so whatever offset the `NORTH`/`SOUTH` cases had just computed was discarded and row 0 was redrawn at the top of the page every time. The selection walked off the page and stayed off it. The same two cases also compared against a hardcoded `32` visible rows while the window is `WinSizeY()` (35 at the minimum 48-row screen), and the `NORTHEAST`/`SOUTHEAST` manual-scroll keys were guarded by `currCon` -- copied from `InventoryManager`, where it means 'a container pane is open'; `BarterManager` never assigns it, so those two keys worked or not according to whether the inventory screen had been opened earlier in the session. Kept the scroll position in a local the redraw cannot reach, derived the page height from the window the way `TextTerm::LMenu` already does (`src/TextTerm.cpp:1052-1055`), and dropped the `currCon` guard (`src/Managers.cpp`) | **Observed** -- seed 1, same key script, `src/Managers.cpp` the only file between the two builds. Roark Ironbeard's store, 40 `DOWN` presses: the page stayed byte-identical to the opening screen before, starts at row 06 with the up-arrow glyph after; 40 `UP` presses returns to row 00. The run proves the selection actually moved by pressing `[x]` at both ends -- two different item descriptions -- so a session whose keys never reached the menu cannot pass. `tools/check_store_scroll.sh`, confirmed red on the unfixed build and green on the fixed one. Sibling sweep: `ClearScroll(true)` appears at 13 other sites (`src/Help.cpp`, `src/Debug.cpp`, `src/Term.cpp`, `src/TextTerm.cpp`), all of them once before a loop rather than inside its redraw path; `InventoryManager`, the only other hand-rolled scrolling list, calls the position-preserving `ClearScroll()` and is unaffected | no | inc-upw.23 |
| `TextTerm::EffectPrompt`'s target cursor scored every candidate on ONE axis. For a sideways arrow it summed only the column gap and never looked at the row; for an up/down arrow only the row gap and never the column (`src/Term.cpp:2312-2337` pre-fix). So `RIGHT` meant "the nearest column to my right" and every row in that column tied at the same score, with the winner decided by the order things happen to sit in `Map::Things`. The cursor moved sideways while going up, skipped the nearest creature for a further one, and walked itself into positions from which most presses did nothing at all. **The replacement is this fork's own design, not a minimal repair of that sum, and must not be offered upstream as one**: the candidates now form a ring around the player ordered by bearing, an arrow steps one place round it, `UP`/`RIGHT` and the east diagonals go clockwise, `DOWN`/`LEFT` and the west diagonals go counter-clockwise, and the first press from the player enters the ring at the nearest candidate in the direction pressed. Which things are candidates is unchanged -- the filters are untouched and belong to the calling prompt -- and the location cursor (`Q_LOC`) is untouched, so it still steps one tile per press (`src/Term.cpp`) | **Observed** -- seed 1, same key script, `src/Term.cpp` the only file between the two builds, with the probe compiled into both so they can be read the same way. Player at (111,110), nine corpses in view. One `RIGHT` press: (112,107) before -- the furthest of three candidates the old sum scored identically -- and (112,108), the nearest, after. One `UP` press from (116,107): up one row and five columns sideways before; the next place round the ring after. Twelve `UP` presses: two moves and then ten presses that moved nothing before; nine distinct places and then the lap again after. Twelve `DOWN` presses walk the same nine backwards. `tools/check_target_order.sh`, confirmed red on the unfixed build and green on the fixed one. Not a port artefact: the arithmetic is byte-identical in `215a7b54^`, before rmtew reformatted the file, and behaves the same on Win32 with the original typedefs | no | inc-upw.26 |
| `Player::MoveDepth` read `RES(BELOW_DUNGEON)->Type` with no zero check on the DOWN path, while the UP path thirteen lines above already guards the identical case with `if (!mID) return;` on `ABOVE_DUNGEON`. `Game::Get` returns `NULL` for a zero id (`src/Res.cpp:312`), and no dungeon in `lib/` defines `BELOW_DUNGEON` at all, so every player who leaves the bottom level of a dungeon downwards dereferences `RES(0)`. Four routes reach it and none needs a debugger: falling into generated chasm on the deepest level, the `>` climb-down, levitating down, and scripted `MoveDepth`. The climb-down route needs no wizard mode, no script and no recursion -- `Creature::Descend` calls `MoveDepth` straight (`src/Skills.cpp:4217`) with `safe=true`, and `safe=true` does not help -- this read happens before the function ever looks at `safe`, whose first test sits over a hundred lines below inside the `if (new_m)` block that a null `mID` never reaches. Gave the down path the up path's guard, unchanged in every other respect (`src/Feature.cpp`) | **Observed** -- `INCURSION_STACK_PROBE=1 tools/headless.sh tools/keys/dive.keys 3362`, same seed, same key script, `src/Feature.cpp` the only file between the two builds: exit 139 before, exit 0 after. Before, the macOS report gives `EXC_BAD_ACCESS` / `SIGSEGV` / `KERN_INVALID_ADDRESS at 0x0000000000000000` faulting in `Player::MoveDepth`, and the probe log stops dead at its tenth line, `MoveDepth nest=2 map_depth=10 per_level=3648`. After, that same nested bottom-level entry is still line 10 of the log with the same numbers -- the branch is entered, not avoided -- and the session runs on to 36 entries, 2 screens to 11, `errors: none` and a clean map audit on both sides. Three further routes were captured crashing real sessions on 2026-08-18, seeds 111 and 777, all exit 139 at `0x0`: walking into a bottom-level chasm, the `>` climb-down, and levitating down (`docs/evidence/inc-x9i/`, `docs/evidence/inc-x9i/observed-routes/`). Not a port artefact: the missing check is plain C++ and behaves the same on Win32 with the original typedefs | no | inc-x9i |
| `Registry::LoadGroup` sets `loadMode` on entry and clears it only where it succeeds, so every throw in between -- bad version, corrupt chunk, missing group, failed allocation -- leaves the Registry claiming it is still loading, and leaks the memory file with it. `Array<>::Array()` and `String::String()` (`src/Base.cpp:546`, `src/Base.cpp:136`) both skip their own initialisation while the Registry is loading, on purpose, so an object rebuilt from a file keeps what it was saved with. With the flag stuck, every `Array` and `String` built anywhere afterwards is uninitialised; the next `LoadGroup`'s own `LoadedObjects` is stack garbage from declaration, and unwinding the next throw hands that garbage to `free()`. `SaveGroup` has the same shape for `saveMode`, where a stuck flag would send `Registry::Block` down the wrong branch for every object serialised afterwards. Both functions now restore their flag and dispose of their `CFile` through one guard object, however they leave (`src/Registry.cpp`). **This fix is Eugene Archibald's**, from commit `7ab1ae8` of the downstream fork `earchibald/incursion-roguelike`; the root cause and the first evidence are his. This tree re-derived the change by hand against its own `inc-l0t` header size checks and re-measured it | **Observed** here -- sandboxed copy of `mod/Incursion.Mod` with its `fileHeader.Version` patched to a stamp the binary refuses, key script asking the start menu for a new character six times: exit 134 before (one refusal, then `malloc: pointer being freed was not allocated`, frame #6 `Registry::LoadGroup` under lldb), exit 0 and six clean refusals after. The `SaveGroup` half is **Traced** -- no write failure was staged | no | inc-w7q |
| `Registry::SaveGroup` parks each object's data-block HANDLE in the object's own POINTER field while it writes the object out -- that is what `Registry::Block` does with `saveMode` set -- and the `Serialize(*this,false)` loop that swaps the pointers back sat AFTER every write. Any throw from the write path skipped it (`posixTerm::FWrite` raises `EWRIERR` on a short write, and a full disk is the ordinary way in), so every object the writer had reached kept a small integer where a pointer belonged, and `Game::Cleanup` walked them and dereferenced it. Simply repeating the old loop on the throw path would have been a second defect rather than a fix: a part-way throw leaves the objects already visited holding handles and the objects not yet reached holding real pointers, so re-walking the whole group would hand a genuine pointer to `GetData()`. A guard object records each object as it is converted and unwinds exactly that record, with `saveMode` cleared first because the swap-back is the branch that flag selects; the success path and every throw path call the same `Restore()`. Sits on top of inc-w7q's `RegistryScope`, which restores the flag and the `CFile` but cannot restore objects (`src/Registry.cpp`) | **Observed** -- an 8 MB HFS+ image with 76 KB free, the run directory's `save/` symlinked onto it, `tools/headless.sh tools/keys/smoke.keys 1`: `Error writing save file (File Write Error).` and then `EXC_BAD_ACCESS (code=1, address=0x7f3)` at frame #0 `Game::Cleanup` under lldb, exit 139, before; the same error reported, the session playing on, exit 0, after. 0x7f3 is 2035, a Registry handle. A genuinely full disk can only ever fail with EVERY object converted -- `SaveGroup`'s two loops write into a memory `CFile` and the disk is not touched until `CFile::CommitCompressed`, after both -- so the part-way case the whole design turns on was staged instead with `INCURSION_SAVE_FAIL_AT` at the first object, object 5, object 40, data block 1 and data block 3: all five exit 139 on an otherwise identical unfixed build and 0 on the fixed one, and all five go on to save a character whose `-dump` report matches a control run of the same seed with nothing staged, field for field. `tools/check_save_fail.sh`, confirmed red on the unfixed build and green on the fixed one | no | inc-i0r |
| `Creature::MakeNoise` checks that the caster is still on the map, then calls `Reveal()`, which can delete it, then dereferences the map pointer that deleting just nulled. Reveal removes HIDING; removing it runs `StatiOff` -> `CalcValues`, which recomputes the creature's size; a size at `SZ_HUGE` or above has a face radius and needs a 3x3 footprint, so `CalcValues` calls `PlaceNear`; and `PlaceNear` deletes any non-player thing it cannot seat, setting `m = NULL; x = y = -1`. Three further `Reveal` callers had the same hole and are fixed alongside: the `A_ROAR` case, and `TerrainEffects`' fall path. The object is queued on `DestroyQueue` rather than freed, so these are null dereferences, not use-after-free (`src/Creature.cpp`, `src/Fight.cpp`, `src/Move.cpp`) | **Observed** for the `MakeNoise` site -- seed 3390 under `tools/keys/dive.keys` on a `-DINCURSION_OOB_PWFIX_WIDEN` build: exit 139 at `KERN_INVALID_ADDRESS 0x10` in `Map::At(this=0x0, x=-1, y=-1)` before, exit 0 after, one file between the builds. A hardware watchpoint on the creature's `m` caught the write of null and named every frame; size measured `SZ_LARGE` -> `SZ_HUGE`. **Traced** for the other three sites, which were found by surveying all 22 `Reveal` call sites and have not been seen to fire. Forensics in `docs/evidence/inc-upw.37/` | **described**, not filed: rmtew#40, [comment 5358799776](https://github.com/rmtew/incursion-roguelike/issues/40#issuecomment-5358799776), 2026-08-20, as a note under the inc-65j addendum and not as its own issue or patch. The comment offers to open one | inc-upw.37 |
| `MapIterate` guards its map pointer in its condition (`m &&`) and in its increment (`m ?`) but not in its initialiser, which runs first and dereferenced `m->Things[0]` unguarded (`inc/Base.h`) | **Traced** -- read out of the macro, never observed to fire. The two guards it already carries are the evidence that a null map was expected; reachable from the `A_ROAR` site fixed under inc-upw.37 | no | inc-upw.38 |
| `Rect::PlaceWithin`, when the wanted size does not fit, insets by one on each side instead. With a space two squares across or less that puts the far edge before the near one, and the function cannot report failure, so it returns an inside-out rectangle. Its single caller (`src/MakeLev.cpp:2903`) uses that rectangle as the WALL RING of a room's inner chamber -- walking its four edges writing wall, then punching a door into one -- so an inverted ring is walked backwards. Repaired by widening to the whole area rather than collapsing to a single row: collapsing leaves a one-square-thick ring, which is a line of wall with a door onto nothing, and slips past the caller's own too-thin corrector twelve lines below (`if (r.y1 + 1 == r.y2)`) because that tests for a ring exactly two across; widening feeds that corrector a two-row ring it recognises and expands. Collapsing is also wrong where the space is a single square, returning a row outside the space entirely (`inc/Base.h`) | **Observed** -- seed 3390 under `tools/keys/dive.keys`, one file between the builds: 47,954 out-of-bounds `Map::At()` reads before, 0 after. **The zero is measured on a different set of levels, not the same levels minus the defect** -- changing the rectangle changes what the random stream is spent on, and no way to hold that constant has been found (`docs/evidence/inc-5xn/README.md`). Both repairs were built and measured; both reach 0 | the reads are reported as rmtew#40 with a patch in rmtew#41, both open. The reply posted on #40 on 2026-08-19 proposed reserving two squares in `PlaceWithinSafely` instead. **This repair site was then described** on #40, [comment 5358799776](https://github.com/rmtew/incursion-roguelike/issues/40#issuecomment-5358799776), 2026-08-20, as prose and not as a patch | inc-65j |
| `Character::SkillLevel` grants the Sharp Senses inherent bonus to Spot and Listen but not to Search, though both entities whose prose describes the ability name all three skills together -- the elf (`lib/races.irh:1067-1068`, "+2 insight bonus to all sensory-based skills (Search, Spot, and Listen)") and the sentinel (`lib/prestige.irh:2448-2449`). Search read `CA_STONEWORK_SENSE` alone. Now takes the larger of the two abilities, because `s_inh` is the single inherent-bonus slot and inherent bonuses do not stack (`src/Create.cpp`) | **Observed** -- `tools/check_sharp_senses.sh` on seed 1: the same elf rogue, on two builds differing in nothing but this hunk, reads `Searching +5 (0 ranks, +3 INT, +2 train)` before and `Searching +7 (0 ranks, +3 INT, +2 train, +2 inherent)` after, while Spot and Listen are identical on both. The sheet is a true oracle: `src/Sheet.cpp:904` prints those terms by calling `SkillLevel` itself. The check is red on the pre-fix binary and green on the post-fix one | no | inc-tek.8.3 |
| Abilities whose levels are supposed to stack across classes are handed out by per-class grant lines, which count only their own class's levels. So a character who splits his levels serves EVERY class's waiting period instead of one, two half-rate classes each round their own odd level up, and two classes with a one-off grant both fire it -- although every one of those classes tells the player the levels stack (`lib/classes.irh:118`, `lib/classes.irh:2395`, `lib/prestige.irh:526`, `lib/prestige.irh:547`, `lib/prestige.irh:802`, `lib/prestige.irh:2566`, `lib/prestige.irh:3125`). `Character::CorrectStackedAbilities` now rebuilds the total for Uncanny Dodge, Sneak Attack, Evasion, Tracking and Lore of Arms from the character's class levels at every level-up: one waiting period, the shortest his classes offer and only once he has reached it, then every level in every granting class at that class's own rate. Rebuilt rather than accumulated, so the ORDER the levels were taken in cannot matter. The difference is carried by an `EXTRA_ABILITY` stati, which `Creature::AbilityLevel` already adds in (`src/Create.cpp`) | **Observed** -- `tools/check_stacked_abilities.sh` on seed 1, five characters on builds differing in nothing else, and `tools/check_huntsman_live.sh` on seed 7 for a sixth. All three failure modes are measured, not argued. Two waits instead of one: Barbarian 3 / Rogue 3 reads "Uncanny Dodge" before and "Improved Uncanny Dodge" after, off the same six levels a Rogue 6 spends to reach it. No wait at all: Bard 7 / Assassin 4 moves the other way, from "Improved" to plain, because his description promises 4 - 2 = 2. Two roundings instead of one: Elf Rogue 7 / Assassin 3, two odd level counts, reads "Sneak Attack +6d6" before and "+5d6" after. Two one-off grants instead of one: the Elf Ranger 3 / Rogue 3 / Twilight Huntsman 5 reads "Tracking (280 feet)" before and "Tracking (220 feet)" after, the 40-foot step being the +10 opening bonus that both his tracking classes were firing. The Rogue 6 and the Elf Rogue 5 / Assassin 2 are unmoved on both builds, which is what says the rule left single-class characters and even splits alone | no | inc-tek.8.3 |
| `inc/Api.h` declares the script side of every engine call, and the resource compiler turns those declarations into the virtual machine's dispatcher, `lib/dispatch.h`. `LOption` -- the call every script uses to add one line to a menu -- declared its value parameter `int16`, so the generated dispatcher wrote `T1->LOption(GETSTR(STACK(1)), (int16)STACK(2), GETSTR(STACK(3)))` and threw away the top half of it. That value is the caller's own cookie, and every script that opens a menu over objects puts an object handle in it (`lib/alchemy.irh:1062` and `:1241`, AutoDrop and AutoLoot) or a resource id (`lib/abilities.irh:98-237`, the undead-form menus); an `hObj` is a signed 32-bit handle and an `rID` runs past sixteen million. `TextTerm::LOption` itself takes an `int32` and `Option::Val` is an `int32` (`inc/Term.h:640`, `inc/Term.h:236`), so only the declaration was ever narrow. Handles are handed out in order from 128, so the first 32639 objects of a game are unaffected and the defect switches on partway through a character's life. Widened the declaration to `int32` and regenerated `lib/dispatch.h` (`inc/Api.h`) | **Observed** -- Brian's own save, sandboxed, Dragonkin Monk 4 standing on a large oak chest: marking one of the twelve items and accepting printed `You stop AutoLoot (finished looting)` and took nothing, because handle 44177 arrived at the macro as -21359 and AutoLoot's own first guard, `!isValidHandle`, dropped it in silence. A probe on `TextTerm::FirstSelected` read the truncated number out directly. After the fix the same save, same keys, prints `? spellbook [Conjurations and Summonings] taken.`, and four marked items come across in four ticks. `tools/check_menu_value.sh` is the regression check: `INCURSION_HANDLE_BASE` starts the handle counter at 40000 so an ordinary scripted character carries wide handles from birth, and the session drives AutoDrop. Red on a build differing only in that one cast, green with it; the same script with the ordinary 128 base passes on both builds, which is the control that stops an unrelated AutoDrop failure being read as truncation. Not a port artefact: `hObj` is 32 bits on Win32 too (`signed long` upstream, `signed int` here), the cast truncates identically on the upstream compiler, and `inc/Api.h` has not been touched since rmtew's own commits in 2014-2015 | no | inc-802 |
| `Character::XPPenalty` tests each of the three class slots with `TCLASS(ClassID[n])->HasFlag(CF_FAVOURED)` as the last clause, and `Game::Get` returns `NULL` for a zero id (`src/Res.cpp:312`), so a character holding fewer than three classes dereferenced `NULL`. Only the Wood Elf reaches it: an unfilled `TRace::FavouredClass` slot holds zero (measured, all 17 races in the module), so for every other race the zero class id matched the zero third favoured id and short-circuited the `||` before `TCLASS` ran. The Wood Elf is the one race that fills all three (`lib/subraces.irh:498`), so nothing short-circuits and the empty slot is dereferenced. Guarded each of the three blocks with its own `ClassID[n] &&` test, including the two that are safe today only by accident of the early return above them. The favoured-class rule itself is left alone -- that is inc-by8 (`src/Create.cpp`) | **Observed** -- `tools/headless.sh tools/keys/xp-penalty-crash.keys 7`, a Wood Elf Rogue 2 / Warrior 1 asking for his own sheet, on builds differing only in this function: exit 139 (SIGSEGV, `EXC_BAD_ACCESS` at `0x4d`, `Character::XPPenalty` under `TextTerm::CreateCharSheet`) with no dump written -> exit 0 with the `Rogue 2 / Warrior 1` sheet dumped. Guarded by (`tools/check_xp_penalty.sh`), which was confirmed red against the retained pre-fix binary | no | inc-5y8 |
| `Character::XPPenalty` charged the multiclass experience penalty by the wrong rule and at the wrong number. It asked whether the character's HIGHEST class was one his race favours and took a flat 25% when it was not, so a favoured class that was not the highest counted for nothing -- the Player's Handbook's own dwarf Fighter 7 / Cleric 2, which is meant to cost nothing, paid -- and a prestige class carrying `CF_FAVOURED`, the flag that says the class counts as favoured for everybody, was honoured only while it was also the highest, which its own entry requirements make impossible; so every prestige character paid from the level he entered the class. The manual said 20% while the code took 25%. Rewritten to the rule Brian ruled on: every class the race favours leaves the comparison, and so does every prestige class, then each remaining class standing two or more levels below the highest remaining class costs 20% and those costs add. Removing ALL of the race's favoured classes rather than the single one D&D grants is his ruling, on the grounds that D&D assumes a party and Incursion sends the player in alone. `lib/help.irh` rewritten to state the rule it now follows (`src/Create.cpp`, `lib/help.irh`) | **Observed** -- seed 1, one function between the two builds, every figure read off a character sheet (`src/Sheet.cpp:62-67`). Elf Barbarian 3 / Rogue 1 25% -> 20%; the same character with Warrior 1 added 25% -> 40%, the figure the old flat penalty could not produce at all; at Barbarian 3 / Rogue 2 / Warrior 2 25% -> nothing; Elf Rogue 5 / Assassin 2 25% -> nothing; Wood Elf Rogue 2 / Warrior 1 25% -> nothing. Control, a single-classed Elf Barbarian 3: nothing on both sides. `tools/check_xp_penalty_rule.sh`, confirmed green on the fixed build and red on the pre-fix binary, where five of the six characters fail and the control passes | no | inc-by8 |
| The Monk's `Ability[CA_KI_STRIKE]` grant was commented out in `lib/classes.irh:1401`, and no other class, race or item grants the ability, so nothing in the compiled module carried it. `src/Fight.cpp:4112` is the ONLY route by which an unarmed attacker may damage an incorporeal creature -- every other arm of that gate wants an `Item` with a plus, a blessing or ghost touch -- so every monk's fist, and every claw and bite, passed clean through a ghost, wraith or shadow forever. Restored the grant line (`lib/classes.irh`). Ki Strike is damage reduction only: the ability is read at exactly two engine sites, that incorporeal gate and the commented branch at `src/Fight.cpp:5735-5738`, with `src/Help.cpp:2041` and `src/Sheet.cpp:546` display-only, so restoring it adds no damage, no element and no to-hit. The bead's second claimed consequence, that `FT_WARCRY` is unreachable, is WRONG and was corrected on it: `src/FeatTab.cpp:1551-1554` gives the feat two alternative disjuncts and the first is Charisma 13 plus `FT_IMPROVED_DEMORALIZE` | **Observed** -- seed 1, one line of module data between the two builds, read off the character sheet's Special Abilities block. A Monk 4 showed no Ki Strike at all before and reads `Ki Strike +1` after; the same character at Monk 1 shows nothing on both sides, which is the grant's own "starting at 4th". `tools/check_ki_strike_live.sh`, confirmed red on the pre-fix module and green on the rebuilt one | no | inc-qep |
| `Creature::Damage` holds the ONLY test of `CA_WEAPON_IMMUNITY` in the engine, and it was gated on a hand-written list of seven attack types -- `A_SWNG`, `A_FIRE`, `A_HURL`, `A_GREA`, `A_LUNG`, `A_CALL`, `A_PREC`. `A_PUNC` (1), `A_CLAW` (6), `A_BITE` (7) and every other natural attack were absent, so a monk's fist and a housecat's claw never met the rule at all -- not beaten, never asked -- while `src/Help.cpp:2060` told the player the creature "is unaffected by weapons with a plus less than %d". The list was missing half of its own category too: `A_THRU`, `A_CHAR`, `A_RIPO`, `A_WHIR` and `A_SPRI` are manoeuvres made with the weapon in hand and bypassed the rule as well. Replaced the list with `isBlowStruckByACreature()`, a RANGE test over the `is_standard_attk` / `is_postgrab_attk` / `is_maneuver` macros `inc/Defines.h:1138-1203` already defines beside the attack constants, so a natural attack or manoeuvre added later is caught by the act that adds it; `A_HURL` is named individually only because its constant sits in the special band, and `A_COUP` is excluded on purpose (Brian ruled on 2026-08-21 that a coup de grace bypassing weapon immunity is intended). Restored the commented-out `CA_KI_STRIKE` branch three lines inside the same block at the same time -- it could never have fired even uncommented, because `A_PUNC` never reached the block. Spells, monster ability attacks and response effects fall outside every range tested; traps and acts of god arrive with `AType` 0 (nothing outside `src/Fight.cpp` assigns `EventInfo::AType`) and are also excluded explicitly (`src/Fight.cpp`) | **Observed** -- seed 1, one file between the two builds, one wizard-mode-summoned lemure (CR 1 devil, `ABILITY(CA_WEAPON_IMMUNITY,1)`, `lib/mon4.irh:1205-1213`) punched bare-handed by the same character at two levels. Pre-fix: the Monk 1 punch reads *"Catching it off-guard, you punch, hitting the lemure"* with no failure line, and the Monk 4 punch kills it. Post-fix: the Monk 1 punch reads *"you punch, hitting the lemure. Your weapon fails to penetrate."* and the Monk 4 punch, with Ki Strike +1 against immunity 1, reads *"You punch, hitting the lemure. The lemure reels!"* with no failure line. `tools/check_weapon_immunity_live.sh`, confirmed red on the pre-fix binary and green on the fixed one. **Scale note, not acted on here**: 35 distinct creatures in `lib/` carry `CA_WEAPON_IMMUNITY` and 13 of them are CR 5 or lower, so this fix makes inc-tek.12's mis-tagging question live for a low-level character for the first time | no | inc-bei |
| The whole two-weapon rule in `Creature::CalcValues` was gated on two `Item` pointers. Fists are not Items, so both are NULL bare-handed and every clause was dead -- including Two-Weapon Tempest, +10 speed and the largest two-weapon payoff in the game. `Character::HasFeat` grants a monk Two-Weapon Style for the express purpose of letting him BUY Tempest (its gate is Two-Weapon Style plus melee BAB 9), so the monk qualified, could spend a feat slot, and got nothing: he had to put his fists away and pick up two nunchaku to use the feat his fists had earned him. The SRD settles it -- "an unarmed strike is always considered a light weapon" -- so the gate now admits two empty fists. Three things had to come with it. (1) With both pointers NULL `offhandWep == meleeWep` is TRUE, so a bare-handed Double Weapon Fighting holder would have read as wielding a double weapon; the test now asks whether one ITEM fills both hands. (2) The size clause dereferences both pointers and asks whether the off hand is light; a fist is light by the SRD, so two fists skip it on the merits rather than by NULL-guard. (3) The off-hand attributes are built for a WEAPON -- `A_HIT_OFFHAND` takes its base from `AttkVal[S_MELEE]`, and a monk attacks at 75% in melee against 100% brawling -- so when the off hand is a fist the whole off-hand `AttrAdj` row is copied from the brawl row, bonus type by bonus type, before the two-weapon adjustments go on top, and the second fist in the brawl loop reads it. That replaces inc-dzz's two hand-written exemptions, which skipped the secondary-natural-attack penalties whenever both hands were empty without ever asking whether the character held the feats -- so a creature with two fists and neither feat got their benefit free. The duplicated dual-wield timing the bead's third consequence names is gone too: `WAttack` and the brawl path both call one `DualWieldTimeout()` the comment at `src/Creature.cpp:2711` records it (`src/Values.cpp`, `src/Fight.cpp`, `src/Term.cpp`) | **Observed** -- seed 1, `tools/keys/monk-tempest.keys`, a Lizardfolk Monk 1 / Warrior 10 with empty hands, every figure read off the character sheet's Brawl row. Pre-fix, after buying Two-Weapon Tempest: `Speed 125% (base 100% +25% Warrior)`. Post-fix: `Speed 175% (base 100% +25% Warrior +50% dual-weapon)`. A warrior's own speed does not move between Warrior 9 and Warrior 10, so the whole 50 points is the feat. No regression on inc-dzz: the 1st-level sidebar reads `Hit:5 / 5`, `Punch:1d6+4` twice, `Spd:100%` byte-for-byte on both builds. `tools/check_two_fist_feats_live.sh`, confirmed red on the pre-fix binary and green on the fixed one. The other half of the rule -- that a two-fist fighter WITHOUT the feats now pays the ordinary penalties -- cannot be built in the game, because `src/Create.cpp` grants Two-Weapon Style and Ambidexterity to anyone with `CA_UNARMED_STRIKE` and `TwoFistFighting()` requires `CA_UNARMED_STRIKE`; it was measured instead on two probe builds differing only in that grant being compiled out. Old code, no grant: `Hit:5 / 5`, unchanged, because the exemption never read a feat. New code, no grant: `Hit:3 / 1` -- the main fist -2 for want of Two-Weapon Style and the off fist -4 for want of both, which is exactly what two daggers cost | no | inc-nie |
| `Player::MoveDepth` collected the player's followers with `MapIterate`, which walks `m->Things` by index, and called `Remove()` on each one as it went. `Array::Remove` memmoves the tail one place left, so the entry after every collected follower slid into the index the loop had just finished with and `i++` stepped over it. A player who changed level with adjacent followers arrived with fewer than he left with, silently (`src/Feature.cpp`) | **Observed** -- `INCURSION_FOLLOWER_PROBE=1` with `tools/keys/followers.keys`: seeds 3362, 111, 555, 888, 999, 1234, 2468 reported `followers_before=3 collected=2 left_behind=1` before and `collected=3 left_behind=0` after; seed 13579 went 2/1/1 to 2/2/0; seed 777, whose two followers are not adjacent in `Things`, collected both on either build and is the control | no | inc-90u |
| `Player::MoveDepth` cleared the player's `ENGULFED` stati with a bare `RemoveStati`. A swallowed creature is deliberately kept out of its square's `Contents` chain by `Creature::DoEngulf` and carried by its coordinates alone, so the stati is what tells `Thing::Remove` to unlink it correctly. Removing it stranded the player in `m->Things` with the engulfer's coordinates and no `Contents` entry, and left the matching `ENGULFER` stati standing on the engulfer; the `PlaceAt` further down then hit `Error("Contents list wierdless in Thing::Remove!")` and abandoned the rest of the removal. Now hands the pair to `DropEngulfed`, which removes both halves and puts the victim back on the map (`src/Feature.cpp`) | **Observed** -- a `-DINC6D5_PROBE` build on seed 21 caught the drift: the player's x moved 38 -> 39 inside `Creature::DoEngulf` at turn 198040 with no `Move`, and the failed unlink followed at turn 198048. `tools/soak.sh 24 1 tools/keys/dive.keys` went from 1 error and 1 audit finding to none, and the 40-seed gate lost both its `wierdless` sessions and both orphan findings | no | inc-6d5 |
| `Creature::Perceives` declared `uint16 Per` uninitialised, then line ~408 ORed `PER_VISUAL` into it on the monster-perceives-its-leader path (a monster whose `getLeader()` or `OrderWalkNearMe` target is the thing being checked) -- a read of indeterminate storage the function then returns. Lines 386-408 assign `Per` nowhere before that `|=`, while every other path assigns it first. Initialised `Per = 0`, the no-bits value the OR intends (`src/Vision.cpp`) | **Traced** -- clang `-Wuninitialized`, its highest-confidence tier, names the read at `src/Vision.cpp:408`, and the reaching path is read out of the source; not observed in play. Guarded by `tools/check_uninit_reads.sh` | no | inc-5aw6 |
| The `MM_CONTROL` polymorph arm (`src/Effects.cpp`) set `best` only when the caster `isMonster()` (a scoring loop) or `isPlayer()` (a menu). An NPC caster is neither, so `best` stayed uninitialised and `mID = Candidates[best]` indexed the 40-entry array out of bounds; NPCs do cast. Initialised `best = 0`, a valid index -- correct NPC selection (routing it through the monster-scoring loop) is a separate design question, not this UB fix. Misbehaves identically on Win32 (`src/Effects.cpp`) | **Traced** -- clang `-Wsometimes-uninitialized` names the use and the not-monster-not-player path is read out of the source; not observed. Guarded by `tools/check_uninit_reads.sh` | no | inc-l796 |
| `Player::GainAbility`'s `CA_TATTOOS` case declared `bool found` uninitialised and set it `true` only when the selection loop offered an eligible tattoo; an all-filtered loop left the `!found` guard reading indeterminate storage, and a non-zero value skipped the "no remaining tattoos" message, opened an empty `LMenu` and asserted. Unreachable in play -- 13 of the 14 tattoos in `lib/alchemy.irh` carry no `EV_PREREQ` and a mystic gains at most 5, so the list cannot empty -- but undefined on any compiler. Initialised `found = false`, matching the sibling `Reincarnate()` at `src/Create.cpp:769` (`src/Create.cpp`) | **Traced** -- clang `-Wconditional-uninitialized` names the read at `src/Create.cpp:~3297` and suggests `= false`, and reachability is disproved by counting the tattoo/prereq definitions; not observed. NOT held by `tools/check_uninit_reads.sh`, which excludes the noisy `-Wconditional` tier; the sibling idiom is the standing correctness reference | no | inc-5yb |
| `Creature::CalcValues` builds all five attack modes in one loop, and the `S_BRAWL` arm of its `switch(i)` had no `break`. It fell into the `S_MELEE` arm, the `it = NULL` it opens with was dead, and the brawl slot was handed the melee weapon: a fist collected that weapon's `Acc`, `Spd`, enchantment plus and weapon-skill bonuses. Because the same function moves a bow to the archery slot and then takes the melee weapon from the LEFT SHOULDER instead (`src/Values.cpp:301-308`), an archer's fists collected them from a sword he was not even holding. Every other arm of the switch breaks, and the loop's own `if (!it) continue` is the no-item path the `ww:` comment beneath the switch describes, so `NULL` is what the brawl arm was written to leave. Added the `break` (`src/Values.cpp`) | **Observed** -- seed 1, elf ranger, elven long bow in his hands and elven long sword on his back, `src/Values.cpp` the only file between the two builds. The character sheet's Brawl block read `+toHit +2 (Ranger +0, +2 weapon)` and `Speed 130% (base 100% +5% Ranger) x 125% weapon` before, and `+toHit +0 (Ranger +0)` and `Speed 105%` after. The Melee block is the control and is unchanged at `+3` and `130%`, so the fix cannot pass by taking weapon bonuses away from everybody. `tools/check_brawl_weapon.sh`, confirmed red on the unfixed build and green on the fixed one | no | inc-nwk |
| `Creature::CalcValues`'s `switch (HungerState())` had no `break` at the end of its `HUNGRY` arm, so a Hungry character paid the `STARVING` penalty as well and came out at FAT/STR/CON -3 where Starving costs -2 of each. Three attributes went UP when he grew hungrier. The arms are exclusive by construction: `BLOATED` breaks, and the `WEAK`/`FAINTING` arm restates its own -2/-4/-4 in full rather than adding to the arm above it. Added the `break` (`src/Values.cpp`) | **Observed** -- seed 1, Dragonkin barbarian, `src/Values.cpp` the only file between the two builds. Sidebar STR/CON, photographed fed, Hungry and Starving in one session: 16/15, 13/12, 14/13 before; 16/15, 15/14, 14/13 after. The fed photograph is the baseline and the Hungry-to-Starving pair is the assertion. A Dragonkin is used because the ordinary once-per-turn hunger tick does nothing for anybody -- that is inc-i9q.2, a separate defect, still open -- and Burning Hunger is the only caller that reaches the working arm of `Creature::GetHungrier`. `tools/check_hunger_penalty.sh`, confirmed red on the unfixed build and green on the fixed one | no | inc-i9q.3 |
| `Portal::Enter` asked "The stair leads to <terrain>. Confirm unsafe action?" before every descent, and the terrain it named was one the player could never arrive on. The test that decided whether the arrival square was unsafe -- the terrain's own `EV_MON_CONSIDER` -- sits commented out directly above the prompt (rmtew, a731043, 2014-07-15), so nothing was left to gate it and the prompt fired on every descent whose old coordinates fell inside the new map. It read those coordinates on the NEW map, and `Player::MoveDepth` throws that square away when it is out of bounds, solid or part of a vault: it re-rolls a random open one instead. So "the stair leads to Dungeon Wall" was false by construction -- a solid square is exactly the square the player is never placed on. Both placement rules now live in one function, `ArrivalSquare`, which `MoveDepth` places with and the warning asks, and the commented-out `EV_MON_CONSIDER` test -- the same one `Move.cpp:477` uses to ask a walking player about deep water -- decides whether to ask at all (`src/Feature.cpp`) | **Observed** -- Brian's own save, restored standing on the down staircase at (74,70) on depth 2 of The Goblin Caves, one file between the two builds: pre-fix, `>` prints "The stair leads to Dungeon Wall. Confirm unsafe action? [yn]" and the descent never happens (status line stays 020m); post-fix, no prompt and the character arrives on depth 3 (030m). `tools/check_stair_warn.sh` is red on the first and green on the second | no | inc-wcf |
| `TextTerm::LMenu` ran out of selection letters and lost every option past them. `MenuLetters` is 52 letters padded with two spaces (36 letters and 18 spaces in the roguelike keyset); the draw wrote `MenuLetters[min(53,i)]` and the keypress resolved back through `strchr()` into the same string, so the 53rd option and everything after it was drawn `[ ]` and no key could name it -- a space never reaches the lookup, because `case ' '` eats it first. Scrolling did not help: the letter followed the option's INDEX, not its place on the page. Menus that fit the alphabet are untouched, byte for byte. A menu that does not now pages: `vRows` is capped so no more rows are drawn than there are letters, `vStart` is quantised to whole pages so a letter cannot change meaning under the cursor, the draw letters each row from its place on the page, the keypress adds `vStart` back and refuses a letter past the page's last row, and `TAB` turns the page. `TAB` and not `PGUP`/`PGDN`: those two already scroll the description pane in this same switch, as `KY_CMD_NORTHEAST`/`KY_CMD_SOUTHEAST` (`src/TextTerm.cpp`) | **Observed** -- seed 7, `tools/keys/menu-overflow.keys`, one file between the two builds. A Wood Elf Druid at character generation is offered 88 feats, of which 36 -- Natural Aptitude, Skill Focus, Quicken Spell, Woodsman and the `(Show All Feats)` escape hatch among them -- were unselectable. Pre-fix the run stops with `@choose (that entry has no letter; use @cursorto and ENTER) "Natural Aptitude"`; post-fix the menu pages `1-51 of 88` then `52-88 of 88`, `[h] Natural Aptitud` is pressed, the follow-up `Choose a skill:` prompt appears, and the finished character's sheet carries Natural Aptitude in its Feats block. `tools/check_menu_overflow.sh`, confirmed red on the pre-fix binary and green on the fixed one. No regression in `tools/check_race_feats.sh`, `check_key_directives.sh`, `check_menu_value.sh`, `check_store_scroll.sh`, `check_prestige_profs.sh`, `check_prestige_tables.sh`, `check_huntsman_live.sh` or `check_headless.sh`, all of which drive menus by letter. Not a port artefact: the string, the clamp and the `strchr` are plain C++ with no typedef, no width and no platform call in them, so a Win32 build on the original compiler loses the same options | no | inc-ysa |
| The Earthsinger's Grounded Stance, the class's signature ability, added nothing at all. Its `TRAP_EVENT` stati carries `yval: PRE(EV_HIT)`, and `src/Event.cpp:325-336` rewrites `e.Event` to `META(e.Event)` before calling the effect, so a trap's handler has to be named `META(<yval>)`. This one was named plain `PRE(EV_HIT)`, a name no dispatch ever asks for, so the body never ran and every guard inside it -- levitation, plane, boots, terrain -- was moot. Divine Sacrifice (`lib/pspells.irh:1156,1171`) and Death Attack (`lib/prestige.irh:600,652`) show the pairing the engine expects. `lib/m_items.irh:5686` (Frostbite) and `:5698` (Fireburn) carry the same defect against `EVICTIM(EV_DAMAGE)` and are NOT fixed here (`lib/prestige.irh`) | **Observed** -- same seed and key script; a barefoot Bard 7 / Earthsinger 2 standing on cave floor rolled `Bofi's Damage: 1d6 = 6` on all three landed blows before and `Bofi's Damage: 1d6 +2 GS = 8` on all three after. `tools/check_grounded_stance_live.sh`, confirmed red on the pre-fix module and green on the rebuilt one | no | inc-brp |
| `Creature::Strike` re-throws `EV_HIT` for every `A_ALSO` rider and every `A_CRIT` rider behind the attack that landed, and asked nothing in between about whether the victim had survived the first blow. A bite that kills is followed by its poison rider aimed at a corpse -- esran reports that as a class of crash, from handlers downstream of `EV_HIT` that dereference the dead creature's map pointer. One sibling loop had the same hole and is fixed alongside: the `A_ALSO` loop that applies a breath weapon's riders (`Creature::Hit`), which is safe to guard because that function is entered with `this == EVictim` by definition. Two further loops in `Creature::SAttack` -- the `A_SONI` sound sweep and the `A_CPRX`/`A_PROX` swarm walk -- were guarded in a first draft and the guard was REMOVED again on review: both damage the creature in `e.ETarget`, not the one in `e.EVictim` that the guard reads, and the sound sweep reassigns `e.ETarget` per listener while `e.EVictim` stays fixed, so the guard would have stopped a roar from damaging the rest of the room as soon as it killed its nominal target. Neither loop was ever reproduced or measured; they are tracked for investigation as inc-iekz. Both fixed loops stop at `isDead()`, which is the liveness test the rest of the engine already uses -- it reads the `F_DELETE` flag (`inc/Map.h:733`) rather than following a pointer, and death sets that flag long before the object is reaped. **This is the narrow rule only -- a rider attack stops when its target dies -- and it does NOT fix the crash class.** rmtew's objection in discussion #38, that this hides the root cause, is correct about the handlers; those are tracked separately as inc-wdi. The fix is upstream's own, esran's `ee51c8a`, never merged (`src/Fight.cpp`) | **Traced** -- read the loops and `Thing::isDead`; no reproduction of the crash itself exists here. The loop's behaviour IS measured: `tools/check_rider_corpse.sh` on seed 1, `src/Fight.cpp` the only file between the two builds, the player polymorphed into a giant spider biting a 25-hit-point giant rat. Before: 2 riders thrown at a living rat, 1 rider thrown at the corpse, 1 `EV_HIT` delivered to the corpse. After: the same 2 riders at the living rat, 0 at the corpse, 0 `EV_HIT` on a corpse, 1 rider loop recorded as stopped. The living-rat count is asserted too, so a fix that simply deleted every rider fails the check. Red on the unfixed build, green on the fixed one. Not a port artefact: the missing test is plain C++ and behaves the same on Win32 with the original typedefs | no | inc-upw.32 |
| Activating an item from the `y` verb menu never asked where to aim it, so a blast resolved on the person who activated it. `Player::YuseMenu` runs only the fixed prompts in that verb's `YuseCommands` row, and the Activate row's `QTarget` is 0 -- correctly, because which query an activation needs belongs to the item and the item is not known until the menu has run. Nothing afterwards asked the item, and with no target chosen `Magic::MagicEvent` (`src/Magic.cpp:746-747`) makes the activator the victim. The item scripts were not at fault: `lib/m_items.irh` declares `qval: Q_DIR\|Q_TAR` on the head of both reported chains, and a sweep of every activated blast or ray effect in `lib/` found no other item missing one. `Player::ItemMenu` already asked, which is why the `a` command prompted on the same item on the same turn; that same `EffectPrompt` call is now made on the `y` route once the item is settled (`src/Player.cpp`) | **Observed** -- seed 1, one file between the two builds, a wizard-mode Circlet of Blasting worn by chargen.keys' orc barbarian. Pre-fix the screen reads *"Your own beam strikes you! You are gravely wounded!"* with no prompt in front of it; post-fix it reads *"Select direction or target: [????\*dtn]"*, and aiming east leaves the player at `HP:42/42`. The ring of Brass and Glass, the second item Brian reported, prompts too -- it matters because its `qval` sits on an `EA_GRANT` head rather than on the blast segment. Control: the `a` command prompted on both builds. `tools/check_yuse_activate.sh`, confirmed red on the pre-fix binary and green on the fixed one | no | inc-azc |
| `Item::DrinkPotion` and `Item::ReadScroll` both open by calling `Item::TakeOne()`, which takes the unit OUT of the pack -- `Remove(false)` on the last of a stack, a fresh parentless copy otherwise -- and both used to end with an unconditional `Remove(true)`, so every path that returned early threw that unit away. Two of those paths are player-visible losses. Reading a scroll whose level beats your Decipher prints *"This scroll is powerful, and possibly beyond you!"*, rolls a Will save, and OFFERS to stop; accepting the game's own offer returned `ABORT` and cost a scroll for reading nothing. Quaffing a potion whose `EV_EFFECT` returns `ABORT` destroyed the potion the effect had just refused to produce. Three more paths leaked the unit out of the world instead of disposing of it: the elf/necromantic branch in each function, and the countered-scroll branch. The give-back is now `Item::GiveBackTo` (`src/Item.cpp`, beside `TakeOne`, `inc/Item.h`), which is the two lines `ReadScroll`'s own preserve branch already used, and that branch now calls it. Same defect and same shape as inc-i9q.1 in `Food::Eat`, whose refusal branch had to be routed through `StopEating` for the same reason. NOT changed, and both are separate decisions: an aborted quaff still costs its 30 Timeout, and `Item::ZapWand` still decrements `Charges` before throwing `EV_EFFECT` (`src/Magic.cpp`) | **Observed** for the scroll half, **Traced** for the potion half. Seed 5, `tools/keys/consumable-abort.keys`, `src/Magic.cpp` + `src/Item.cpp` + `inc/Item.h` the only files between the two builds. Pre-fix, answering yes to *"Stop reading?"* took *"3 Scrolls of Wizard Sight"* down to 2; post-fix the stack stays at 3, and the control half -- drinking one of two Potions of Healing -- still leaves one, so the fix has not simply stopped consuming things. `tools/check_consumable_abort.sh`, confirmed red on the pre-fix binary and green on the fixed one. The potion half is Traced only because it cannot be driven from the keyboard today: no `AI_POTION` effect in `lib/` returns `ABORT` from `EV_EFFECT`, and the one potion prompt a player can escape is asked by `Player::UseItemMenu` BEFORE `EV_DRINK` is thrown, so escaping it never reaches `DrinkPotion`. The two functions are the same shape, so the scroll proves the branch | no | inc-bp2 |
| `WeaponGroupNames[]` is the only thing that turns a creature's `Proficiencies` bits into words, and it carried no row for `WG_LIGHT` (`inc/Defines.h:2110`). All three printing sites guard each bit with `LookupOnly(WeaponGroupNames, 1L << i)` and step over a bit the table does not hold without saying so -- the character sheet (`src/Sheet.cpp:855`), the class help page (`src/Help.cpp:101`), and `src/Help.cpp:1240`, which walks the table itself and so gave light weapons no section at all. So no class ever said it was proficient with light weapons: the bard, whose own prose promises them (`lib/classes.irh:289`), the warrior (`lib/classes.irh:2872`), the assassin (`lib/prestige.irh:526`) and the Thaumaturge mage (`lib/classes.irh:1021`). Added `{ WG_LIGHT, "Light Weapons" }` in bit order, between `WG_FIREARMS` and `WG_FLAILS` -- the row must sit before `WG_SHIELDS`, because the `src/Help.cpp:1240` loop breaks there; the wording follows the prose, and the title case follows the neighbouring rows. `WG_CROSSBOWS` was missing for the same reason and was added on 2026-08-20 (f5444e9) (`src/Tables.cpp`) | **Observed** -- seed 1, `tools/keys/bard-profs.keys`, `src/Tables.cpp` the only file between the two builds. An elf bard's sheet read *"Proficiencies  Simple Weapons, Short Blades, Archery, Daggers, Light Armour, rapier, long sword, long bow and short bow"* before the fix and *"... Daggers, Light Weapons, Light Armour, rapier, ..."* after it -- 5 named weapon groups became 6, and no other word on the line changed. `tools/check_weapon_groups.sh`, confirmed red on the pre-fix binary (both halves: 1 of 23 declared `WG_` bits unnamed, and the sheet missing Light Weapons) and green on the fixed one. | no | inc-tek.15 |
| `Creature::Dig` divided by `SkillLevel(SK_MINING)` with no zero guard, and zero is reachable in ordinary play: nothing floors a skill rating at 1, and `MaterialHardness` answers an immune material with **-1** rather than a large number, so `if (hard > pow)` lets an untrained digger straight through. A gnome warrior with Wisdom 3 holding a plain pickaxe arrives at the division with exactly 0 (training +2, skill-kit +2, attribute modifier -4). AArch64 `SDIV` quietly yields 0, which is why the port survived it; x86 `IDIV` raises #DE, so upstream's own platform crashes (`src/Skills.cpp`) | **Observed for the zero divisor, Traced for one step of the reach** -- `tools/check_dig_zero_skill.sh`, seed 1: the `-DDIG_PROBE` build logs `hard=-1 pow=0 skill=0` at the division, a `-fsanitize=integer-divide-by-zero` build aborts on that line, and the same run reports 100~ structural integrity before the guard and 0~ after. **The run gets there through a second, separate defect** and this row must not be quoted as a player-reachable crash without it: the verb menu's Dig asks for a direction and nothing turns that direction into a target square (`Creature::SetupDig` is an empty stub and `Creature::Dig` reads `e.EXVal`/`e.EYVal`, which `e.Clear()` left at 0), so the measured swing landed on square (0,0), the permanent border. The DIVISOR does not depend on that -- `SkillLevel(SK_MINING)` is 0 whatever square is dug, and 0 is what was measured. What is only **Traced** is that a correctly aimed dig also meets a hardness of -1: solid igneous rock answers -1 too, and `Map::Generate` rewrites every wall square adjacent to magma, brimstone, smooth igneous rock or obsidian into it, so a Magma Vein's walls are squares a player stands beside. That last step was read, not run | no | inc-4ht |
| Helmet, gauntlet and boot Coverage was declared in the data and promised to the player, and no code read it. Two separate holes, both in `Creature::CalcValues`: the `armourSlots[]` array it walks held only `SL_ARMOUR`, `SL_READY` and `SL_WEAPON`, so the loop never saw a helmet, a gauntlet or a boot at all; and the type chain inside the loop branched only on `T_SHIELD` and `T_ARMOUR`, so even once reached they had no branch to land in. `help.irh:3061` tells the player that "coverage can be increased by wearing protective items such as armoured helmets and boots", and `weapons.irh:1301` gives the hard steel cap `Coverage: 2` and the cured leather boots `Coverage: 1`. The player paid weight, money and an armour penalty for a stat that went nowhere. Both holes fixed; a helmet is built as a plain `Item` (`Item.cpp:298`) whose `CovVal` returns 0, so its Coverage is read from the template -- which the parser fills for every item type (`yygram.cpp:2458`) -- rather than by changing what class a helmet is and with it the saved form of every helmet in every save file (`src/Values.cpp`) | **Traced** -- read `CalcValues`, `Item::CovVal`, `Armour::CovVal` and the parser together; `grep -n A_COV src/Values.cpp` returned those two branches and nothing else, so no other site could have picked the value up. **No live A/B exists and none is currently possible**: the only items in the module carrying helmet, gauntlet or boot Coverage are mundane, and `src/Tables.cpp:320` gives the wizard-mode acquisition browser no category reaching `T_HELMET`, `T_BOOTS` or `T_GAUNTLETS`, so no scripted session can put one on a character. `tools/check_armour_model.sh` checks the constant those items add up to (`NATURAL_COVERAGE` = suit 10 + helm 2 + gauntlets 1 + boots 1) against the shipped data, and states plainly that the branch itself is unmeasured | no | inc-vifx |
| The Wand of Animal Summoning summoned dragons. Its whole constraint line was copied off the Wand of Dragon Summoning, the entry directly above it in the same file, so it read `xval: MA_DRAGON; rval: $"generic summons";` (`lib/m_items.irh:2832`). `Magic::Summon` copies `xval` into `xe.enConstraint` before throwing `EV_ENGEN` (`src/Effects.cpp:1454`), and `enConstraint` is the monster-type filter the encounter generator selects on, so the wand drew from the dragon list. Its own `Desc` promised an animal, and the other three generic-summons wands each name their own kind -- `MA_BEAST`, `MA_UNDEAD`, `MA_VERMIN`. Changed to `MA_ANIMAL`, which 65 monsters declare across `lib/mon*.irh` (`grep -c '^Monster "[^"]*" : .*MA_ANIMAL'`; a plain `grep -c MA_ANIMAL` answers 81, but 16 of those are list headers, templates and script conditions rather than monsters). A copy-paste slip in a data file: `lib/m_items.irh` is read identically on Win32, with the original typedefs and on the upstream compiler (`lib/m_items.irh`) | **Observed** -- `tools/check_wand_animal.sh`, six seeds x eight shots, the module the only thing between the two builds. Before the fix 12 creatures arrived over 48 shots and **0 were animals**: 5 shattering ur-dragons, 4 plant ur-dragons, 2 brilliant ur-dragons, 1 copper dragon. After it 32 creatures arrived over the same 48 shots and **all 32 were animals**: 14 elephants, 8 giant crocodiles, 2 smilodons, and 8 others. Confirmed red again by reverting the one constant and rebuilding, which reproduced the 12/0/12 figures exactly | no | inc-tek.8.8 |
| Holy Summoning I to VI and Summon Nature's Ally I to VI carried no `Flags` clause at all, so none of the twelve set `EF_XSUMMON` -- the engine's own one-live-summon-per-spell limiter, which `Magic::Summon` enforces at `src/Effects.cpp:1374-1381`. The wizard summoning line sets it, and so do Dust Devil, Spiritual Hammer and Flaming Sphere, so a mage was limited and a priest or druid was not. Incursion replaced Vancian spell slots with a regenerating mana pool, which removed the only other brake, so a monster priest could summon without limit -- which is how this was met, in play. Added the flag and an `MSG_XSUMMON` refusal message to all twelve, and wrote the limit into each spell's own `Desc` the way lesser planar conjuration already does (`lib/pspells.irh`) | **Observed** -- seed 5, `tools/keys/xsummon-priest.keys`, one file between the two builds. Two casts of Holy Summoning I on consecutive turns, with wizard-mode Infinite Mana on so the second cast cannot be refused for mana before it reaches the guard: "Your protectar appears!" twice before, and "Your protectar appears!" then "The outsider you called before still serves you." after. `tools/check_xsummon_live.sh`, confirmed red on a module rebuilt from the pre-fix `lib/pspells.irh` and green on the post-fix one; it also asserts that all twelve ranks still carry the flag, because the live half exercises one of them. Not a port artefact: `lib/` is unchanged by the port, and a Win32 build reads the same missing clause | no | inc-j6ni |
| Spook did not set `EF_ALLIES_IMMUNE`. It is a permanent mobile aura centred on its caster (`EF_PERSISTANT`, `FI_MODIFIER|FI_MOBILE`, radius 3) giving a -2 morale penalty on every roll, and `Magic::isTarget` spares the caster's own side only for an effect carrying that flag (`src/Magic.cpp:340`). `EF_CASTER_IMMUNE` spares the caster alone. So a night hunter taken as a companion held its own master, and every party member, at -2 for as long as it lived, with nothing to time it out. Paralyzing Aura, the closest thing in the game -- a permanent monster debuff aura at `lib/mon4.irh:519` -- already sets the flag. `EF_NOTBAD` was deliberately left alone: despite its name it gates friendly-fire retaliation (`src/Monster.cpp:2695`), and clearing it would set allied casters against each other (`lib/wspells.irh`) | **Observed** -- seed 5, `tools/keys/spook-ally.keys`, one file between the two builds. Orc warrior, Freeze Monsters on so the bat neither attacks nor follows, four states read off the character sheet's Hit line: bare 6; a HOSTILE night hunter beside him 4; tamed with wizard mode's Make Player Master of Monster and walked out of radius 6; walked back in 4 before and 6 after. The hostile state is the control and reads 4 on both builds, so the spell was aimed and not switched off. `tools/check_spook_ally.sh`, confirmed red on the pre-fix module and green on the post-fix one. Not a port artefact: `lib/` is unchanged by the port, and a Win32 build reads the same missing flag | no | inc-9mhh |
| Sanctuary registered a `TRAP_EVENT` trap for `EV_EFFECT` and defined a `META(EV_STRIKE)` handler, so the two lists did not match and the handler that ends the spell when its holder attacks could never run: `src/Event.cpp:322-340` walks a victim's traps and calls a trap's effect only where `META(S->Mag)` equals the event being thrown, and no trap carried `EV_STRIKE`. The spell's own `Desc` promises "This protection ceases if you take any directly hostile action against another creature", and the 3.5 SRD says the subject cannot attack without breaking the spell, so a priest could hold Sanctuary while summoning and swinging -- which is how this was met, in play. Changed the one trap registration from `EV_EFFECT` to `EV_STRIKE`. No `META(EV_EFFECT)` handler was added in its place, because the SRD says the spell "does not prevent the warded creature from being attacked or affected by area or effect spells", so the unanswered `EV_EFFECT` trap was producing the correct behaviour by accident; the `SS_MISC` source is left alone as well, being the comment's deliberate choice so that Sanctuary cannot be dispelled. Not a port artefact: `lib/` is unchanged by the port, the mismatch is between two lists in one declaration, and the dispatch reading it is plain control flow with no dependence on integer width, the original typedefs or the upstream compiler (`lib/pspells.irh`) | **Observed** -- seed 4, `tools/keys/sanctuary-strike.keys`, one file between the two builds. An orc priest given Sanctuary through wizard mode's Learn Any Spell, with Freeze Monsters on so that only his own blow counts. The oracle is the game's Sanctuary indicator on the map status line, which `src/Term.cpp:185` prints from the player's live stati on every redraw. Three screens: after the cast, after a summon and three searches, and after one melee blow at a goblin. Before, the indicator read `Sanctuary` on all three and the three `TRAP EVENT` stati (`Mag` 2019, 52, 83) survived the blow; after, the traps read `Mag` 2019, 19, 83, the indicator still stands on the middle screen -- the control, and the SRD's own "may use nonattack spells or otherwise act" case -- and is gone on the third, where the game prints "You feel less safe." `tools/check_sanctuary_strike.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with `EV_EFFECT` put back | no | inc-r1e6 |
| The paladin spell Zeal is a copy of Sanctuary and copied its defect (inc-r1e6) unchanged: it registered a `TRAP_EVENT` trap for `EV_EFFECT` and defined a `META(EV_STRIKE)` handler, so the two lists did not match and the handler that ends the spell could never run -- `src/Event.cpp:322-340` calls a trap's effect only where `META(S->Mag)` equals the event being thrown, and no trap carried `EV_STRIKE`. The spell's own `Desc` promises "If the paladin attacks any other target, the effect ceases -- the target is free to attack him in turn", so a paladin under Zeal could swing at anything and keep it. Changed the one trap registration from `EV_EFFECT` to `EV_STRIKE`. No `META(EV_EFFECT)` handler was added in its place, for the reason given at Sanctuary above, and the `SS_MISC` source is left alone for the same reason. The three handlers read the spell's own exemption flag back as `$"Zeal"` while the cast grants it as `$"zeal"`; that pair of spellings is one symbol and needed no second correction, because `Module::FindResource` (`src/Res.cpp:227`) matches a resource name with `stricmp` and `Game::Find` (`src/Res.cpp:358`) caches on the same comparison. Not a port artefact: `lib/` is unchanged by the port, the mismatch is between two lists in one declaration, and the dispatch reading it is plain control flow with no dependence on integer width, the original typedefs or the upstream compiler (`lib/pspells.irh`) | **Observed** -- seed 4, `tools/keys/zeal-strike.keys`, one file between the two builds. A human paladin of Erich given Zeal through wizard mode's Learn Any Spell, with Freeze Monsters on so that only his own blows count, and two summoned babaus -- one east, the target Zeal chose, one north. The oracle is the game's Zeal indicator on the map status line, which `src/Term.cpp:185` prints from the player's live stati on every redraw. Four screens: after the cast, after three searches, after one blow at the CHOSEN babau and after one blow at the OTHER one. Before, the indicator read `Zeal` on all four and the three `TRAP EVENT` stati (`Mag` 2019, 83, 52) survived both blows; after, the traps read `Mag` 2019, 83, 19, the indicator still stands on the first three -- including the blow at the chosen target, which is what proves the exemption still works and is the empirical answer to the `$"Zeal"`/`$"zeal"` question -- and is gone on the fourth, where no trap is left at all. `tools/check_zeal_strike.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with `EV_EFFECT` put back | no | inc-tek.31 |
| `Magic::MagicHit` tested immunity before running a field LEAVE or an item UNWIELD. Those passes only undo what the matching enter or wield applied -- `Magic::Grant` and `Magic::Inflict` both open with an `isRemove || isLeave` branch whose whole body is `RemoveEffStati`. Asking whether the effect may be APPLIED is the wrong question there, and a creature that became immune while standing inside a field was refused the removal and kept a stati it could never shed, with "You are unaffected." printed on the way out. Routes in: gaining a resistance, being polymorphed, or becoming friendly to the field's caster. Immunity is now skipped when `isLeave` or `isRemove` is set; only `EA_GRANT` and `AR_MFIELD` segments reach this line on such a pass, per the filter at `src/Magic.cpp:724-729`, so no blast and no saving throw is bypassed (`src/Magic.cpp`) | **Observed** -- found by fixing the row above, which turned the defect from latent into permanent. Same seed 5 and `tools/keys/spook-ally.keys`: with `EF_ALLIES_IMMUNE` added and this line untouched, the tamed bat's -2 followed the player out of the field and never left (walked-out state Hit 4, and the screen carries "You are unaffected."); with both fixes the same state reads Hit 6 and the message is gone. Guarded by `tools/check_spook_ally.sh`, whose walked-out assertion exists for this defect alone. Regression: `tools/check_headless.sh` and `tools/check_alienist_live.sh` -- the latter being the other mobile modifier field in the game -- both green after | no | inc-9mhh |
| `Thing::Remove` destroyed every mobile field its subject had created, and `Creature::Mount` reaches it. Mounting is not leaving: the animal goes under you, not away, but `Creature::Mount` unlinks it from the map's lists with `Remove(false)` (`src/Skills.cpp:4313`) and the sweep cannot tell that from a creature walking off the level. So climbing onto a Spook-casting night hunter destroyed the aura it was emitting from underneath its rider. The engine's intent is the opposite -- `Thing::Move` and `Thing::PlaceAt` both deliberately carry a field created by the mover OR ITS MOUNT, and the author's own comment at `src/Display.cpp:307` records the gap: "Temporary assumption: mounts don't emit mobile fields. This is a very, very shaky assumption (dragons casting Spook, etc.)". `Remove` gained a defaulted `keepMobileFields`, passed by `Creature::Mount` alone, which then re-centres the kept fields on the rider's square with `Map::MoveField` so only creatures that actually cross the edge are notified. Size fields are never kept: they mark the squares a body fills, and a carried body fills none of its own, so keeping one would make `MoveField` try to fit a Huge mount's footprint around its rider (`src/Display.cpp`, `src/Skills.cpp`, `inc/Map.h`, `inc/Item.h`) | **Observed** -- seed 5, `tools/keys/spook-mount.keys`, one build between the two columns. Orc warrior, Freeze Monsters on, a night hunter summoned and tamed. On foot he reads Hit 4 with `Spook` on the status line, on both builds -- that is the control, and it fails if a fix merely stops the bat casting. Mounted on the same bat: Hit 7 and no `Spook` before, Hit 5 and `Spook` after, 5 being the 4 plus the +1 for riding. That -2 is a debt from before the bat was tame: wizard mode summons it hostile and taming does not scrub a penalty already taken. It is the only witness available to the aura's survival, because a player who is immune on entry cannot tell a live aura from a dead one. The same run then clears the debt by stepping out of the radius and back in, and reads Hit 6 on foot and Hit 7 mounted with no `Spook` on either -- an animal you own demoralizes you in neither posture, which is what `EF_ALLIES_IMMUNE` fixed under inc-9mhh and what this run now also guards. `tools/check_spook_mount.sh`. Not a port artefact: plain control flow, no dependence on integer width, the typedefs or the compiler | no | inc-izzy |
| `Thing::PlaceAt` gave a mount's fields to its rider. The harvest above it collects mobile fields created by the creature being placed OR BY ITS MOUNT, and the re-creation named `(Creature*)this` -- the rider -- as creator of all of them. So an animal's aura changed hands at every staircase. Three reported symptoms follow from that one word: the rider stops being affected, because the spell spares its own caster and the caster is now him; the animal starts being affected, because it no longer counts as the caster; and it recasts without end, because the guard that asks "is my aura already up?" looks for a field IT created (`src/Monster.cpp:2350`, `src/Magic.cpp:2370-2372`) and every one now names the rider. The count grows by one per level, which is where four `Spook` rows in one dismiss menu came from -- that menu lists a field only when the player is its recorded creator (`src/Skills.cpp:398`). Each field is now re-created with its original creator, falling back to the placed creature when the handle no longer exists (`src/Display.cpp`) | **Observed** -- seed 5, `tools/keys/spook-mount.keys`, third state. The before-column needs the row above already fixed, because with neither fix there is no aura left at the staircase to hand over; measured on 2026-08-22 on a build carrying the `Thing::Remove` fix alone. Mounted, one wizard-mode depth change, then the dismiss menu: `[a] Dismount the night hunter` and `[b] Drop Spook` before, `[a] Dismount the night hunter` alone after. The `Dismount` row is the control and proves the menu drew. `tools/check_spook_mount.sh`. Regression after both fixes: `tools/check_headless.sh`, `tools/check_spook_ally.sh`, `tools/check_xsummon_live.sh` and `tools/check_alienist_live.sh` -- the last being the other mobile modifier field in the game -- all green. Not a port artefact: plain control flow, no dependence on integer width, the typedefs or the compiler | no | inc-izzy |
| The Earthsinger's Geomancy paid `5d12+12` mana while the class page and the effect's own description both promise `5d12`. The whole line was copied from the *Mana* potion (`lib/m_items.irh`), whose description does state the bonus; the copy took the number and left the sentence (`lib/prestige.irh`) | **Observed** -- same seed and key script, the Spell Manager's power column reads `5d12+12` before and `5d12` after. Guarded by `tools/check_geomancy.sh` | no | inc-tek.21 |
| The Alienist's Outer Pathways charged none of the mana its own class description promises. The description states that each pseudonatural summoning "drains [Summoned Creature's CR x 2] mana, and this mana does not regenerate"; `ALIENIST_CLAUSE` (`lib/defines.irh`) had no mana clause at all | **Observed** -- same key script, an Alienist 5 casting Monster Summoning III at a CR 3 creature: mana 720 before, 720 after and still 720 fifteen thousand turns later on the old macro; 720, 714 and still 714 on the new one, so the six is held and does not regenerate. Guarded by `tools/check_alienist_drain.sh` | no | inc-tek.22 |
| The Assassin's Death Attack also demanded that the TARGET be out of combat. The class page attaches that condition to the assassin alone; the gate read `EActor->isFlatFooted() && EVictim->isFlatFooted()`, and `isFlatFooted()` is not the D&D condition but a per-creature out-of-combat counter (`inc/Creature.h`) that any strike zeroes for both fighters (`src/Fight.cpp`). So a target already fighting could never be assassinated -- not even unseen and from behind while the party held it (`lib/prestige.irh`) | **Observed** -- same seed and key script, two victims differing only in that field: a goblin (`FFCount` 20) is assassinated before the fix and after it, while a grippli, which Improved Initiative starts at `FFCount` 0 (`src/Monster.cpp`), reads "you deliver a killing blow, killing the grippli" before and "You assassinate the grippli!" after. Both blows report "Catching him unaware", so perception is not what differs. Guarded by (`tools/check_death_attack.sh`), proved red on the pre-fix module first | no | inc-tek.24 |
| The gravestone's epitaph read `REQUISCANT IN PACE,`, and there is no such Latin word. The phrase R.I.P. abbreviates is `requiescat in pace` -- singular -- and the plural is `requiescant`; the drawing dropped the `e` from either spelling. Singular is the right one, because `Player::Gravestone` writes one character's own name, class and killer into the same stone. `REQUISCANT` and `REQUIESCAT` are both ten characters, so no column of the drawing moves (`src/Main.cpp`) | **Observed** -- seed 11, `tools/keys/gravestone.keys`, `tools/gates/Options.Dat`, one word between the two builds: the dumped death screen reads `REQUISCANT IN PACE,` before and `REQUIESCAT IN PACE,` after, both starting at screen column 14 with the stone's two walls where they were, and `diff` of the two 48-line dumps reports that one line and nothing else. Guarded by (`tools/check_gravestone.sh`), proved red on the pre-fix binary first | no | inc-r6z |
| A monster's damage-equipment attack told the player "the orcish bastard sword +1 protects its orcish bastard sword +1". `ETarget` and `EVictim` are not two fields; they are one event slot read through two macros (`inc/Events.h:214-215`), so the assignment that aimed the `EV_DAMAGE` rethrow at the item -- which is intended -- also erased the item's owner from the slot the message reads. Both halves broke, not only the wording: `VPrint` gives its first-person string to `e.EVictim`, so that string went to the ITEM, whose `Thing::__IPrint` is an empty virtual, and the character fell into the onlooker branch and got the garbled third-person line about his own sword. Moving the assignment below the save is the whole fix; the save path returns before the rethrow, so the rethrow still receives the item (`src/Fight.cpp`) | **Observed** -- seed 5, `tools/keys/dequ-save-message.keys`, two binaries differing in the position of one line. An orc warrior strikes a wizard-summoned brown pudding (A_DEQU 5d6 AD_DCAY at DC 19, live in `lib/program.i` as `60 for 5d6 57 (DC 19)`) six times with the save forced by an env-gated probe, `-DDEQU_PROBE`. Before: 6 of 6 strike screens read "The orcish knife +1 protects its orcish knife +1." and 0 of 6 carried the first-person line. After: 6 of 6 and 0 of 6 the other way round. The same count settles the routing, because in a one-player session `VPrint` cannot put both strings on one screen. Guarded by `tools/check_dequ_save_message.sh`, proved red on the pre-fix binary first | no | inc-473 |
| Fifty-eight printf-style format strings disagreed with their arguments, and nothing could see them: `Format` is a plain `vsprintf` (`src/Base.cpp:467`) and `Error` a plain `vsnprintf`, both varargs, and `build_macos.sh` compiled with `-w`. **Only 38 of the 58 are claimed here.** Those 38 misbehave on Win32 exactly as they do on macOS: 26 calls pass arguments the string never consumes; 3 pass FEWER arguments than it consumes, so `vsprintf` prints whatever the register file holds next (`Error("Strange Portal xval (%d).")` with no argument at `src/Feature.cpp:423`, `" %d (%d) resources\n"` with one at `src/Main.cpp:1740`, and a trailing colour `%c` with none at `src/Prayer.cpp:1305-1308`); 3 are invalid specifiers -- the `%l` at `src/Magic.cpp:426`, which also mis-typed the three arguments after it, and two `ASSERT`s whose asserted expression contained a `%` and so became a conversion specifier, because the macro pasted `#a` INTO the format string rather than passing it as an argument; and 6 pass an argument of a type wrong on any platform (a `const char*` printed with `%d` twice in `src/Debug.cpp`, and `narg+1` -- pointer arithmetic on an `int*` -- printed with `%d` in `lang/Grammar.acc`, where the sibling call five lines up correctly writes `(*narg)+1`). **The other 20 are NOT upstream's and are not claimed**: 14 are upstream code that is merely non-portable to LP64 (a `long`, `size_t`, `intptr_t` or `ptrdiff_t` that was 32 bits on Win32), and 6 are OURS, created by this port narrowing `int32`/`uint32`/`hObj` from `long` to `int` (`inc/Defines.h:48-63`) under format strings that still say `%ld`. The `ASSERT` fix is at the macro, not the two call sites, so every future `ASSERT` is immune. The `format(printf,1,2)` attribute stays on `Format` so the next one is a compile-time warning (`inc/Base.h`, `inc/Defines.h`) | **Observed** -- the compiler is the oracle: 58 `-Wformat` warnings before, 0 after, from `WARN_FLAGS="-Wno-everything -Wformat" EXTRA_CXXFLAGS=-DFMTAUDIT BACKEND=posix ./build_macos.sh`. `-w` cannot be overridden by a later `-W` flag in clang, so `build_macos.sh` gained a `WARN_FLAGS` knob that replaces it and defaults to `-w`. No behaviour change: `tools/headless.sh tools/keys/dive.keys 3` under `INCURSION_OPTIONS=tools/gates/Options.Dat` reports the same 1972 turns and `errors: none` as the unmodified tree, and `tools/check_headless.sh` passes. Guarded by `tools/check_format_strings.sh` against `tools/format_strings.baseline`, proved red by removing the `-GREY` argument at `src/Prayer.cpp:1305-1308` and green again when it was restored | no | inc-9t6 |
| The gravestone was dated `sprintf(buff,"12th of Suntide")` -- a constant, so every character who ever died was buried on the same day whatever turn killed him, and the `'g'` case that draws it is on the path of every death. The real date was always reachable: `TextTerm::ShowTime` (`src/Term.cpp`) derives one from `theGame->GetTurn()`, but its `Months[]` table was a function-local array, so no second reader could share it. The table is now `MonthNames` in `src/Tables.cpp`, declared beside `EncumbranceNames` in `inc/Globals.h` as that file already does for every other name table, and the arithmetic moved with it into `GameDate(uint32 turn)` (`src/Term.cpp`, declared in `inc/Globals.h`) so that the two readers cannot drift apart. Sharing the arithmetic and not merely the table is the point: `Player::AddJournalEntry` (`src/Player.cpp:2704`) is a third copy that already has drifted -- it hardcodes the month and computes the ordinal suffix from the wrong variable, so it writes "11st" -- and it is left alone here as a separate defect. `GameDate` holds the day in a `uint32` where `ShowTime` held it in a `uint8`, which only differs after 256 in-game days. The drawing does not move: the date is centred by the same `23-length/2` rule the name, the class and the killer already use (`src/Main.cpp`) | **Observed** -- seed 11, `tools/keys/gravestone.keys`, `tools/gates/Options.Dat`, one build between the two columns. The character dies on turn 198067, which the screen dump's own header records; 198067/432000 is day 0, and day 0 is the 1st of Suntide. The dumped death screen reads `12th of Suntide` before and `1st of Suntide` after, and `diff` of the two 48-line dumps reports that one line and nothing else. Guarded by (`tools/check_gravestone.sh`), which derives the expected line from the turn in the dump rather than recording what a run printed, and which was proved red on the pre-fix binary first | no | inc-pg5 |
| The Wand of Cleansing Light rolled about four times the damage its own page promises. The page says "2d6 points of damage per magical plus the wand possesses"; the damage field asked for `LEVEL_2PER1`, twice the CASTING level, and for an item the casting level is the item's own level (`src/Magic.cpp:470`), which for this wand is three times its plus plus one (`src/Item.cpp:2210`, `Level: PLUS_3PER1_ADD1`, and the base wand carries `Level: 0` at `lib/mundane.irh`). So the plus was multiplied twice and the wand rolled `(6*plus+2)d6`. Two independent sources agree with the page: this was the ONLY `LEVEL_*` scaling in the whole of `lib/m_items.irh`, every other `pval` in the file scaling on the plus; and the item's own balance comment grades it `Damage +1`, the same grade Wand of Fire Bolts carries for exactly `(PLUS_2PER1)d6`. Changed to `PLUS_2PER1` (`lib/m_items.irh`) | **Observed** -- the game argues with itself on one screen, so the oracle needs no second build. The wand's inventory line calls `TEffect::Power(GetPlus(), ...)` and so passes the PLUS as the level (`src/Magic.cpp:135-157`, printed at `src/Message.cpp:1326-1327`), while the damage line at the target is the dice the engine rolled (`src/Effects.cpp:271-297`). On seed 1 with the same +2 wand in the same turn they read `[4d6+3]` and `14d6` before the fix, and `[4d6+3]` and `4d6` after it. `tools/check_wand_cleansing.sh`, confirmed red on a module built from the pre-fix line and green on the rebuilt one | no | inc-tek.8.8 |
| The Ring of Elemental Command (Fire) turned every wound its wearer took into a cold wound and multiplied it by 3/2. The ring's page curses the wearer with "150~ damage from all cold-based attacks (before resistance)", and its handler asked `if (e.DType = AD_COLD)` -- a single equals sign. That is an assignment, not a comparison: the grammar routes `=` to `CodeAssignment` (`src/yygram.cpp:9373`, `src/RComp.cpp:1014`) and the expression yields the value assigned, which is `AD_COLD` = 2 (`inc/Defines.h:987`) and never zero. So the ring wrote cold into the wound, tested what it had just written, and amplified a sword cut, a fall or a fireball alike. Changed to `==` (`lib/m_items.irh`) | **Observed** -- seed 1, the wearer shooting himself so no monster can write over the damage window. A wand of glass shards (`AD_SLASH`) read `4d10 = 28 Cold` before and `4d10 = 19 Slashing` after, and 28 is exactly `(19 * 3) / 2`. The control, a wand of ice, reads `2d8 = 15 Cold` on both builds -- it is what stops the fix from being "delete the block", which would drop that number to 10. `tools/check_ring_fire_curse.sh`, confirmed red on a module built from the pre-fix line and green on the rebuilt one | no | inc-tek.8.8 |
| The Ring of Elemental Command (Water) handed its wearer command of FIRE creatures. Two adjacent lines disagreed: the ring read the wearer's existing command of water, `GetStatiMag(COMMAND_ABILITY,MA_WATER)`, and then spent that measurement granting `COMMAND_ABILITY` against `MA_FIRE`. So the water command it measured was thrown away and the wearer walked off commanding fire, against a page that promises "the ability to command water creatures". The three sibling rings each read and write their own element, which is what makes this a copy-paste slip rather than a design. Changed the write to `MA_WATER` (`lib/m_items.irh`) | **Observed** -- seed 1, one word between the two modules. The character sheet's Special Abilities block prints one line per commandable type (`src/Sheet.cpp:492-499`), and an orc barbarian wearing the ring reads `Command Fire Creatures` before and `Command Water Creatures` after. `tools/check_ring_water_command.sh`, confirmed red on a module built from the pre-fix line and green on the rebuilt one | no | inc-tek.8.8 |
| `Item::MakeKnown` recorded that a KIND had been identified -- `EFFMEM(eID,player)->Known`, which is what makes the next item of that kind arrive already named -- only for a hand-written list of item TYPES: scroll, wand, ring, gauntlets, girdle, crown, helmet, tome, cloak, boots, rod, staff, and potion through `PKnown`. That list had drifted from the `Flavored` column of `DungeonItems` (`src/Tables.cpp`), which is what decides whether a kind has a flavour name to learn at all. Flavoured and missing from the list: amulets, bracers, lenses, mushrooms and horns -- so identifying one of those taught nothing, and every later item of the same kind had to be identified again. In the list and not flavoured: tomes and crowns. The reader already asks the right question -- `Item::Name` (`src/Message.cpp:1131` and `:1145`) prints the true name when the effect carries a flavour and that flavour is `Known` -- so the fix records `Known` under exactly that condition and deletes the list, leaving nothing that can drift again (`src/Item.cpp`) | **Observed** -- seed 1, `tools/keys/amulet-idtype.keys`, `src/Item.cpp` the only file between the two builds. An unidentified Periapt of Wisdom reads `? pearl amulet`; after Identify Whole Pack a second one of the SAME effect reads `? pearl amulet` before and `? Periapt of Wisdom` after. The ring control in the same run, by the same route, reads `? electrum ring` then `? Ring of Good Fortune` on BOTH builds, which is what proves the identify itself ran. `tools/check_item_type_id.sh`, confirmed red on the pre-fix build and green on the fixed one | no | inc-bj4z |
| The Ring of Good Fortune and the Boots of Providence each open their page with "When carried or worn, this item bestows good luck upon its wielder in the form of a magic bonus to his Luck score", and only worn paid. A grant is thrown from `Wield` (`src/Inv.cpp:396` and `:111`) and only for a slot `Item::activeSlot` calls active, and the one exemption from the worn-slot rule was for stones, which is how the Luckstone's "When carried" works. The wording is not copied from the Luckstone above them in `lib/m_items.irh`: the Luckstone says "When carried" and somebody deliberately added "or worn" to these two. Fixed with an effect flag, `EF_CARRIED`, declared on those two effects and read by `activeSlot`, which now calls the five belt slots active for a flagged item. Belt slots and not every slot, because `Monster::InSlot` (`src/Inv.cpp:48`) answers "what is in my armour slot?" through `activeSlot`, and a wider test would let a monster's boots answer as its armour (`inc/Defines.h`, `inc/ConstNam.h`, `lib/m_items.irh`, `src/Item.cpp`) | **Observed** -- seed 1, `tools/keys/boots-providence.keys`, the two builds sharing one module so that only `src/Item.cpp` differs. Boots +3 of Providence in a belt slot read `LUC: 15/00  (base 15)` before and `LUC: 18/00  (base 15, +3 magic)` after; on the feet both builds read `+3 magic`, which is what proves the item itself works. The same run reads base again once the boots are dropped, and a control pair of Boots of the Winterlands, which promise nothing about being carried, stay inert on the belt on both builds. `tools/check_boots_providence.sh`, confirmed red on the pre-fix build and green on the fixed one | no | inc-izuu |
| All four Rings of Elemental Command granted a weaker command power than their own pages promise. Each page says the ring lets the wearer "command <element> creatures as an <Element> priest of 12th level, or augments their existing ability by +6 -- whichever is superior" (`lib/m_items.irh:4743`, `:4831`, `:4908`, `:5013`), and all four then granted `max(10,clev+5)` -- 10th level and +5. Neither pair of numbers has a published rule behind it: the 3.5 SRD ring grants no command ability at all, and the 1e ring's "12th level" and "+6" belong to its spell caster level and to flat weapon damage. The numbers are the author's own, so the page is the copy of them the player reads, and the grants now say `max(12,clev+6)` (`lib/m_items.irh`) | **Observed** -- seed 1, one token per ring between the two modules. The number is an effective priest turning level, which `src/Skills.cpp:4459-4562` adds to a d20 and to four other bonuses before dividing by resistance, so it shifts a probability and crosses no visible threshold; wizard mode's "Examine Player Data" prints it outright instead (`src/Debug.cpp:1528-1584`). A 1st-level orc barbarian wearing the rings two at a time reads `COMMAND ABILITY from SS ITEM (Val:92 Mag:10)` and `(Val:95 Mag:10)`, then `(Val:93 Mag:10)` and `(Val:94 Mag:10)` before -- Water, Fire, Air, Earth by `MA_WATER` 92, `MA_AIR` 93, `MA_EARTH` 94, `MA_FIRE` 95 (`inc/Defines.h:1610-1613`) -- and `Mag:12` on all four after. `tools/check_ring_command_level.sh`, confirmed red on a module built from the pre-fix lines, green on the rebuilt one, and red again with the Earth line alone put back | no | inc-tek.8.8 |
| The Staff of Winter's page promises four powers and the entity delivered one. Its Desc (`lib/m_items.irh:3588`) says the staff "grants such a worthy wielder a +2 enhancement bonus to Charisma and a +4 enhancement bonus to Intimidate, but numbs her ability to understand the feelings of others, granting a -6 penalty to the Appraise skill". The entity held exactly one effect block -- the `STAFF_SPELLS` grant of its six cold spells -- and closed with no `and EA_GRANT` continuation, so three of the four sentences described powers no script ever gave. The page is what the player reads, so the page wins: three continuation blocks now carry the three missing numbers. They are the page's LITERAL figures and are deliberately not scaled on the item's plus with `PLUS_2PER1`, which is what most of this file's skill boosters use -- the page states flat numbers, and a scaled grant would make it true only at the staff's `INITIAL_PLUS` of +2 and false at every plus above it (`lib/m_items.irh`) | **Observed** -- seed 1, three blocks of module content between the two builds. The staff gates itself: its `EV_MAGIC_HIT` handler returns `DONE`, which swallows the event before `Magic::Grant` runs, whenever `ItemPrereq(CLEV_VAL + 255,3,20)` fails, and that handler belongs to the whole effect resource rather than to one block of it (`src/Event.cpp:248-259` dispatches once per `ReThrow` by `e.eID`), so the three new grants sit behind the same caster-level-three door as the spells. The session therefore builds a Mage 3 through wizard mode's Free Advancement switch and refuses to report at all unless the run's own `STAFF SPELLS from SS ITEM (Val:8 Mag:3) [CL 3]` stati proves the door opened. With the staff in the weapon hand, wizard mode's "Examine Player Data" (`src/Debug.cpp:1524-1590`) printed nothing for Charisma, Intimidate or Appraise before and printed `ADJUST from SS ITEM (Val:A CHA Mag:2)`, `SKILL BONUS from SS ITEM (Val:SK INTIMIDATE Mag:4)` and `SKILL BONUS from SS ITEM (Val:SK APPRAISE Mag:-6)` after; the character sheet moved with it, `CHA: 14/00 [+2/+1] (base 16, -2 natural)` -> `CHA: 16/00 [+3/+2] (base 16, +2 magic, -2 natural)` and `Intimidate +4 (0 ranks, +2 STR, +2 train)` -> `Intimidate +8 (0 ranks, +2 CHA, +2 train, +4 magic)`. `tools/check_staff_winter_grants.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the Intimidate block alone taken back out. **The -6 reaches the character but not the number**: `Creature::SkillLevel` collects an item's skill bonus with `s_item = max(s_item,S->Mag)` from a zero start (`src/Create.cpp:4022`, `src/Create.cpp:4199`), so any NEGATIVE item skill bonus loses to the zero and never reaches the total -- Appraise still reads `+6 (2 ranks, +2 WIS, +2 train)` on the sheet. That is a separate engine defect, older than this fix and shared with the Staff of the Goblin Queen, whose page promises three -4 penalties by the same means (`lib/m_items.irh:3237-3239`); it is tracked as inc-e7wp and is NOT fixed here | no | inc-tek.8.8 |
| The Staff of Winter is a quarterstaff of NUMBING in its script and a quarterstaff of WEAKENING on its own page. The Desc says "A Staff of Winter acts as a `<14>+2 quarterstaff of weakening<7>`" while the entity's `ITEM_QUALITIES` list said `WQ_NUMBING`. The two are not the same power: weakening inflicts 1d6 Strength ability damage and numbing inflicts 1d6 Dexterity ability damage (`src/Tables.cpp:1831-1832`), so the player was promised one ability score and given another. Here the SCRIPT is the slip and the page wins by evidence rather than by the partition's usual rule: `WQ_WEAKENING` and `WQ_NUMBING` are ADJACENT entries in one table (`inc/Defines.h:2161-2162` numbers them 37 and 38 under a comment that their order must be kept), and this partition has already produced one confirmed slip of exactly that shape -- the Ring of Water Elemental Command took `MA_FIRE` from the line beside `MA_WATER`. A thematic reading, that numbing suits an ice staff, was raised and rejected as an inference rather than evidence. Changed the one token to `WQ_WEAKENING` (`lib/m_items.irh`) | **Observed** -- seed 1, one token of module content between the two builds. The oracle is the item description screen (Inventory Mode, `x`, `src/Managers.cpp:789-792`), which prints the entity's page and the item's weapon qualities in one box, so both halves of the contradiction land on one screen. Before: "A Staff of Winter acts as a +2 quarterstaff of weakening" above "Numbing: A weapon of numbing inflicts 1d6 points of Dexterity ability damage with every successful hit (save DC 10+7 for half)". After: the same page sentence above "Weakening: A weapon of weakening inflicts 1d6 points of Strength ability damage with every successful hit (save DC 10+7 for half)". The item's NAME could not have shown this: the entity carries `EF_HIDEQUAL`, which suppresses the quality word in `Item::xName` and nowhere else (`src/Message.cpp:1241`), so the staff is called "Staff +2 of Winter" on both builds, while `QItem::Describe` (`src/Help.cpp:3508-3524`) carries no such test. `tools/check_staff_winter_quality.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with `WQ_NUMBING` put back | no | inc-tek.8.8 |
| The Staff of the Abyss advertised twelve spells and granted nine. Its page named `cacodaemon`, `death knell` and `ensnarement` alongside the nine its `STAFF_SPELL_LIST` actually carries. Here the PROSE loses, against this partition's usual rule, because the three names have nothing behind them: ensnarement has no definition anywhere in `lib/`; cacodemon has none either, being commented out at `lib/sp_books.irh:504` and again inside the Evil domain's commented block at `lib/domains.irh:266`; and Death Knell exists only as a stub, `lib/pspells.irh:1830` reading `Effect "Death Knell" : EA_NOTIMP { }`. Honouring the page would mean writing two spells from nothing and finishing a third that upstream itself marked not-implemented -- a feature, not a prose fix, tracked as inc-tek.26, with the live-defect half (classes and monsters that still hand out the Death Knell stub) as inc-tek.27. The three names came off the page with their `<9>`/`<7>` colour codes and the list's punctuation was repaired; the script, including upstream's own commented-out `$"cacodemon"` and `$"ensnarement"`, is untouched (`lib/m_items.irh`) | **Observed** -- seed 1. This change moves no behaviour, so no behavioural check is possible and none was faked. The oracle is the page as the GAME renders it, not as the file stores it: the item description screen, reached from Inventory Mode with `x` (`src/Managers.cpp:789-792`), which prints the entity's Desc through `Item::Describe` (`src/Help.cpp:3405-3408`). The check counts the names the game printed. Before: "bestow curse, burning blood, cacodaemon, contagion, death knell, deeper darkness, desecrate, dire charm, enervation, ensnarement, unholy blight and wall of fire" -- twelve. After: "bestow curse, burning blood, contagion, deeper darkness, desecrate, dire charm, enervation, unholy blight and wall of fire" -- nine, and the check compares the full list rather than the count, so nine of the WRONG spells could not pass. `tools/check_staff_abyss_spell_list.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the three names put back | no | inc-tek.8.8 |
| The Staff of the Abyss worked perfectly in a good character's hands. Its page says "A staff of the Abyss is an inert stick in the hands of any good character, or any character with less than five levels as an arcane or divine spellcaster", and its `PRE(EV_MAGIC_HIT)` handler tested the caster level and nothing else. So a GOOD fifth-level caster got the whole item: the nine-spell list, the +2 vampiric quarterstaff of terror and the three-per-day vrock. The page's druid clause needed nothing -- druids cast with `SP_PRIMAL`, which the caster-level test does not name, so they were already excluded. Added `if (EActor->isMType(MA_GOOD)) return DONE;` ahead of the caster-level test (`lib/m_items.irh`). **The obvious mechanism is dead code and was NOT used**: the sibling Staff of Exorcism gates itself with `!EActor->ItemPrereq(ALIGN_VAL + MA_GOOD,0,20)`, and `Creature::ItemPrereq` (`src/Skills.cpp:5679-5761`) has branches for `ABIL_VAL`, `FEAT_VAL`, `SKILL_VAL`, `MFLAG_VAL`, `MTYPE_VAL`, `ATTR_VAL` and `CLEV_VAL` and none at all for `ALIGN_VAL`; `ALIGN_VAL` is 9000 (`inc/Defines.h:2096`), so the argument is 9106, below the `0xFFFF` that sends a value down the resource-id path, and the function reaches its closing `return false`. The call is false for every creature, so the Staff of Exorcism returns `DONE` for every creature and is inert in every hand -- a separate live defect, tracked as inc-tek.29 and NOT fixed here | **Observed** -- seed 1, one line of module content between the builds, and TWO sessions, because an absence proves nothing on its own and an inverted gate would leave the good character exactly as empty-handed as a correct fix while locking out the evil characters the item exists for. Nothing in wizard mode can change an alignment after generation (`src/Debug.cpp:534-572`), so the two halves are two characters who differ in alignment and in nothing else: a Neutral Good orc Mage 5 and a Neutral orc Mage 5, both raised through the Free Advancement switch, both holding the staff in the weapon hand. The oracle is wizard mode's "Examine Player Data" (`src/Debug.cpp:1524-1590`). Before: BOTH characters carried `STAFF SPELLS from SS ITEM (Val:0 Mag:0) [CL 5]`. After: the Neutral Good character carries no such stati and the Neutral one still carries it. The check refuses to report unless each session's own sheet says `Mage 5` and says the alignment that half was built for, and unless the Weapon Hand row names a `Staff +2 of the Abyss`. The Exorcism line was tried here FIRST and measured: with `ItemPrereq(ALIGN_VAL + MA_GOOD,0,20)` in place the good character still held the grant, which is why that claim is Observed and not Reasoned. `tools/check_staff_abyss_alignment.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the line taken back out | no | inc-tek.8.8 |
| The Ring of Item Preservation's page promised a mechanic that is compiled out. It said "upon the death of the wearer, or if the wearer is rendered unconscious or paralyzed, they transport any items the wearer carries into a specially prepared extradimensional space", and that "these items can later be retrieved by the merchant guild or family the merchant works for using powerful magical rituals". The whole `META(POST(EVICTIM(EV_DEATH)))` handler that would have done it is inside `#if 0`. **The ring is NOT inert, which is why the page was corrected and the `#if 0` was left alone**: the registration line `xval: TRAP_EVENT; yval: `POST(EVICTIM(EV_DEATH))` sits OUTSIDE it, and two live behaviours read it -- Apportation, the wizard spell that teleports one item out of a creature's inventory, refuses against a wearer via `HasEffStati(TRAP_EVENT,$"Item Preservation")` and prints "The <hObj> flickers briefly." (`lib/wspells.irh:3518`), and dunking the ring in acid makes the acid and flask "vanish to another plane", which is the ring's identification clue (`lib/alchemy.irh:740`). The transport clause and the guild-retrieval sentence came off the page and one sentence took their place, naming the ward the ring really has. The "Thus" sentence that followed them was re-aimed rather than deleted -- its premise had gone, so it followed from nothing, and murder-and-loot is exactly what the ring does NOT prevent -- so it now names the theft the ring does stop, which keeps the deterrent theme. The second paragraph is gone as well, by a follow-up ruling on 2026-08-24 that every promise of death-time preservation leaves the page: it said the ring preserves a wearer's items "for their (hopeful) resurrection", which is the retention the compiled-out handler does not perform; it described guilds with "the means to recover the items sequestered by the ring's magic", a recovery route that does not exist; and it concluded the ring is of little use to a player character, which is false, because the Apportation ward protects whoever wears it. No replacement text was written, so the page ends on the deterrent sentence. The script, the `#if 0` and the registration line are untouched; finishing the feature is tracked as inc-tek.28 (`lib/m_items.irh`) | **Observed** -- seed 1. This change moves no behaviour, so no behavioural check is possible and none was faked. The oracle is the page as the GAME renders it, not as the file stores it: the item description screen, reached from Inventory Mode with `x` (`src/Managers.cpp:789-792`), which prints the entity's Desc through `Item::Describe` (`src/Help.cpp:3405-3408`). Before: "Their enchantment is simple -- upon the death of the wearer, or if the wearer is rendered unconscious or paralyzed, they transport any items the wearer carries into a specially prepared extradimensional space. These items can later be retrieved by the merchant guild or family the merchant works for using powerful magical rituals. Thus, they serve as powerful deterrants to those who would murder merchants and shop-keepers in order to loot the goods from their bodies." After: "Their enchantment is simple -- they hold the wearer's possessions fast against any magic that would spirit them away, so that a spell reaching into his pack closes on nothing. Thus, they serve as powerful deterrants to those who would rob merchants and shop-keepers of the goods they carry." The check refuses to report unless the description box's own title names a Ring of Item Preservation -- the row above it in the acquisition list is the Ring of Elemental Command (Water), and describing that instead would pass every "no longer says X" test for the wrong reason -- and unless the box carries the entity's own page, which an unidentified ring would not. `tools/check_ring_preservation_page.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the old page put back | no | inc-tek.8.8 |
| The Luckblade took an irreversible payment and then refused the service. Its `EV_MAGIC_HIT` activation handler spent one of the sword's magical plusses with `SetInherentPlus(GetInherentPlus() - 1)`, patched the wielder's `ADJUST` and `ADJUST_LUCK` stati to match, and only THEN reached the wish redirect -- which its own author had commented out -- and printed "Wishes aren't implemented yet; sorry." `INITIAL_PLUS` is `+3`, so three activations ground a +3 keen short sword down to +0, and the sword's own page says that loss "cannot be restored in any manner". Brian's ruling was to refuse before charging: the apology and its `return DONE` now stand immediately after the `if (!e.isActivation)` test, ahead of every state change. **The author's original code is left below it, whole and in order, and is unreachable**; a comment says so and names inc-tek.30, so whoever implements wish deletes the early return and inherits working logic. The Desc is deliberately untouched -- the cost it describes is a correct design and only the payoff is missing, and the game now says so before charging. There is no wish entity anywhere in `lib/`: the commented-out `$"wish"` in this handler is the only occurrence in the whole ruleset (`lib/m_items.irh`) | **Observed** -- seed 1, two lines of module content between the builds. The oracle is the Activate command's own menu, which is the screen a player reads before choosing what to activate: `Item::xName` writes the plus onto the item's name for any known item (`src/Message.cpp:1300-1308`), and writes no plus at all once `GetPlus()` reaches zero. A wielded Luckblade activated three times read `[a] mildly damaged keen Luckblade +3`, then `+2`, then `+1`, then `mildly damaged keen Luckblade` with no plus before the fix, and `+3` on all four readings after it, with "Wishes aren't implemented yet; sorry." printed on all three activations either way. The check refuses to report unless that message appears on all three, because a run in which the activation never fired would show a stable plus for the wrong reason. **The LUC score on the sidebar is NOT a usable oracle and was not used**: it read 18, 17, 17, 17 across the same four measurements on the pre-fix binary, because the handler's own "awkward kludge" comment marks a stati patch that only takes the first time. `tools/check_luckblade_plus.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the early return taken back out. **This stops the bleeding and does not refund**: the "There are no wishes left." branch is now unreachable for any sword not already drained, and a sword a player has already ruined stays ruined | no | inc-tek.8.8 |
| The flame tongue sword never set a corporeal undead alight. Its page promises that when the sword "strikes a corporeal undead, that creature's dry, withered flesh catches flame and continues to burn, causing it to suffer 3d6 fire damage the first turn after the hit, 2d6 the second turn and 1d6 on the third", and the handler meant to do it was an empty stub -- one comment, `/* Immolation damage vs. undead */`, and `return NOTHING;`. So the sentence was false in every game ever played, and the sword's most distinctive power was the one it did not have. The handler now applies a `TRAP_EVENT` stati to the victim and a new sibling resource, `$"flame tongue immolation"`, carries the `META(EV_TURN)` body that spends the three turns of dice. **The burn is a separate resource on purpose**: a `TRAP_EVENT` stati names the effect whose META handler the dispatch calls (`src/Event.cpp`, the `StatiIterNature(t,TRAP_EVENT)` loop), and giving that name to the sword's own multi-block `EA_GRANT` entity would aim the wielder's grants at it. The idiom is the Wand of Acid's, four thousand lines up in the same file, which applies its own residual burn the same way; the counter lives in the stati's `Val`, which for `TRAP_EVENT` is the `Mag` argument, because `Thing::GainTempStati` swaps the two and says so (`src/Status.cpp`). "Corporeal undead" is the game's own category -- it names the summoning list -- and is tested as `M_UNDEAD` and not `M_INCOR`, so a wraith has no dry flesh to light. A second blow restarts the three turns rather than stacking a second burn, because `GainTempStati` merges only for `SS_ENCH` and two stati would tick two handlers a turn. The Desc is untouched: the page was right and the script was missing (`lib/m_items.irh`) | **Observed** -- seed 1, one handler and one resource of module content between the two builds, and a subject that cannot fight back. A mummy is summoned beside a first level orc barbarian holding a wizard-mode Long Sword, Flame Tongue +2, with the Freeze Monsters switch on so that it stands still for the eighty game turns the measurement takes. The oracle is `Creature::Dump`'s `HP:cur/max` line, reached through wizard mode's Examine Nearby Things (`src/Debug.cpp`), which is the only place in the game that shows a monster's hit points as a number; the browser spends no game time, so a reading never disturbs what it reads. Five readings, one per game turn, each wait being exactly 21 Searches because a Search costs 3 ticks and `EV_TURN` is thrown once every 60 (`src/Main.cpp`), so 63 ticks cross exactly one boundary whatever the phase. Before: `73, 73, 73, 73, 73` -- the mummy did not lose one hit point in four turns. After: `61, 45, 38, 38, 38`, with the ignition line *"The mummy's withered flesh catches fire!"* and the first turn's *"The mummy's withered flesh burns!"* both on the first screen, because an attack costs about ten ticks and the world runs past a turn boundary before the keyboard comes back. The check counts that first turn from its own message rather than from the numbers, so it asserts three turns of burning and not a range, and it bounds each turn at 27 and the whole burn at 54 -- 3d6 and 6d6 against a mummy's 1.5x fire vulnerability (`lib/mon3.irh`). `tools/check_flame_tongue_undead.sh`, confirmed red on the pre-fix module, green on the rebuilt one, and red again with the handler put back to its stub | no | inc-tek.8.8 |
| The Robe of Eyes granted no infravision. Its page says it "grants the wearer 60 feet of Infravison"; the entity's five grant blocks gave Spot +5, Search +5, See Invisible, Improved Initiative and a gaze-attack handler, and nothing else, so that clause was false for every wearer since the item was written. Every other promise on the page holds. Fixed by adding the grant block the page already describes, in the file's own idiom for 60 feet -- `EXTRA_ABILITY / CA_INFRAVISION / pval: +6`, which is what "Eyes of the Night" uses one thousand lines earlier and explains in its own Desc (`lib/m_items.irh`) | **Observed** -- seed 1, `tools/keys/robe-eyes.keys`, `lib/m_items.irh` the only file between the two builds. The oracle is the "Special Abilities" block of the [W]rite Dump character dump, which prints `Creature::AbilityLevel(CA_INFRAVISION) * 10` (`src/Sheet.cpp:538`). An orc, who owns 60 ft of his own, read 60 / 60 / 60 ft before the fix over the three dumps -- no robe, robe worn, robe removed -- and 60 / 120 / 60 ft after it. The same run reads `See Invisibility` in that block only while the robe is worn on BOTH builds, which is what proves the robe's other grants were applying all along. `tools/check_robe_eyes.sh`, confirmed red on the pre-fix build and green on the fixed one | no | inc-tek.8.8 |
| The Springblade Bracers charged nothing for either of the two things their page sells. The page says snapping the blades out "does require a Handle Device check (DC 15)", and the `EV_MAGIC_HIT` handler contained no `SkillCheck` call, so deployment never failed. The page also sells a free off-guard strike on "the first attack made with a springblade within a single round of their deployment", and every deployment re-armed the `COUNTER` that grants it, with nothing tying it to a fight at all -- so the free strike was payable again and again inside one melee, which for a rogue is sneak attack damage before every blow. Both halves are shared game data and misbehave identically on Win32. Added the DC 15 `SK_HANDLE_DEV` check, with a failure costing 50, the engine's own value for "a full round action, in the base system" (`src/Fight.cpp:2531`). For the second half the rule is now one opening per fight, in combat or out of it: a `COUNTER` stati on the bracers marks a fight that has already yielded its opening, and `Creature::isFlatFooted()` -- the engine's per-creature out-of-combat counter `FFCount > min(5,10+Mod(A_WIS))` (`inc/Creature.h:571-572`), zeroed for both fighters by any strike (`src/Fight.cpp:3258-3259`), the same field `lib/prestige.irh` reads for the Assassin's Death Attack -- does not grant the opening but clears that mark. The page gained one clause for each half. Two things deliberately NOT fixed here: retraction is free because `Player::CancelMenu` (`src/Skills.cpp:288`) and its caller (`src/Player.cpp:1143-1145`) never set `Timeout` for any cancellable ability, which is engine behaviour and not an item defect; and no second deployment is reachable at all, so the mark cannot yet bind -- `ALT_ITEM` exists only to serve this item (`inc/Defines.h:3056`), carries a negative duration the stati clock never decrements (`src/Status.cpp:50-62`), is removed nowhere in `src/`, is skipped by `Player::CancelMenu` because the blades belong to no inventory (`src/Skills.cpp:312-315`), and survives dropping the bracers (`lib/m_items.irh`) | **Observed** -- seed 2, three scripted runs per build, on builds differing in nothing else. Against the original: eight deployments in a row, none failed, turn stamp 198046 -> 198046; against a first, wrong rule that armed the opening only while the wielder was out of combat, a first deployment made mid-fight bought nothing. After: the first attempt jams and costs 51 turns, the deployment that follows costs 0, and a first deployment made mid-fight buys the opening, on a blow that killed the goblin. The control -- blades deployed before the fight -- fires on every build. The rule's second half (a SECOND deployment in one fight arms nothing) is stated and not guarded, because no second deployment can be produced in a running game. Guarded by (`tools/check_springblade.sh`) | no | inc-tek.8.8 |
| `Item "springblade"` carried `Group: WG_SBLADES` and nothing else. It is a short sword in every other respect -- same material, size, weight, cost, parry, damage, accuracy and critical, and a better threat range -- and `Item "short sword"` carries `Group: WG_SBLADES | WG_LIGHT` (`lib/weapons.irh:401`). `Creature::CalcValues` charges an extra -2 to the main hand and -2 to the off hand when the off-hand weapon is neither light nor smaller than the main-hand weapon (`src/Values.cpp:1345-1352`), and the Springblade Bracers deploy two springblades of the same size into the two hands, so the wielder of the pair the item exists to give him paid a two-weapon penalty a pair of short swords does not. Weapon groups are shared game data, so the same weapon is mis-tagged on Win32. `WG_LIGHT` added (`lib/m_items.irh`) | **Observed** -- seed 2, an orc rogue carrying Two-Weapon Style so that both blades deploy, on builds differing in nothing else: the sidebar's own `Hit:` line read `4 / 2` with both blades out before the tag and `6 / 4` after it, the +2 to each hand that waiving the clause is worth. Guarded by (`tools/check_springblade.sh`) | no | inc-tek.8.8 |
| Eyes of the Eagle named Superior Senses and claimed doubled vision even though the item grants Acute Senses, whose text and calculation both give a 50% increase. Corrected that sentence and its `you` typo (`lib/m_items.irh`) | **Traced** -- the grant, feat text and range calculation agree; this change is text only and carries no runtime effect | no | inc-tek.8.8 |
| The Springblade Bracers page claimed every pair was two +2 keen blades, but birth rolls one of six different loadouts and none matches that claim. Replaced the claim with a note that the item's name carries the rolled details (`lib/m_items.irh`) | **Traced** -- the six birth cases and six name suffixes were read together; this change is text only and carries no runtime effect | no | inc-tek.8.8 |
| The Springblade Bracers type-3 name said `+3 flame/+2 frost` although birth gives both elemental blades inherent plus 2. Changed only the label to `+2 flame/+2 frost`; neither blade's plus moved (`lib/m_items.irh`) | **Observed** -- seed 6, the activation menu printed the incorrect label before and the corrected label after; guarded by (`tools/check_springblade_label.sh`) | no | inc-tek.8.8 |
| The Rod of Lordly Might's paralyzing-touch option granted seven touches while its own page promises three; its `7` was copied from the life-transfer option immediately below, whose page does promise seven. The counter is shared game data read identically on Win32, so the same overlong effect exists upstream. Changed `7` to `3` only for `$"paralysis;RoLM"` (`lib/m_items.irh`) | **Observed** -- the landed-touch path's real `TOUCH_ATTACK` magnitude counted down 7 -> 0 before and 3 -> 0 after; guarded by (`tools/check_rod_lordly_might.sh`) | no | inc-tek.8.8 |
| The Rod of Lordly Might's flaming-long-sword option called itself +1 in both player-facing labels but set the weapon's inherent plus to +2. The transform is shared game data read identically on Win32, so upstream gives the same mislabeled bonus. Changed only that form's inherent plus from 2 to 1 (`lib/m_items.irh`) | **Observed** -- same seed and key script; the transformed weapon's sidebar read `Hit:9`, `Dmg:1d8+6` before and `Hit:8`, `Dmg:1d8+5` after; guarded by (`tools/check_rod_longsword_plus.sh`) | no | inc-tek.8.8 |
| The Horn of Madness struck its own blower with both Wisdom drain and stun because its shared effect entity lacked caster immunity, while its page incorrectly called the scripted `AD_DAWI` Wisdom drain Intelligence damage. Added `EF_CASTER_IMMUNE` and, on the owner's 2026-08-25 ruling that madness is Wisdom, corrected that one word from Intelligence to Wisdom (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before Item Acquisition: a goblin's Wisdom fell 9 -> 8 and it gained `STUNNED`, while the blower remained Wisdom 16 -> 16 and was not stunned. Guarded by (`tools/check_horn_madness.sh`), confirmed green with both activation flags and red after mutating only `EF_ACTIVATE2` away | no | inc-tek.8.8 |
| The Horn of Madness carried no activation flags, so `Item::isActivatable()` omitted the identified horn from ordinary activation menus and, after component 0 gained `EF_ACTIVATE`, component 1 was still skipped. Added `EF_ACTIVATE` for the Wisdom-drain component and `EF_ACTIVATE2` for the stun component in its shared effect data (`lib/m_items.irh`) | **Observed** -- seed 1: the goblin's Wisdom fell 9 -> 8 and it gained `STUNNED`. With only `EF_ACTIVATE2` mutated away, Wisdom still fell 9 -> 8 but `stunned: 0`, proving the check defends the formerly missing half. Guarded by (`tools/check_horn_madness.sh`) | no | inc-j4dy |
| Bracers of Killing Hands granted one point per magical plus to unarmed hit rolls and damage although their own page promises two points per plus. Both `ADJUST` grants used `pval: PLUS_1PER1`; changed only the `A_HIT_BRAWL` and `A_DMG_BRAWL` rates to `PLUS_2PER1`. The shared effect data is read identically on Win32 with the original typedefs and upstream compiler (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring known Bracers +2 of Killing Hands. The character sheet's Brawl block read `+2 magic` for both +toHit and Damage before, and `+4 magic` for both after. The equipped inventory row proved the item remained +2. Guarded by (`tools/check_killing_hands.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| Enchant Armour (graceful) advertised that it bestowed the featherlight quality, a verbatim copy of the preceding Enchant Armour (featherlight) Desc, while its script both refuses `AQ_GRACEFUL` repeats and grants `AQ_GRACEFUL`. Changed only that one word in the graceful scroll's shared Desc; Win32 with the original typedefs and upstream compiler reads the same mislabeled page (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring both scrolls in one session. The graceful scroll's own item-description screen read `bestow the featherlight quality` before and `bestow the graceful quality` after. The featherlight control's own screen read `bestow the featherlight quality` on both modules. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| Enchant Armour (life-keeping) advertised that it bestowed the featherlight quality, the last verbatim copy of the Enchant Armour (featherlight) Desc, while its script both refuses `AQ_LIFEKEEPING` repeats and grants `AQ_LIFEKEEPING`. Changed only that one word in the life-keeping scroll's shared Desc; Win32 with the original typedefs and upstream compiler reads the same mislabeled page (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring all three scrolls in one session. The life-keeping scroll's own item-description screen read `bestow the featherlight quality` before and `bestow the life-keeping quality` after. The graceful scroll's own screen read `bestow the graceful quality` and the featherlight control's own screen read `bestow the featherlight quality` on both modules. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Staff of the Goblin Queen advertised that its thirteen charms could be used by a goblinoid spellcaster of 1st level or above, while its unchanged `ItemPrereq(CLEV_VAL + 255,3,10)` gate requires 3rd level. Changed only `1st` to `3rd` in the shared Desc; Win32 with the original typedefs and upstream compiler reads the same understated page (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring the staff in the same session as three scroll-page checks. The staff's own item-description screen read `1st level or above` before and `3rd level or above` after. The graceful, featherlight and life-keeping scroll pages continued to read `bestow the graceful quality`, `bestow the featherlight quality` and `bestow the life-keeping quality`. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Staff of Exorcism's unchanged `STAFF_SPELL_LIST` grants eight spells, but its Desc omitted `magic circle vs. evil`; Win32 with the original typedefs and upstream compiler reads the same incomplete page. Added only the missing tagged spell name in list order (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring the staff as the fifth page in one session. Before, the staff's own item-description screen read `aura of abjuration, bless, detect evil, dispel evil, dispel magic, remove curse and spiritwrack`; after it read `aura of abjuration, bless, detect evil, dispel evil, dispel magic, magic circle vs. evil, remove curse and spiritwrack`. The other four pages continued to read `bestow the graceful quality`, `bestow the featherlight quality`, `bestow the life-keeping quality` and `3rd level or above`. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Staff of Abjuration's unchanged `ItemPrereq(CLEV_VAL + SP_DIVINE + SP_ARCANE,3,20)` gate accepts a third-level divine or arcane caster, but its Desc said only `any character with three or more arcane casting levels`; Win32 with the original typedefs and upstream compiler reads the same contradictory page. Changed the sentence to say `any character with three or more levels as a mage or priest`, and repaired its two grammar errors, `If the hands of` and `the a +2 magic bonus` (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring the staff as the sixth page in one session. Before, the staff's own item-description screen printed `If the hands of any character with three or more arcane casting levels, it grants the a +2 magic bonus`; after it printed `In the hands of any character with three or more levels as a mage or priest, it grants a +2 magic bonus`. The other five pages continued to advertise their own qualities, caster-level gate or spells. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Staff of Wind and Water's unchanged `ADJUST_CIRC` grant gives a circumstance bonus to Luck, but its Desc called that bonus enhancement; Win32 with the original typedefs and upstream compiler reads the same mislabeled page. Changed only the bonus-type word in the shared Desc; the grant did not move (`lib/m_items.irh`) | **Observed** -- seed 1, wizard Auto-Identify enabled before acquiring the staff as the seventh page in one session. Before, the staff's own item-description screen printed `+2 enhancement bonus to Luck`; after it printed `+2 circumstance bonus to Luck`. The other six pages continued to advertise their own qualities, caster-level gates or spells. Guarded by (`tools/check_enchant_graceful.sh`), confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Horn of Panic carried no activation flag, so the identified horn could not be blown and its promised fear effect was unreachable. Added `EF_ACTIVATE` to its single shared effect component (`lib/m_items.irh`) | **Observed** -- seed 4: the ordinary activation produced a goblin failed Will save and `AFRAID`, while the blower was not afraid. With `EF_ACTIVATE` mutated away, the check printed `goblin failed save: 0; afraid: 0` and failed. Guarded by (`tools/check_horn_panic.sh`) | no | inc-j4dy |
| `TargetSystem::Serialize` is empty, so a `Creature`'s whole target array -- who it fights, who it follows, and why -- is written by nothing. It survived only because `Registry::SaveGroup` dumps the embedding `Creature` as raw bytes, which the empty body then never contradicts. The v0 body is left alone; the v1 schema gains the real field list, `TargetSystem::FieldsV1` (`src/Target.cpp`) | **Traced** -- read `Registry::SaveGroup` and the empty body together, as `docs/ENGINE-SERIALISATION.md` ("Whatever a `Serialize` body omits") records. Guarded by (`tools/check_schema_roundtrip.sh`), whose `creature` group round-trips three live targets | no | inc-mdi6 |
| `Player::TouchGallery` and `Player::DisableReincarnation` index `ri[GallerySlot]` -- an array sized from `gallery.dat` -- and WRITE through it, guarded only against `GallerySlot` being negative. `GallerySlot` is restored from the save file, so a save naming a slot past the gallery's length writes past the end of a heap block. The only upper guard either site had was a `DEBUG`-only `ASSERT(sz >= GallerySlot)`, off by one, in the second of them (`src/Create.cpp`) | **Traced** -- read out of the code while auditing every file-fed index for the v1 save schema; never seen to fire. The sibling load-time bounds it was found beside are guarded by (`tools/check_v1_adversarial.sh`) | no | inc-3c6x |

| The Ring of Fire Resistance promised that its wearer "(and her mount)" could cross fiery terrain safely, but neither fiery terrain named the ring. A +5 ring grants only 15 fire resistance, below magma's 36-point admission threshold, so its page's final clause was unreachable. `magma` and `brimstone` now exempt `$"Fire Resistance"` everywhere they exempt `$"the Inferno"`: the mounted branches test both mount and rider, and the unmounted branches test the actor, in both `EV_MON_CONSIDER` and the movement/damage handler. The 36 threshold, the ring's `pval`, and its Desc are untouched. The fix lands in `lib/dungeon.irh`, which belongs to inc-tek.8.12; Brian approved that reach on 2026-08-25 (`lib/dungeon.irh`) | **Observed** -- seed 1, wizard mode changed the square immediately east of the same orc barbarian to magma. Before, a worn Ring +5 of Fire Resistance left the character at screen column 21 and raised `Confirm enter the magma?`; after, the wearer moved 21 -> 22 with no refusal. The no-ring control stayed 21 -> 21 and raised the refusal on both builds. `tools/check_ring_fire_terrain.sh`, confirmed red on the unfixed module and green on the rebuilt one | no | inc-tek.8.8 |
| The Sunblade's page promises "the accuracy modifier and crit multiplier of a bastard sword", but its item definition gave Acc +3 / Crit x3 while a bastard sword is Acc +2 / Crit x2; Win32 reads the same stat data. Corrected the item to Acc +2 / Crit x2, leaving damage and the short-sword speed unchanged (`lib/m_items.irh`). | **Observed** -- the disagreement is present directly in the stat line and the bastard sword's own stats; guarded structurally by `tools/check_sunblade_acc_crit.sh`, confirmed red with `Acc: +3; Crit: x3` and green with `Acc: +2; Crit: x2`. | no | inc-tek.8.8 |
| The Bloodspear's page says its regeneration "lasts for 20 turns for every hit point" of a starting crit and that additional hits add 5 turns per hit point, but the handler started the effect at 5 turns per hit point (`amt*5`) and a crit landed while already at the maximum rate added only 1 turn per hit point (`amt`); the same arithmetic runs unchanged on Win32. Corrected the initial crit to `amt*20` and the maxed-rate crit path to `amt*5`, leaving the two paths that were already 5-per-hit-point untouched (`lib/m_items.irh`). | **Observed** -- seed 4, an orc coup-de-graces a sleeping goblin (an automatic critical), and the granted REGEN stati's remaining turns, read as "[Dur N]" off the wizard "Examine Player Data" dump, are the initial-crit duration. `tools/oracle_ab.sh` reproduces the change across commit 36bf101 (before build `36bf101^`): `[Dur 224]` before the fix and `[Dur 899]` after -- the `amt*5` -> `amt*20` correction, a four-fold increase for the same coup. `tools/keys/bloodspear-regen-duration.keys` drives it, `tools/check_bloodspear_regen_duration.sh` reads the current build, and `tools/check_bloodspear_regen.sh` still guards all four multipliers structurally. | no | inc-tek.8.8 |
| The Bloodspear's page lists bane against "humans, elves, dwarves, gnomes and halflings", but its BANE_LIST named only four types, omitting MA_GNOME; the same list data runs unchanged on Win32. Added MA_GNOME to the list (`lib/m_items.irh`). | **Observed** -- the omission is present directly in the BANE_LIST beside the page that names five races; guarded structurally by `tools/check_bloodspear_bane.sh`, confirmed red without MA_GNOME and green with it. | no | inc-tek.8.8 |
| The Bloodspear's page grants a +3 wounding longspear to "a kobold, goblin, lizardman, troll or other sometimes-ally to the orcs", but its wield handler tested only MA_GOBLINOID, MA_TROLL and MA_KOBOLD, so a lizardfolk wielder fell through to the plain +2 form; the same handler runs unchanged on Win32. Added MA_LIZARDFOLK to that branch (`lib/m_items.irh`). | **Observed** -- the missing type is present directly in the wield branch beside the page that names the lizardman; guarded structurally by `tools/check_bloodspear_lizardfolk.sh`, confirmed red without MA_LIZARDFOLK and green with it. | no | inc-tek.8.8 |
| The Bloodspear's page grants "+4 ... saving throws versus spells" only "when wielded by an orc", but the SAVE_BONUS sat on the effect component unconditionally and every EITEM(EV_WIELD) branch returned NOTHING, so any wielder received the +4; the same handler and grant run unchanged on Win32. Gated the grant by the wield return value -- the orc branch returns NOTHING so Grant() applies the bonus, the non-orc branches return DONE so the engine skips it (`lib/m_items.irh`). | **Observed** -- seed 1, an orc and a dwarf each wield the Bloodspear; the orc's character sheet carries a `+4 vs. spells` line on the Will save and the dwarf's does not. `tools/oracle_ab.sh` reproduces the gate across commit 24b7eb7 (before build `24b7eb7^`): the dwarf's Will breakdown reads `+2 vs. magic, +2 vs. poison, +2 vs. traps, +4 vs. spells, +4 vs. knockdown` before the fix and `+2 vs. magic, +2 vs. poison, +2 vs. traps, +4 vs. knockdown` after, the `+4 vs. spells` read off the character sheet on each side. `tools/keys/bloodspear-orc-save.keys` and `tools/keys/bloodspear-dwarf-save.keys` drive it, `tools/check_bloodspear_orc_save.sh` reads the current build. | no | inc-tek.8.8 |
| The Holy Avenger's page says its dispel magic fires "with a caster level equivalent to the wielder's paladin levels", but the handler commented out the paladin-level line and hardcoded caster level 12; the same literal runs unchanged on Win32. The intended LEVEL_AS macro was itself malformed (a ternary with no else branch) and is referenced nowhere else. Set the caster level directly to `EActor->LevelAs($"paladin")`, leaving the dead macro untouched (`lib/m_items.irh`). | **Observed** -- the hardcoded 12 sits beside the page that promises the wielder's paladin levels; guarded structurally by `tools/check_holy_avenger_dispel_cl.sh`, confirmed red with the literal 12 and green with LevelAs($"paladin"). A headless behavioral check remains the goal. | no | inc-tek.8.8 |
| The Dwarven Thrower's page calls it a "throwing returning warhammer", but its BASE_ITEM was the plain warhammer, which lacks IT_THROWABLE, so the artifact could not be thrown at all and its WQ_RETURNING was inert; the dead WQ_THROWING constant it referenced is defined nowhere. Added a throwable warhammer base item (`warhammer;thrown`, a hand-copy of the warhammer plus IT_THROWABLE/IT_NOGEN) APPENDED at the end of `lib/main.irc` per the append-only save rule (`docs/SAVE-SCHEMA-SPEC.md`) so existing saves keep every item position, and pointed the Dwarven Thrower's BASE_ITEM at it; removed the three dead WQ_THROWING fragments (`lib/main.irc`, `lib/m_items.irh`). | **Observed** -- oracle `tools/keys/dwarven-thrower-throw.keys`: with the fix a dwarf throws the Dwarven Thrower at a goblin four squares away and the message log reads "The Warhammer, Dwarven Thrower +4 returns to you." (it lands back in the weapon hand); on the reverted (pre-fix) build the plain-warhammer base is not throwable, so Fire will not throw it (the throw slot ejected a potion instead) and no return appears. A boomerang (`WT_RETURNING`) thrown as a control returns on both builds, proving the ranged-throw setup. Also guarded structurally by `tools/check_dwarven_thrower_throwable.sh` (the item is defined in `lib/main.irc`, appended, and the BASE_ITEM points to it). | no | inc-tek.8.8 |
| `Preprocess()` in the DECUS preprocessor closes its input file twice: `src/cpp1.c:503` hands the stream to the include stack with `addfile()`, `src/cpp6.c:650` closes it when `cppmain()` reaches EOF, and `src/cpp1.c:506` then closed the same `FILE*` again. Any libc that validates frees aborts `-compile`; Win32 and the original compiler are affected identically wherever the CRT checks. Removed the second `fclose(fin)` and left `fclose(fout)` alone (`src/cpp1.c`). | **Observed** -- the first Linux run of `-compile main.irc` aborted with `free(): double free detected in tcache 2`; gdb on Debian 11 arm64 put frames `_IO_new_fclose` -> `Preprocess` -> `Game::ResourceCompiler (src/RComp.cpp:118)` on the stack. With the line removed, `-compile` completes and writes `mod/Incursion.Mod` on Linux x86_64 and arm64. Guarded by `tools/check_linux_build.sh`, confirmed red by restoring the `fclose(fin)`. macOS never reported it because its allocator does not check. | no | inc-0dyb |
| The Javelin of Lightning's page promises a Reflex save at "DC 10 + 1/2 the thrower's character level + the thrower's Dexterity modifier", but the effect's `EV_CALC_EFFECT` handler left the entire `saveDC` formula inside a `/* */` comment, so `e.saveDC` was never set and the save used an engine default; the same commented block runs unchanged on Win32. Uncommented the assignment (`e.saveDC = 10 + EActor->ChallengeRating()/2 + EActor->Mod(A_DEX)`, where a character's `ChallengeRating()` is its total class level) and removed the two dead commented `vRange` duplicates (`lib/m_items.irh`). | **Observed** -- the oracle `tools/keys/javelin-lightning-savedc.keys` (run with `INCURSION_OPTIONS=tools/gates/Options.Dat`, seed 1) throws the javelin from a Barbarian-3, Dex +1 thrower at a frozen goblin and reads the goblin's Reflex-save line: without the fix it shows `vs DC 17`, with the fix `vs DC 12` = 10 + 3/2 + 1, matching the Desc; both numbers read from the player-facing save line. Also guarded structurally by `tools/check_javelin_lightning_savedc.sh`, red with the commented formula and green once it is live. | no | inc-tek.8.8 |
| The Horn of the Sewers summons at `pval: (PLUS_2PER1)d(1)` -- twice its magical plus -- exactly like its three sibling horns (Ordered Defense, the Frenzied Hunt, the Tritons), but its Desc alone said the summoned rodents have "CR equal to its magical plus", dropping the "twice" the three siblings and the shared code carry; the same Desc ships on Win32. Added "twice" to the Sewers' Desc so it matches its siblings and the code (`lib/m_items.irh`); no behaviour changes. | **Traced** -- prose-only correction; the summon CR is unchanged, so no in-play value moves and no gameplay observation applies. The three sibling Descs and the shared pval prove the intended wording; guarded structurally by `tools/check_horn_sewers_cr.sh`, red without "twice" and green with it. | no | inc-tek.8.8 |
| The Horn of Goodness' Desc says its protective Magic Circle vs. Evil field is "60 feet in radius", but `EV_EFFECT` laid the field with `NewField`'s radius argument set to 2 (20 ft); the fear globe's `lval` 6 (60 ft) applies to the separate `AFRAID` effect, not this field, and the same radius-2 call ships on Win32. Changed the `NewField` radius from 2 to 6 so the field is 60 ft, matching the Desc and the fear globe (`lib/m_items.irh`) | **Observed** -- oracle `tools/keys/horn-goodness-radius.keys` blows a Horn of Goodness and reads the white-floor field's extent off the map: the field reaches 6 squares (60 ft) with the fix and 2 squares (20 ft) without it. Also guarded structurally by `tools/check_horn_goodness_radius.sh`, red at radius 2 and green at radius 6. | no | inc-tek.8.8 |
| `Item::Activate` splits one unit off a stack with `TakeOne()`, which always leaves that unit outside the actor's pack, and then disposed of it on no exit at all -- success included. Activating a rod or a horn does not spend it, so every activation of one of a stack destroyed one unit of that stack. The give-back had been commented out upstream because it also fired for a `Quantity`-1 item, and the Rod of the Python `Remove(false)`s the very rod it turns into a snake. It is plain control flow -- no typedef, pointer width, endianness or compiler extension is involved -- so Win32 with the original typedefs and upstream compiler loses the unit in exactly the same place. Restored the give-back through the existing `Item::GiveBackTo` helper, gated on this function having done the splitting and on `F_DELETE` (`src/Magic.cpp`) | **Observed** -- `tools/check_activate_stack.sh`, seed 4: before, the activate menu's `3 Horns of Plenty` read `2 Horns of Plenty` after one was blown; after, it still reads `3 Horns of Plenty`, and the same screen still carries the effect's own `Fruits and produce appear at your feet!`, so the stack is not intact merely because nothing fired | no | inc-od6j |
| Cure Critical Wounds healed `3d8 + LEVEL_MAX20` while the effect's own `Desc` promises 4d8 -- "Heals you of 4d8 + your level ... to a maximum of 4d8+20" -- so the spell paid one die less than it advertised. Its damage twin Inflict Critical Wounds already rolls `4d8 + LEVEL_MAX20`, and the tier-down Cure Serious Wounds is the 3d8 spell (`3d8 + LEVEL_MAX15`), so the intended value is unambiguous. Changed only the dice token to `4d8`; the `Desc`, the `LEVEL_MAX20` cap and the `yval` are left alone. Not a port artefact: `lib/` is unchanged by the port, the value is a spell-declaration constant, and a Win32 build with the original typedefs reads the same `3d8` against the same 4d8 prose (`lib/pspells.irh`) | **Traced** -- `tools/check_cure_critical.sh` reads the effect's pval out of the file and fails on `3d8`; confirmed red on the pre-fix line and green after, and the module rebuilds so the token parses. A clean single gameplay number was not forced: the heal rolls 4d8 and dice variance blocks one deterministic reading | no | inc-tek.8.7 |
| Retributive Mirror reflected `bonus = e.vDmg / 5` -- one fifth of each incoming hit -- while the spell's own `Desc` promises "one third of all damage dealt to you (round down) is reflected back to the attacker". The `META(PRE(EVICTIM(EV_DAMAGE)))` handler paid one fifth, not the one third the prose advertises. Retributive Mirror is an Incursion invention with no D&D twin, so its own `Desc` is the only reference and the divisor is unambiguously wrong. Changed only the divisor from 5 to 3; the `ThrowDmg` call, the `ABORT` logic and the `Desc` are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a script constant in a spell declaration, and a Win32 build with the original typedefs parses the same `/ 5` against the same "one third" prose (`lib/pspells.irh`) | **Observed** -- a fire giant strikes a warded caster for four hits (23, 23, 16, 16, read from the log with Show All Combat Rolls) on seed 1; the giant's own hit points drop 205 -> 181 = 24 with the fix, exactly `floor(23/3)+floor(23/3)+floor(16/3)+floor(16/3) = 7+7+5+5`, and 205 -> 191 = 14 on the pre-fix `/ 5` build, exactly `4+4+3+3`, same seed and identical incoming; `tools/keys/retributive-mirror.keys` drives the run and `tools/check_retributive_mirror.sh` guards the divisor structurally, red on `/ 5` and green on `/ 3` | no | inc-tek.8.7 |
| Flame Strike rolls `pval: (LEVEL_SCALED)d6` -- 1d6 per caster level, half fire / half divine, the SRD-authentic value -- but its own `Desc` said "1d8 points of damage per caster level", one die size too large; the same Desc typo ships on Win32 with the original data, so it misdescribes the damage there too. The script d6 is the SRD-correct value and the prose was the outlier, so the fix corrected only the prose to 1d6 and left the pval, flags and `EF_HALF_UNTYPED` split untouched (`lib/pspells.irh`) | **Traced** -- prose-only correction; the effect's damage is unchanged, so no in-play value moves and no gameplay observation applies. The SRD (1d6 per level) and the untouched pval prove the intended wording; guarded structurally by `tools/check_flame_strike.sh`, red with "1d8" and green with "1d6" | no | inc-tek.8.7 |
| Unholy Blight's "sicken good creatures" clause did nothing. Its `EA_INFLICT` segment assigned `xval` twice -- `xval: ADJUST_CIRC` then `xval: A_AID` -- so the second write overwrote the first: the applied stati became `A_AID` (51, `inc/Defines.h`), an undefined stati, and `yval` -- the attribute group the `ADJUST_CIRC` penalty acts on -- was never set. A `MA_GOOD` creature that failed the Fort save gained no stati at all. The second token was meant to be `yval: A_AID`, as the sibling Thornwrack effect shows (`xval: ADJUST_CIRC; yval: A_AID`) and as Order's Wrath (`xval: STUNNED`) and Chaos Hammer (`xval: CONFUSED`) confirm by each carrying a single `xval`. Changed only that one token from `xval` to `yval`; `ADJUST_CIRC`, the `pval`, the Flags and the `MSG_STATINAME` are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a data double-assignment in an effect declaration, and a Win32 build with the original typedefs parses the same doubled `xval` and applies the same undefined stati (`lib/pspells.irh`) | **Traced** -- `tools/check_unholy_blight.sh` reads the inflict segment and demands one `xval` (`ADJUST_CIRC`), a `yval: A_AID`, and no second `xval: A_AID`; confirmed red on the pre-fix double-`xval` line and green after, and the module rebuilds so the tokens parse. A deterministic gameplay reading was not forced: the sicken lands only when a `MA_GOOD` creature fails a Fort save while surviving the same-cast evil blast, the stati lasts 1d4 rounds, and no wizard facility forces a save result | no | inc-tek.8.7 |
| `Creature::Devour(Corpse * c)` (`src/Skills.cpp`) builds a fake `Monster` from the corpse's `mID` to score a devoured meal, then copies `TEMPLATE` stati onto it -- but it iterated `StatiIterNature(this, TEMPLATE)`, the EATER's templates, not the corpse `c`'s. A templated corpse stores its own templates on itself (`Corpse::Corpse`, `src/Item.cpp`) and every sibling consumer reads them back off the corpse: Sacrifice (`StatiIter(e.EItem)`, `src/Prayer.cpp`) and the burning-hunger eat (`StatiIterNature(c,TEMPLATE)`, `src/Item.cpp`). So a character eating a celestial / fiendish / half-dragon / pseudonatural corpse got the base monster's payoff only -- the template's resistances (celestial: acid/cold/elec, MR35; fiendish: fire/cold) and its SAPIENT-on-eat marker were silently dropped -- while a templated eater wrongly applied ITS own template to the food. `StatiIterNature`/`StatiIterEnd` also bump a `Nested` counter on their argument (`inc/Map.h`), so both the open and the matching close were moved from `this` to `c` together. The comment one line up already stated the intent, "now that we're taking Templates into account for corpses" (`src/Skills.cpp`) | **Observed** -- A/B on Brian's own save (a level-9 Orc with Devouring standing on a fresh celestial arsinoitherium corpse), same seed and the same 465-turn meal, one build differing only in the two tokens: the pre-fix binary finishes the corpse and grants NOTHING, while the fixed binary prints `You gained Lightning resistance from the celestial arsinoitherium's flesh` and `You gained Disease resistance from the celestial arsinoitherium's flesh`. The pre-fix zero is load-bearing -- the fake monster carries no celestial resistance to grant, so both gains come from the celestial template the bug dropped (acid/cold are already resisted and so go unannounced by the `curr < lev` guard). Evidence, the exact save and the key script are in `docs/evidence/inc-x1f3/`. Also guarded structurally by `tools/check_devour_template_source.sh`, confirmed red on the pre-fix `StatiIterNature(this,...)`/`StatiIterEnd(this)` and green after; the corpse-vs-eater confusion is confirmed by reading `Corpse::Corpse` (writes templates onto `c`) against the two sibling consumers (both read off `c`). Not a port artefact: no typedef/width/endianness/compiler dependence; a Win32/MSVC build with the original typedefs reads `this` and drops the same templates | no | inc-x1f3 |
| Divine Power's `POST(EV_MAGIC_HIT)` handler has three branches, and the spell's own `Desc` promises two different STR-18 rewards: with 18 Strength and no Power Attack feat the caster GAINS Power Attack; with the feat already held he gains Knock Prone instead. Both STR-18 branches called `GainTempStati(EXTRA_FEAT, ... FT_KNOCK_PRONE, ...)`, so the second branch -- `else if (EActor->IAttr(A_STR) >= 18)`, the one WITHOUT the `HasFeat(FT_POWER_ATTACK)` test -- duplicated the first, and the "gain Power Attack" case never fired. Changed only that one branch's feat token to `FT_POWER_ATTACK`; the `HasFeat` branch (still `FT_KNOCK_PRONE`), the STR-raise `else` branch and the `Desc` are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a feat constant in a spell declaration, and a Win32 build with the original typedefs reads the same duplicated `FT_KNOCK_PRONE` against the same `Desc` (`lib/pspells.irh`) | **Observed** -- a levelled orc priest (inherent STR raised to 18 by the level-4 attribute bump, no Power Attack feat) casts Divine Power and strikes a summoned monster on seed 1; the caster's own stati dump then carries `EXTRA FEAT from SS ENCH (Val:Power Attack) (eID:Divine Power)`, and on the pre-fix build the same run carries only `Val:Knock Prone` and zero Power Attack. `tools/keys/dp-oracle.keys` drives the run; `tools/check_divine_power.sh` guards both branches structurally, red when the bare STR-18 branch grants `FT_KNOCK_PRONE` and green on `FT_POWER_ATTACK`, while asserting the `HasFeat` branch still grants `FT_KNOCK_PRONE` | no | inc-tek.8.7 |
| Snowstrike (an `EA_BLAST` cold blast, a directly-learnable Druid spell) carried no `Flags` line, so `EF_CASTER_IMMUNE` and `EF_ALLIES_IMMUNE` were both absent and the `AR_BALL` blast froze the caster and her allies -- directly against the spell's own `Desc`, which promises "The caster and her allies are immune to the effect". `Magic.cpp`'s target test exempts the caster only when `EF_CASTER_IMMUNE` is set and a friendly creature only when `EF_ALLIES_IMMUNE` is set, so neither exemption fired. The sibling Ice Dagger, an `EA_BLAST` cold blast whose `Desc` says "The caster is immune", carries `flags: EF_CASTER_IMMUNE` and confirms the mechanism. Added `Flags: EF_CASTER_IMMUNE, EF_ALLIES_IMMUNE;` -- both, because the prose names caster and allies; the `pval` `(LEVEL_EVERY2)d4` and the `Desc` are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a data declaration, and a Win32 build with the original typedefs ships the same flagless block and drops the same immunity against the same prose; not a D&D divergence, the `Desc` states the immunity explicitly (`lib/pspells.irh`) | **Traced** -- `tools/check_snowstrike.sh` reads the Snowstrike block's `Flags` declaration and demands both `EF_CASTER_IMMUNE` and `EF_ALLIES_IMMUNE`; confirmed red before the flags were added and green after, and the module rebuilds so the flags parse. A behavioural before/after was not forced: staging a friendly creature reliably inside the 1-square blast, alongside a hostile, for a clean cold-damage reading proved infeasible | no | inc-tek.8.7 |
| Command's `EA_GENERIC` block deliberately removed the spell's `EF_LIM_LEVEL` restriction -- the `tval: LEVEL_2PER3; Flags: ..., EF_LIM_LEVEL;` line is commented out and the maintainer's own note above it reads "I don't see anything in the SRD about this having a level limit ... Let's remove that limit." -- yet the `Desc` still ended with "Does not affect creatures whose CR is more than 2/3rds your level", advertising the removed limit. The code is correct: the removal is deliberate, so nobody should re-add `EF_LIM_LEVEL` to match the old prose. Deleted only that trailing sentence; the commented-out `tval`/`EF_LIM_LEVEL` line, the maintainer comment, the Flags and the script are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in a spell declaration, and a Win32 build with the original data ships the same stale sentence against the same limit-free code (`lib/pspells.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The commented-out `EF_LIM_LEVEL` gate and the maintainer's "Let's remove that limit" note prove the intended wording; guarded structurally by `tools/check_command_prose.sh`, red while the "2/3rds your level" sentence is present and green once it is removed | no | inc-tek.8.7 |
| The "Brazier Commanding Fire Elementals" item (`lib/m_items.irh`) is coded `EF_3PERDAY` -- three activations per day, `EF_3PERDAY` defined in `inc/Defines.h` and the shared idiom across this file's activated summon items -- yet its `Desc` opened "Once per day, this brazier can be lit to summon", understating its own charges. The code is correct: `EF_3PERDAY` grants three uses per day, so nobody should cut the flag to match the old prose. Restated only the Desc's opening to "Three times per day"; the `Flags` (`EF_3PERDAY` stays), the summon script and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same understating sentence against the same three-per-day code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `EF_3PERDAY` flag proves the intended wording; guarded structurally by `tools/check_brazier_prose.sh`, red while the "Once per day" claim is present and green once it is restated to three per day | no | inc-tek.8.8 |
| The "Telepathy" helm (`lib/m_items.irh`) states a flat "within 60 feet of him", yet its range is coded `pval: PLUS_ADD5` with `EF_NEEDS_PLUS` -- plus+5 SQUARES, i.e. 50 feet + 10 feet per magical plus at the file's 10-feet-per-square scale, so the range SCALES and never has a fixed value; 60 feet holds only at +1. The code is correct: the scaling is deliberate, so nobody should pin the range to match the old flat prose. Reworded only the Desc to state the scaling range; the `pval` (`PLUS_ADD5` stays), `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code; the sibling Medallion of ESP states the same pattern correctly (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: PLUS_ADD5` scaling range and the sibling Medallion of ESP correct wording prove the intended prose; guarded structurally by `tools/check_telepathy_prose.sh`, red while the flat "60 feet" is present and green once the Desc states "per magic plus" | no | inc-tek.8.8 |
| The "Periapt of Wound Closure" item (`lib/m_items.irh`) promised in prose "stops bleeding, removes infections and speeds healing", yet the entity removes no disease or infection at all -- it has no `HEAL_MALADY`, no `IMMUNITY AD_DISE` and no disease-stati removal; its two `TRAP_EVENT` handlers only clear `BLEEDING` (`RemoveStati(BLEEDING)`) and full-heal on `EV_REST`. The code is correct: the item really does the other two things it claims, so nobody should add a disease-cure to match the old prose. Deleted only the false ", removes infections" clause from the Desc; both `TRAP_EVENT` handlers, the `RemoveStati(BLEEDING)` registration, `Level` and `Flags` are left alone, and no resource is added. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same over-promising sentence against the same disease-free code; the SRD Periapt of Wound Closure has no infection removal and the sibling Periapt of Health is the disease item (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The absent disease machinery and the sibling Periapt of Health prove the intended wording; guarded structurally by `tools/check_periapt_closure_prose.sh`, red while the "removes infections" claim is present and green once it is deleted | no | inc-tek.8.8 |
| The "Elemental Command (Air)" ring (`lib/m_items.irh`) opens its granted staff-spell list with "wind column", yet no spell of that name exists anywhere in `lib/`; the ring's `STAFF_SPELL_LIST` grants the real air spell "gaseous form" in that first slot (`1 $"gaseous form" ...`), and the other five names match. The code is correct: the ring grants "gaseous form", so nobody should invent a "wind column" spell to match the old prose. Corrected only the Desc's first spell name from "wind column" to "gaseous form"; the `STAFF_SPELL_LIST` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same phantom name against the same grant (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `STAFF_SPELL_LIST`'s `gaseous form` grant proves the intended wording; guarded structurally by `tools/check_air_ring_spell.sh`, red while the phantom "wind column" is present and green once the Desc names "gaseous form" | no | inc-tek.8.8 |
| The ball Wand of Life Stealing (`lib/m_items.irh`) -- the first `AI_WAND Effect "Life Stealing" : EA_BLAST`, whose `MSG_BLASTNAME` is "ball of dark energy" -- states a flat "1d6 points of necromantic damage" with no per-plus, yet the blast damage is coded `pval: (PLUS_1PER1)d6` with `EF_NEEDS_PLUS` -- 1d6 per magical plus -- so the damage SCALES with the wand plus and never has a fixed value; 1d6 holds only at +1. The code is correct: the scaling is deliberate, so nobody should pin the damage to match the old flat prose. Inserted only " per plus" into the Desc's damage clause; the `pval` (`(PLUS_1PER1)d6` stays), `Flags`, `Level` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code; the sibling bolt Wand of Life Stealing states "per plus" correctly (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: (PLUS_1PER1)d6` scaling damage and the sibling bolt wand's correct "per plus" prose prove the intended wording; guarded structurally by `tools/check_life_stealing_prose.sh`, red while the flat "necromantic damage to" is present and green once the Desc states "necromantic damage per plus" | no | inc-tek.8.8 |
| The Wand of Unearthly Harmonies (`lib/m_items.irh`) states a flat "1d2 points of Intelligence damage" with no per-plus in its Intelligence-damage clause, yet that damage is coded in a second `EA_BLAST` block as `xval: AD_DAIN; pval: (PLUS_1PER1)d2` under `EF_NEEDS_PLUS` -- 1d2 per magical plus -- so the Intelligence damage SCALES with the wand plus and never has a fixed value; 1d2 holds only at +1. The wand's own sonic clause already states "1d8 points of sonic damage per plus the wand possesses". The code is correct: the scaling is deliberate, so nobody should pin the Intelligence damage to match the old flat prose. Inserted only " per plus the wand possesses" into the Desc's Intelligence-damage clause; the second `EA_BLAST` `pval` (`(PLUS_1PER1)d2` stays), the already-correct sonic clause, the later "but not to the Intelligence damage." sentence and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code; the wand's own sonic clause states the pattern correctly (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The second `EA_BLAST` `pval: (PLUS_1PER1)d2` scaling damage and the wand's own correct sonic clause prove the intended wording; guarded structurally by `tools/check_unearthly_harmonies_prose.sh`, red while the flat "1d2 points of Intelligence damage" is present and green once the Desc states "1d2 points of Intelligence damage per plus" | no | inc-tek.8.8 |
| The Dragonshield (`lib/m_items.irh`) Desc colour list names a dragon colour "brown" that the item never displays, yet its `EV_GETNAME` handler maps the seven damage types to the names it actually shows -- Red (`AD_FIRE`), White (`AD_COLD`), Blue (`AD_ELEC`), Black (`AD_ACID`), Purple (`AD_NECR`), Yellow (`AD_SLASH`) and Green (`AD_TOXI`). So "brown" names no shield and the real name "Yellow" (`AD_SLASH`) was absent from the Desc. The code is correct: the displayed name comes from `EV_GETNAME`, so the prose should match it. Changed only the lowercase Desc word "brown" to "yellow"; the uppercase glyph constant `BROWN` in `EV_CALC_EFFECT` (`case AD_SLASH: e.cval = BROWN;`, a deliberate glyph colour), every handler and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same "brown" against the same `EV_GETNAME` name table | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `EV_GETNAME` name table (`AD_SLASH` -> "Yellow", no case producing "brown") proves the intended wording; guarded structurally by `tools/check_dragonshield_prose.sh`, red while the Desc colour list names "brown" and green once it names "yellow" | no | inc-tek.8.8 |
| The Staff of the Goblin Queen (`lib/m_items.irh`) describes the +4 Handle Device/Bluff bonuses and the -4 Spot/Listen/Concentration penalties with a generic "Wielders of the staff", yet its `EV_MAGIC_HIT` handler gates ALL six granted effects behind one all-or-nothing goblinoid + 3rd-caster-level test with NO per-effect (`e.efNum`) test, so a non-goblinoid wielder gets none of them -- spells, +4 bonuses and -4 penalties alike. The code is correct: the sibling Staff of the Spider uses `e.efNum` gating (`e.efNum >= 2`), which proves the author had per-effect tooling and deliberately did not use it here, so single-tier goblinoid scope is intended. Scoped only the two generic "Wielders of the staff" phrases to "Goblinoid wielders of the staff"; the "any goblinoid spellcaster of 3rd level or above" spell clause, every handler and every effect block are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same generic sentences against the same all-or-nothing goblinoid gate (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The all-or-nothing goblinoid gate and the sibling Staff of the Spider's `e.efNum` gating prove the intended wording; guarded structurally by `tools/check_goblin_queen_prose.sh`, red while the bare "Wielders of the staff" phrases are present and green once both read "Goblinoid wielders" | no | inc-tek.8.8 |
| The Ring of Animal Kinship (`lib/m_items.irh`) Desc promised that "At +3 or higher" the ring would let you use Animal Empathy even with no ranks in it, yet the entity holds no +3 test and no `INNATE_KIT` grant, so no code ever enforced that threshold. It is also redundant: the ring's own `SKILL_BONUS SK_ANIMAL_EMP` (`pval: PLUS_2PER1`) makes `SkillLevel(SK_ANIMAL_EMP)` nonzero, the Animal-Empathy use-gate (`src/Skills.cpp:1111`) tests `SkillLevel` not `SkillRanks`, and `SkillLevel` sums the item bonus with no trained-only gate zeroing an untrained total (`src/Create.cpp:4198-4243`), so a rank-less wearer can already attempt the skill from +1. The code is correct: untrained use works from +1, so nobody should add a +3 gate to match the old prose. Dropped the false "+3 or higher" threshold and stated the untrained use plainly ("It will also allow you to use the skill even if you have no ranks in it."); the `SKILL_BONUS` grant, the `NEUTRAL_TO` grant, every value and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same "+3 or higher" sentence against the same code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `SkillLevel` use-gate at `src/Skills.cpp:1111` and the absence of a trained-only zeroing in `SkillLevel` (`src/Create.cpp:4198-4243`) prove the intended wording -- untrained attempts already succeed from +1; guarded structurally by `tools/check_animal_kinship_prose.sh`, red while the Desc reads "At +3 or higher" and green once that threshold is dropped and "no ranks in it" remains | no | inc-tek.8.8 |
| The Eyes of Precision item (`lib/m_items.irh`) states a flat "20 feet (2 squares)" lowlight-vision bonus, yet its CA_LOWLIGHT grant is coded `pval: PLUS_2PER1` with `EF_NEEDS_PLUS` -- 2 SQUARES per plus, i.e. 20 feet per magical plus at the file's 10-feet-per-square scale, so the bonus SCALES and never has a fixed value; 20 feet holds only at +1. The code is correct: the scaling is deliberate, so nobody should pin the bonus to match the old flat prose. Reworded only the Desc to state the scaling; the `pval` (`PLUS_2PER1` stays), `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: PLUS_2PER1` scaling bonus proves the intended prose; guarded structurally by `tools/check_precision_prose.sh`, red while the flat "20 feet (2 squares)" is present and green once the Desc states "per magical plus" | no | inc-tek.8.8 |
| The Cowl of Warding item (`lib/m_items.irh`) states a flat "a +4 bonus to save versus spells, a +6 luck bonus to armour", yet the SN_SPELLS save is coded `pval: PLUS_ADD3` (plus+3) and the A_ARM luck bonus `pval: PLUS_ADD5` (plus+5), both additive with the magical plus under `EF_NEEDS_PLUS`, so +4 / +6 hold only at +1 and rise with the plus. The code is correct: the scaling is deliberate, so nobody should pin the bonuses to match the old flat prose. Reworded only the Desc to state the scaling; the `pval`s (`PLUS_ADD3` and `PLUS_ADD5` stay), `Level`, `Flags`, the immunities and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: PLUS_ADD3` and `pval: PLUS_ADD5` additive bonuses prove the intended prose; guarded structurally by `tools/check_cowl_warding_prose.sh`, red while the flat "a +4 bonus to save versus spells" is present and green once the Desc states "+3 plus its magical plus" | no | inc-tek.8.8 |
| The Periapt of Proof against Poisons item (`lib/m_items.irh`) states a flat "a +2 magic bonus to saving throws versus poison", yet the SN_POISON save is coded `pval: PLUS_2PER1` with `EF_NEEDS_PLUS` -- 2 per magical plus -- so the save SCALES and never has a fixed value; +2 holds only at +1. The code is correct: the scaling is deliberate, so nobody should pin the save to match the old flat prose. Reworded only the poison-save clause of the Desc to state the scaling; the `pval` (`PLUS_2PER1` stays), the already-correct toxic-resistance clause, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: PLUS_2PER1` scaling save proves the intended prose; guarded structurally by `tools/check_periapt_poison_prose.sh`, red while the flat "a +2 magic bonus to saving throws versus poison" is present and green once the Desc states "a +2 per magical plus magic bonus" | no | inc-tek.8.8 |
| The Lesser Divine Aspect item (`lib/m_items.irh`) states a flat "+2 bonus to saving throws against disease and poison" and "resistance of 5", yet the acid/cold/electricity resistances are coded `pval: PLUS_5PER1` (5 per plus) and the disease/poison saves `pval: PLUS_2PER1` (2 per plus), both under `EF_NEEDS_PLUS`, so both SCALE and hold their +2 / 5 values only at +1. The code is correct: the scaling is deliberate, so nobody should pin the values to match the old flat prose. Reworded only the Desc's save and resistance clauses to state the scaling; the `pval`s (`PLUS_5PER1` and `PLUS_2PER1` stay), the genuinely-flat Charisma +4 and darkvision, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same flat sentence against the same scaling code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `pval: PLUS_5PER1` and `pval: PLUS_2PER1` scaling values prove the intended prose; guarded structurally by `tools/check_divine_aspect_prose.sh`, red while the flat "resistance of 5." is present and green once the Desc states "resistance of 5 per magical plus" | no | inc-tek.8.8 |
| The Ring of Elemental Command (Fire) (`lib/m_items.irh`) grants its wearer a fire resistance coded `xval: RESIST; yval: AD_FIRE; pval: 12`, yet the ring's own `Desc` states "The ring also grants its wearer a Fire Resistance of 10." The two disagree by two points. Brian ruled the `Desc` is authoritative -- prose wins -- so the coded value must be 10. Changed only that grant's `pval` from 12 to 10; the ring's other `EA_GRANT` blocks (the fire-elemental strike-and-command handler, the staff-spell list, the cold-damage curse), the `Desc` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a data value in an item declaration, and a Win32 build with the original data ships the same `pval: 12` against the same `Desc` "Fire Resistance of 10" (`lib/m_items.irh`) | **Observed** -- an orc barbarian (`chargen.keys`) reads his character-sheet Resistances page before and after wizard-mode wearing a Ring of Elemental Command (Fire) on seed 1; the sheet shows no Fire line before and `Fire 10` after the fix, where the pre-fix build showed `Fire 12`. `tools/keys/fire-ring-resist.keys` drives the run; `tools/check_fire_ring_resist.sh` guards the value structurally, red while the grant reads `pval: 12` and green at `pval: 10` | no | inc-tek.8.8 |
| The Horn of the Kobolds (`lib/m_items.irh`) `Desc` promises that "In the hands of a member of any other race, however, the kobolds will be hostile", yet the entity was a bare declarative `EA_SUMMON` with no handler and no race test, and `Magic::Summon` hardcodes `xe.enFlags = EN_SUMMON` with no hostile flag and no race check (`src/Effects.cpp:1448-1471`), so every wielder -- kobold or not -- got FRIENDLY kobolds and the second `Desc` sentence was never implemented. Brian ruled prose wins -- implement the code. Added an `On Event EV_MAGIC_HIT` handler to the block: a non-kobold wielder summons `EN_SUMMON|EN_SINGLE|EN_HOSTILE` kobolds via `thEnGenMTypeXY($"mass summons", MA_KOBOLD, ..., e.vDmg, 0, EActor->x, EActor->y)` and returns `DONE` to suppress the friendly built-in, while a kobold wielder returns `NOTHING` and falls through to the unchanged declarative friendly summon, so the CR (`2 x plus`, via `e.vDmg`), the group size (`EN_SINGLE`, the horn has no `EF_MULTIPLE`) and every other value match the friendly path exactly. Not a port artefact: `lib/` is unchanged by the port, this is a missing feature in shared item data, and a Win32 build with the original data ships the same `Desc` against the same friendly-only `Magic::Summon` (`lib/m_items.irh`) | **Observed** -- same seed 4, same keys, same non-kobold blower (Henk, an orc barbarian from `chargen.keys`): on the pre-fix module the summoned kobold's examine Hostility line reads `Henk: it felt strong allegiance because it owes fealty` (allied), on the post-fix module it reads `Henk: it felt minimal enmity because it is evil` (hostile). `tools/keys/kobold-horn-hostile.keys` drives the run and reads the summoned kobold's Hostility section; `tools/check_kobold_horn.sh` guards the handler structurally, red on HEAD and green on the working tree. The kobold-wielder friendly path is unchanged by construction: the handler returns `NOTHING`, so that branch runs the identical prior code | no | inc-tek.8.8 |
| The flame tongue sword (`lib/m_items.irh`) `Desc` promises the tongue-of-flame lash acts "if the victim still lives and is a Large or smaller corporeal creature", yet the lash effect (`0 Effect "tongue of flame" : EA_BLAST`) gated the opposed-Strength yank on `if (EVictim->GetAttr(A_SIZ) >= SZ_LARGE) return NOTHING;` -- an off-by-one that bailed for Large AND everything larger, so only Medium-and-smaller creatures ever reached the contest and a Large victim was silently dropped. Brian ruled prose wins: include Large, exclude only LARGER. Changed only that comparison operator from `>=` to `>`; the opposed-Strength test, the `PlaceAt`/knockdown/`STUCK` yank body, the `EV_CALC_EFFECT` range and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is a comparison in shared item data, and a Win32 build with the original data ships the same `>= SZ_LARGE` against the same `SZ_LARGE`/`A_SIZ` constants (`lib/m_items.irh`) | **Observed** -- seed 1, a summoned otyugh (Large, `A_SIZ` 5, Str 11) three squares east is hit by the activated lash for 15 of its 53 hit points and lives: on the pre-fix module (`>=`) the otyugh stays three squares east and its Stati list holds five entries, while on the post-fix module (`>`) the lash prints "Your tongue of flame lashes around the otyugh and pulls it to your side. The otyugh is knocked from its feet!", the otyugh's map glyph moves to the square east of the player and its Stati count rises to six (the knockdown Prone). `tools/keys/flame-tongue-large-yank.keys` drives the run; `tools/check_flame_tongue_large.sh` guards the operator structurally, red on HEAD and green on the working tree | no | inc-tek.8.8 |
| The "Sword of the Planes" weapon (`lib/m_items.irh`) scoped its +3 enhancement tier in prose to "denizens of the ethereal or astral planes", yet the `EA_GENERIC` `EV_WATTACK` handler grants the +3 to any `MA_OUTSIDER` (`else if (EVictim->isMType(MA_OUTSIDER)) newBonus = 3;`), which is broader than ethereal/astral denizens and is the only engine type for planar creatures; the demon/devil/celestial `+4` branch runs first, so the +3 tier covers every other outsider. The code is correct: the `MA_OUTSIDER` branch is deliberate, so nobody should narrow it to match the old prose. Reworded only the +3-tier clause of the Desc to name outsiders generally, keeping the "or when the wielder is in such a locale herself" clause that the `PHASED` locale bonus still backs; the handler, the `PHASED` max, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same narrow sentence against the same `MA_OUTSIDER` code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `MA_OUTSIDER` branch and the `PHASED` locale bonus prove the intended wording; guarded structurally by `tools/check_planes_sword_prose.sh`, red while the narrow "denizens of the ethereal or astral planes" is present and green once the Desc names outsiders generally | no | inc-tek.8.8 |
| The flame tongue sword (`lib/m_items.irh`) gave its "Tongue of Flame" lash a maximum range in prose of "the bearer's Charisma modifier times ten in feet (minimum 20)", yet the `EV_CALC_EFFECT` handler sets `e.vRange = 3 + max(0, EActor->Mod(A_CHA))` and `vRange` is in SQUARES, which at this file's scale of 10 feet per square gives a real range of (3 + Charisma modifier) squares = 30 feet plus 10 feet per point of Charisma modifier, floored at 30 feet because `max(0, ...)` clamps the modifier at 0; the prose both dropped the +3-square (30-foot) base and stated a 20-foot floor the clamp cannot produce. The code is correct: the +3-square base and the clamp are deliberate, so nobody should change the range to match the old prose. Reworded only the range clause of the Desc to state the real formula -- a base of 30 feet plus 10 feet per point of Charisma modifier, minimum 30 feet; the `EV_CALC_EFFECT` script, `e.vRange`, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same wrong sentence against the same `vRange` code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `e.vRange = 3 + max(0, EActor->Mod(A_CHA))` formula and the file's 10-feet-per-square scale prove the intended wording; guarded structurally by `tools/check_flame_tongue_range_prose.sh`, red while the "Charisma modifier times ten in feet (minimum 20)" prose is present and green once the Desc states the 30-foot base plus 10 feet per Charisma modifier | no | inc-tek.8.8 |
| The "Staff of Divination" (`lib/m_items.irh`) named a granted spell "true sight" in its Desc's spell list, yet no spell of that name exists anywhere in `lib/`; the staff's own `STAFF_SPELL_LIST` grants the real spell "true seeing" (`$"true seeing"`) and the spell entity is `Wizard / Priest Spell "True Seeing"` (`lib/wspells.irh`). The code is correct: the staff grants "true seeing", so nobody should invent a "true sight" spell to match the old prose. Corrected only the Desc's last spell name from "true sight" to "true seeing"; the `STAFF_SPELL_LIST` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same phantom name against the same grant (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `STAFF_SPELL_LIST`'s `true seeing` grant and the `True Seeing` spell entity prove the intended wording; guarded structurally by `tools/check_divination_staff_prose.sh`, red while the phantom "true sight" is present and green once the Desc names "true seeing" | no | inc-tek.8.8 |
| The "Circlet of Blasting" (`lib/m_items.irh`) Desc said its radiance beam inflicts "10d6 damage to undead, 5d6 damage to constructs and 5d8 damage to everything else", yet the base 5d8 `EA_BLAST` is coded `tval: MA_LIVING` under `EF_LIM_MTYPE`, so it strikes LIVING creatures only, not everything; the `MA_UNDEAD` (10d6) and `MA_CONSTRUCT` (5d6) sibling blasts carry the other two clauses. This matches Incursion own sibling spell Searing Light (`lib/pspells.irh`), whose base 5d8 blast is likewise `tval: MA_LIVING` and whose prose correctly says "living creatures". It is a DELIBERATE Incursion divergence from the SRD -- the SRD Searing Light hits any creature, and Incursion narrowed both the code AND the spell prose to living creatures -- so the Circlet Desc is being brought into line with Incursion own design, not with the SRD. The code is correct: the `MA_LIVING` limit is deliberate, so nobody should broaden it to match the old prose. Reworded only the "5d8 damage to everything else" clause to "5d8 damage to living creatures", keeping the 10d6-undead and 5d6-construct clauses; the base `EA_BLAST` `tval: MA_LIVING`, the sibling blasts, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same over-broad sentence against the same `MA_LIVING` code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The base `EA_BLAST` `tval: MA_LIVING` limit and the Searing Light twin "living creatures" prose prove the intended wording; guarded structurally by `tools/check_circlet_blasting_prose.sh`, red while the "everything else" claim is present and green once the Desc says "living creatures" | no | inc-tek.8.8 |
| The "Horn of Plenty" (`lib/m_items.irh`) entity declared its `Desc` field TWICE: the first Desc names the fatigue cost -- "When blown, at the cost of fatiguing its bearer, this horn creates a rich pile of fresh fruits and vegetables" -- that the entity `On Event EV_EFFECT` handler charges via `LoseFatigue(4,false)`, while the second, later Desc ("this horn conjures a pile of fresh food at the blower feet") omitted the fatigue cost entirely; the parser keeps only one Desc, so the duplicate was dead but misleading prose. The code is correct: the horn does fatigue its bearer, so the surviving Desc must say so. Deleted only the second, fatigue-less Desc field; the first Desc, the `EV_EFFECT` `LoseFatigue(4,false)` handler, the `EV_MAGIC_STRIKE` message pair, `Level`, `Flags` and every other entity are left alone, and the entity still parses with exactly one Desc. Not a port artefact: `lib/` is unchanged by the port, this is a duplicated prose field in an item declaration, and a Win32 build with the original data ships the same second fatigue-less Desc against the same `LoseFatigue(4,false)` code (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `LoseFatigue(4,false)` handler and the surviving fatigue-naming Desc prove the intended wording; guarded structurally by `tools/check_horn_plenty_prose.sh`, red while the entity holds two Desc fields (the fatigue-less second present) and green once it holds exactly one Desc that names the fatigue cost | no | inc-tek.8.8 |
| The "Wand of Striking" (`lib/m_items.irh`) fires a telekinetic bolt: a primary `EA_BLAST` with `sval: REF partial`, and a separate `and EA_BLAST` knockback rider (`xval: AD_KNOC`) that carried no `aval`. With no `aval` the rider did not share the bolt's strike, so `Magic::MagicStrike` (`src/Magic.cpp`) resolved it as its own strike and rolled a SECOND, independent Reflex save at its `SavingThrow` call; a target therefore made TWO Reflex saves against one bolt. The wand's own `Desc` promises a SINGLE save: "A Reflex save halves the damage, but on failure the target will also be knocked back." Brian ruled prose wins -- one Reflex save governs both. Gave the rider `aval: AR_BOLT` so it shares the bolt's strike, where the same-`sval` guard in `MagicStrike` makes no reroll, and added a `PRE(EV_MAGIC_HIT)` handler that returns `DONE` for the knockback segment (`efNum` 1) when the target made that one save (`e.Resist`): success = half damage and no knockback, failure = full damage and knockback. Only the Striking wand's rider and handler changed; the `Desc`, `Level`, `Flags` and every other entity are left alone. The mechanism mirrors Priest Spell "Holy Orb" (`lib/abilities.irh`), whose `AD_KNOC` rider is already `aval: AR_BOLT` with the same `efNum == 1 && e.Resist` handler. Not a port artefact: `lib/` is unchanged by the port, this is item data in an item declaration, and a Win32 build with the original data and compiler ships the same riderless-`aval` second save (`lib/m_items.irh`) | **Traced** -- the second save follows from the rider's missing `aval`: a first-in-strike segment always saves at the `SavingThrow` call in `Magic::MagicStrike`, and the same-`sval` reroll guard suppresses a second save only WITHIN one shared strike, so a separate-strike rider saves again. A clean OBSERVED gameplay A/B is not scriptable without seed-hunting -- the marginal knockback rate is unchanged (both builds knock back exactly when the knockback save fails); only its correlation with the damage save changes, so a visible difference needs a seed where the target makes the damage save yet fails the independent knockback save. Guarded structurally by `tools/check_striking_wand_knockback.sh`, red while the rider lacks `aval: AR_BOLT` or the `efNum == 1 && e.Resist` handler is absent and green once both hold | no | inc-tek.8.8 |
| The "Elemental Command (Earth)" ring (`lib/m_items.irh`) named the wearer's own item "the ring of air" in its curse clause -- "The bearer of the ring of air does suffer a minor curse ... a -4 penalty to saving throws versus petrification" -- yet the entity is unambiguously the Earth ring: its `META(EVICTIM(EV_STRIKE))` handler tests `isMType(MA_EARTH)`, its `EV_MAGIC_HIT` handler grants `COMMAND_ABILITY` `MA_EARTH`, and its staff-spell list is the earth spells (soften stone, stoneskin, meld into stone, wall of stone, stone tell, tremor); the "-4 versus petrification" penalty is itself the earth ring's correct curse, so the ring NAME is the copy-paste error, not the penalty. The code is correct: nothing needs to change to match the prose. Renamed only the curse clause's "ring of air" to "ring of earth"; every handler, grant, staff-spell and value is left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same wrong name against the same earth-typed handlers (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `MA_EARTH` handler tests, the `COMMAND_ABILITY` `MA_EARTH` grant and the earth staff-spell list prove the ring is the Earth ring; guarded structurally by `tools/check_earth_ring_prose.sh`, red while the curse clause reads "the ring of air" and green once it reads "the ring of earth" | no | inc-tek.8.8 |
| The "Elemental Command (Water)" ring (`lib/m_items.irh`) introduced its staff-spell list in prose as spells "related to elemental fire", yet every spell in the list is a water spell -- water walking, part water, wall of ice, ice storm, create water and waterspout -- and the entity is unambiguously the Water ring: its `META(EVICTIM(EV_STRIKE))` handler tests `isMType(MA_WATER)`, its `EV_MAGIC_HIT` handler grants `COMMAND_ABILITY` `MA_WATER`, and it grants the wearer water breathing; the "elemental fire" phrase is a copy-paste of the Fire ring's own staff-spell introducer. The code is correct: nothing needs to change to match the prose. Changed only the introducer "elemental fire" to "elemental water"; the spell list, every handler, grant and value is left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same wrong introducer against the same water-typed handlers and water spell list (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The all-water spell list, the `MA_WATER` handler tests and the `COMMAND_ABILITY` `MA_WATER` grant prove the ring is the Water ring; guarded structurally by `tools/check_water_ring_prose.sh`, red while the introducer reads "elemental fire" and green once it reads "elemental water" | no | inc-tek.8.8 |
| The "Dragonshield" shield (`lib/m_items.irh`) stated in its Desc that the item "is a +1 (or higher) light fortification fearsome shield", yet the entity's `Constants` set `INITIAL_PLUS +2`, so the shield is generated at +2 and never appears below +2; the stated "+1" floor is unreachable. The code is correct: the +2 initial plus is deliberate, so nobody should lower it to match the old prose. Changed only the stated floor in that Desc clause from "+1 (or higher)" to "+2 (or higher)"; `INITIAL_PLUS`, the `AQ_LIGHT_FOR` and `AQ_FEARSOME` qualities, the chromatic-dragon-hostility clause, `Level`, `Flags` and every other entity are left alone. Not a port artefact: `lib/` is unchanged by the port, this is prose in an item declaration, and a Win32 build with the original data ships the same unreachable "+1" floor against the same `INITIAL_PLUS +2` (`lib/m_items.irh`) | **Traced** -- prose-only correction; no in-play value moves and no gameplay observation applies. The `INITIAL_PLUS +2` constant proves the shield never generates below +2, so the intended floor is +2; guarded structurally by `tools/check_dragonshield_plus_prose.sh`, red while the Desc floor reads "+1 (or higher)" and green once it reads "+2 (or higher)" | no | inc-tek.8.8 |

### Fixes deliberately NOT listed, because they are ours and not upstream's

Recording these matters as much as the table above: claiming our port artefact
is upstream's bug is the credibility cost this document opens with.

| Fix | Why it is ours |
|---|---|
| `*((long*)&hm)` destroyed the player's position in every save | Harmless on Win32, where `long` is 4 bytes. Our typedef narrowing created the overrun. The gate's worked example. |
| `int32` etc. were 64-bit off Windows; `src/AbiCheck.cpp` now gates the widths | The typedefs were correct for LLP64. This is the port meeting the base code, not a defect in it. |
| `src/RComp.cpp` is inside `#ifdef DEBUG` while generated `yygram.cpp` calls it unconditionally | A build-configuration collision that only a non-Windows build reaches. |
| `argv[0]` resolved to an absolute path at startup | Visual Studio always supplied an absolute `argv[0]`, so Windows never saw it. Borderline -- a Win32 user launching from `cmd` in the game directory would get a bare filename -- but there is no evidence it misbehaves there, and no-evidence means do not claim it. |
| macOS window dimming, and the input failure traced to the machine | Platform-specific, and the second was not a code defect at all. |
| Saves keyed on the layout they were written with | A port-format problem that the base code cannot have. |
| The `#ifdef DEBUG` VM breadcrumb's *speed* cost | Upstream ships without `DEBUG` and never paid it. Only the unbounded write inside it is theirs. |
| The pathfinding terrain cache, and clearing only the map that exists | A performance improvement, not a defect fix. Nothing was incorrect before it. |
| `AuditMap`'s orphan sweep reported every mounted or engulfed creature as map corruption | `src/MapAudit.cpp` is this port's own diagnostic. Upstream ships no map audit, so it cannot hold this defect. The exemption carried an `upstream:` marker written by habit; the marker is gone and the row moved here (`inc-nen`). |

### Base-code fixes that arrived before this port

`esran` fixed three base-code defects in the `networkingguru` fork before this
port began, and they are in the tree unmarked: the god-abandonment bug
(961c54b, 2021), grey dwarves' missing racial weapons (ceceebf, 2021), and
`Race::HasSkill` reading only the first six skills (9cbc308, 2025). They are
upstream defects by the gate's test, but they are not this port's work and
carry no evidence we generated. They are listed here rather than marked in the
source, so the marker convention keeps meaning "this port found and fixed it".

**This table was completed by inc-iqh on 2026-08-16**, which swept the port's
history for base-code defects. It is complete as of that date for fixes made in
this repository. `tools/check_upstream_marks.sh` still cannot find an unmarked
fix on its own -- nothing tells it which diffs were bug fixes -- so the rule in
AGENTS.md and CLAUDE.md remains the only thing that keeps this table current.

## Current status of our issues

### Sent to rmtew

| PR | Tier when sent | Status as of 2026-08-20 |
|---|---|---|
| [#42](https://github.com/rmtew/incursion-roguelike/pull/42) zero-initialise `Target` at its eight construction sites | **Observed** -- 89,545 asserts to 0 over 250 seeds, against a build differing in nothing else | **MERGED** 2026-08-15 |
| [#41](https://github.com/rmtew/incursion-roguelike/pull/41) bounds-check `Map::GetAt()` | **Tier as sent: Observed** -- 444 errors in 13s, then 0 across 877 turns. **That tier is withdrawn** (inc-5xn, 2026-08-18): the guard is in `GetAt` and those reads arrive through `Map::At` by another route, so it cannot have caused the difference. Re-measured: 8 reads of 47,962, no player-visible change | open. rmtew asked on 2026-08-15 what the player observes; the correction was **posted 2026-08-19** to issue #40, [comment 5345710784](https://github.com/rmtew/incursion-roguelike/issues/40#issuecomment-5345710784). It withdraws the monster-AI cause, gives the level-builder cause with 500-seed evidence, and offers to close #41 unless he prefers to keep the guard. Awaiting his answer |
| [#43](https://github.com/rmtew/incursion-roguelike/pull/43) `MoveDepth` re-entrancy | **Reasoned** -- sent as hardening, explicitly NOT as a crash fix, after the crash claim was retracted | open. rmtew's three questions were answered 2026-08-18, [comment 5329016046](https://github.com/rmtew/incursion-roguelike/pull/43#issuecomment-5329016046). See inc-upw.15 |
| [#44](https://github.com/rmtew/incursion-roguelike/pull/44) compare handles, not addresses, in `TargetSort` | **Observed** -- seed 8 went 1 divergence in 6 runs to 18 identical in 18; randomisation off gave 30 of 30 identical against 4 of 15 diverging with it on | sent 2026-08-16 |

**Read the pattern before sending anything else.** The one that merged is the one
whose evidence was a number that moved on both sides of a controlled run. That is
exactly what this document predicts, and it was the first real-world confirmation
the gate had.

**Then #43 complicated the picture, in a useful direction.** On 2026-08-15 rmtew
answered it -- not with a merge and not with a rejection, but with three questions:
how many times can the function call itself, what is the provable maximum stack
from recursive calls, and why not size the array from the number of followers.

Two lessons, and the second one costs more than the first.

*A weak tier does not mean silence.* #43 went as **Reasoned**, the lowest tier in
this document, and it is the one that got engagement. The tier governs what we may
CLAIM, not whether a patch is worth sending. What earned the reply was almost
certainly that the patch was small, honest about being hardening rather than a
fix, and did not ask him to trust a number.

*An unanswered question costs more than a rejected patch.* His comment sat for two
days before anybody here noticed, and it was found by accident while auditing
documentation. A maintainer who asks and gets nothing has been told what our
attention is worth. Watch the submissions, not just the queue: `gh api
repos/rmtew/incursion-roguelike/pulls/N --jq .comments` is the cheap check, and
nothing in this project was doing it.

**Why #44 was the next one sent, and it is not only the tier.** #41 is Observed
too and has not moved, so meeting the evidence bar is necessary and not
sufficient. The sharper difference is how much work the patch asks of a
maintainer who does not know this fork. #42 changed nothing any caller could
observe except the bug, so it could be verified in isolation. #41 makes
`Map::GetAt()` return NULL where it used to return a square, which cannot be
accepted without auditing every accessor that funnels through it. #44 sits on
the #42 side: the comparator's contract is unchanged and only its tiebreak input
moves, so nothing outside the function needs re-reading. Whether that reading is
right is now a live prediction rather than a claim, and #44 is the test of it.

Note that #41's tier is Observed and it still has not moved. Meeting the bar buys
a fair hearing, not a merge. Nothing is owed by a maintainer who never asked for
our help.

### Not sent

This table takes only issues we decided NOT to report and have NOT fixed; a
defect fixed here goes in *Base-code bugs fixed locally* above, whose fourth
column this three-column table would silently drop.

| Issue | Tier | Why not |
|---|---|---|
| `Thing::Remove` corruption | **Observed** -- 57 occurrences logged | no diagnosis, so no patch; fileable as an observation |
| `FI_SIZE` inconsistency | **Observed** once; not reproduced | one sighting is not a report |
| window flicker | **Reasoned** -- cause unknown, one hypothesis already disproved | fails the gate; also macOS-only |
| `Magic::Augury`, 3 defects | **Traced** -- never executed | not until Observed |

Base-code defects fixed here but never submitted are marked in the source and
listed above under *Base-code bugs fixed locally*. Marking is not sending.
