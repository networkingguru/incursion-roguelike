# lib/dispatch.h is generated by the resource compiler from inc/Api.h.
# A line-wise merge can produce duplicate dispatch numbers that do not compile.
# On a conflict here, take either side and run
# BACKEND=posix ./build_macos.sh TWICE, then commit what the compiler produced.
lib/dispatch.h -merge
